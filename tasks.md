# tasks.md — Madar Build Roadmap

Status legend: `[x]` done · `[~]` in progress · `[ ]` pending

This roadmap follows the **build sequence in `CLAUDE.md`** (Phase 1 → 4) and references the design bundle at `docs/design/` (HTML/JSX prototypes from Claude Design). The job for each screen is to **recreate the prototype in Next.js 14 + Tailwind + shadcn/ui** — not to copy the prototype's structure verbatim. Match the visual output.

---

## Already done

- [x] `CLAUDE.md` — project instructions, design system, build sequence, security rules
- [x] Design bundle imported at `docs/design/` (prototypes for tenant app, admin app, auth, print receipts; design tokens; Arabic translations)
- [x] Chat transcripts read — intent captured (specialty coffee chain "Bayt Coffee Co.", Cairo, 5 branches, AI-first owner dashboard, bilingual EN/AR)

---

## Phase 1 — MVP Foundation

Goal: a working bilingual POS that decrements inventory on sale and accepts bank-transfer receipts. Ends with super-admin MVP (10 pages).

### 1.1 Project scaffolding
- [x] Root `package.json` + `pnpm-workspace.yaml` (Turborepo + pnpm)
- [x] Root `tsconfig.base.json`, `.gitignore`, `.editorconfig`, `.env.example`, `.nvmrc`
- [x] `apps/web/` scaffold (Next.js 14 + next-intl + Tailwind v4)
- [x] `apps/admin/` scaffold landed in 1.6b (Next.js 14 on :3001, English-only, slate-teal). `apps/api/` landed in 1.5a (NestJS 10 on :4000).
- [x] `packages/ui/` scaffold (tokens + fonts + Tailwind theme)
- [x] `packages/db/` scaffold. `packages/shared/` and `packages/config/` remain deferred — Dockerfiles no longer reference them, so deployment is unblocked.
- [ ] `docker-compose.yml` — deferred (no backend this slice)
- [ ] GitHub Actions CI skeleton — deferred

### 1.2 i18n + RTL infrastructure (build BEFORE any UI)
- [x] `next-intl` set up in `apps/web` with `[locale]/...` routing
- [x] `<html lang dir>` flipped per locale; `dir="rtl"` for `ar`
- [x] `messages/en.json` + `messages/ar.json` seeded (89 POS keys, AR sourced from `docs/design/project/i18n-ar.js`)
- [ ] ESLint rule: no hardcoded user-facing strings — deferred
- [x] `pnpm i18n:check` script: validates AR keys match EN, fails on drift
- [ ] Admin app: deferred
- [x] Logical-properties via Tailwind v4 (native) + per-component `ms-`/`me-`/`ps-`/`pe-` usage
- [x] Glossary: `docs/i18n-glossary.md` exists (pre-existing)

### 1.3 Design system package `packages/ui`
- [x] Port `docs/design/project/design-tokens.css` → `packages/ui/src/tokens.css`
- [x] Font loader: Fraunces, Inter Tight, JetBrains Mono, IBM Plex Sans Arabic
- [x] Tailwind v4 `@theme inline` block that consumes CSS variables as theme tokens
- [x] Light + dark variants (`[data-theme="dark"]`)
- [x] Accent variant: `terracotta` (tenant) and `theme-admin` class for slate teal
- [ ] Base components: deferred — built as POS screen needed them inline; will extract when admin app starts
- [x] Lucide React icons wired (stroke 1.5) — used throughout the POS screen
- [ ] Visual regression harness — deferred to later slice

### 1.4 Database + RLS — DONE, runtime-verified
- [x] `packages/db/prisma/schema.prisma` — Phase 1 models: `tenants`, `users`, `branches`, `products`, `categories`, `customers`, `tenant_bank_accounts`, `stock_movements`, `branch_stock`, `sales`, `sale_lines`, `payment_proofs`, `subscription_invoices`, `audit_log`, `platform_users`, `platform_audit_log`, `platform_bank_accounts`, `plans` (18 models, 10 enums)
- [x] All tenant-scoped tables have `tenant_id`, `created_at`, `updated_at`, `created_by`, `deleted_at` + soft-delete index
- [x] Money as `bigint` cents + sibling `currency_code`
- [x] Translatable fields as `jsonb` `{ en, ar }`
- [x] RLS policies on every tenant-scoped table — `ENABLE` + `FORCE` RLS, `tenant_isolation` policy with USING + WITH CHECK; recognizes `app.current_tenant_id` and bypasses on `app.is_super_admin = 'true'`
- [x] Migration `20260514000000_init/migration.sql` (immutable) — hand-appended `updated_at` triggers, append-only audit triggers, RLS DDL
- [x] **Migration `20260514010000_app_role`** — non-superuser `madar_app` role added in a follow-up after runtime verification revealed RLS wasn't enforcing: the original `madar` role (set via `POSTGRES_USER` in docker-compose) is a Postgres superuser, which bypasses RLS regardless of `FORCE`. App now connects via `DATABASE_URL=madar_app`; migrations still run via `DIRECT_DATABASE_URL=madar`.
- [x] **Migration `20260514020000_rls_policy_nullif`** — fixes the `USING`/`WITH CHECK` cast: `current_setting('app.current_tenant_id', true)::uuid` errored with `22P02` when the GUC was unset because Postgres does not short-circuit `OR`. Wrapped in `NULLIF(...,'')` so empty becomes `NULL`.
- [x] Seed script — uses `adminPrisma` (with `app.is_super_admin` bypass) so it works under enforced RLS. Demo tenant "Bayt Coffee Co. / بيت كوفي", 5 Cairo branches, 21 bilingual products, owner + 5 cashiers, 3 customers, sample sale TX-94819, payment proofs (sale + subscription contexts).
- [x] `pnpm test:rls` — **43 passing**: 13 tenant-scoped models × 3 assertions (tenantScoped sees only own, adminPrisma sees both, basePrisma sees 0 = FORCE-RLS canary) + 2 WITH CHECK rejections + 2 append-only audit blockers.
- [x] psql canary confirmed: tenant-scoped GUC → 21 rows; no context → 0 rows; super-admin GUC → all rows.

### 1.5 Auth (tenant)
#### 1.5a — backend + login + signup (DONE)
- [x] `apps/api` scaffolded as `@madar/api` (NestJS 10, ts-node + node --env-file). Boots on :4000. `/healthz` probes DB.
- [x] `apps/api/src/tenant/auth/` module: signup, login, refresh, logout, me, slug-available — controllers, services, DTOs (zod), token service (JWT HS256, 15m access + 30d refresh, jti rotation in Redis with reuse-detection), audit service writing `audit_log` rows via `tenantScoped` (and via `adminPrisma` inside the signup bootstrap).
- [x] `TenantAuthGuard` (APP_GUARD) — verifies access JWT, asserts `realm:'tenant'` AND `typ:'access'`; rejects admin-realm tokens (canary verified manually).
- [x] `IdempotencyInterceptor` (signup) + `RateLimitGuard` (sliding window via Redis, in-memory fallback when REDIS_URL unset).
- [x] Refresh cookie: `madar_refresh; HttpOnly; SameSite=Lax; Path=/`; rotation + family-revocation on reuse. (Path was originally `/v1/auth` for tighter scoping — relaxed to `/` because the Next.js server-side `requireAuth()` guard reads the cookie on shell routes like `/en` which don't match the auth path. Cookie stays HttpOnly + SameSite=Lax so the security delta is small.)
- [x] Password hash: argon2id (memoryCost 19MiB, timeCost 2, parallelism 1, hashLength 32 — OWASP 2024).
- [x] `docker-compose.yml` adds `redis:7-alpine`; `.env`/`.env.example` carry `JWT_TENANT_SECRET`, TTLs, `REDIS_URL`, `NEXT_PUBLIC_API_URL`, `API_CORS_ORIGIN`.
- [x] `apps/web` shared infra: TanStack Query provider, Zustand auth store (in-memory; refresh cookie + /me is the durable layer), `apiFetch` wrapper with mutex-guarded 401 refresh-and-retry, zod schemas mirroring API DTOs, `useDebouncedValue` hook, server-side `requireAuth(locale)` guard wired into `[locale]/(shell)/layout.tsx`.
- [x] `apps/web/src/app/[locale]/(auth)/` route group: split-screen layout (logical CSS, RTL-natural), `LocaleToggle` (EN/ع pill switcher), login page (RHF + zod, show/hide password, forgot-link parked behind `aria-disabled` until 1.5b), signup page (all prototype fields, debounced 300ms slug availability indicator with checking/free/taken/reserved/invalid states, country→currency mirror).
- [x] i18n: `auth.*` namespace added to `en.json` AND `ar.json` (262 keys parity, `pnpm i18n:check` green).
- [x] Migration impact: packages/db downgraded from `"type":"module"` to CJS so NestJS (CJS) can import `@madar/db` cleanly via ts-node. `pnpm test:rls` still 43/43 green.
- [x] Manual end-to-end verified via curl: `/healthz`, slug-available (taken/free/reserved/invalid), signup (happy + Idempotency-Key replay returns 201 + duplicate slug 409), login (happy + bad password 401), /me with valid + admin-realm canary 401, refresh rotation + reuse-detected 401, logout, rate limit (slug at request 31 returns 429), `/en/login` and `/ar/signup` render 200, `/en` redirects to `/en/login` when no refresh cookie.
- [x] Design reference followed: `docs/design/project/Madar Auth.html` — split-screen layout, field order, copy tone, brand-rail atmospheric card.
- [x] **1.5b — Vitest integration test suite** for the auth module — 35 tests across 7 spec files under `apps/api/test/auth/` (signup 9, login 7, slug-available 6, refresh 4, logout 3, me 3, realm-canary 3). Boots a real NestJS app via `@nestjs/testing` + supertest against a real Postgres (RLS-enforced via `madar_app` role) and the in-memory Redis fallback. Asserts: cookie attrs (HttpOnly/SameSite=Lax/Path=/, Max-Age vs session), audit_log rows (`signup_complete`, `login_success`, `login_failure`, `login_blocked_tenant_status`, `refresh_token_rotated`, `logout`), refresh rotation + family revocation on replay, idempotency key replay returns identical body, rate limits enforced when `NODE_ENV=production`, realm canary rejects admin-realm and refresh-as-access tokens with `code: 'access_expired'`. `pnpm test:api` 35/35 green, `pnpm test:realm` 3/3 green, `pnpm test:rls` still 43/43.

#### 1.5b — email-bound flows (DONE)
- [x] **Schema migration `20260516020000_user_auth_extras`** — hand-edited SQL adding to `users`: `email_verified` (bool default false), `email_verification_token_hash` + `email_verification_expires_at`, `password_reset_token_hash` + `password_reset_expires_at`, `mfa_enabled` (bool default false), `mfa_recovery_codes_hash` (text[]). Partial indexes on both token-hash columns. No RLS changes — the new columns inherit the existing `tenant_isolation` policy.
- [x] **Backend endpoints** under `apps/api/src/tenant/auth/`:
  - `POST /v1/auth/forgot-password` — `@Public`, neutral 200 on unknown email (no enumeration), 5/min rate limit keyed by email. Generates `randomBytes(32).hex` token → stores SHA-256 → sends `password_reset` email with `${TENANT_WEB_ORIGIN}/${locale}/reset-password?token=…`. Audit `password_reset_requested`.
  - `POST /v1/auth/reset-password` — `@Public`, 10/min. Hash inbound token → lookup → validate `expires_at`. argon2id-hash new password, clear token columns, `revokeAllRefreshTokensForUser(userId)` so stolen cookies die. Codes: `invalid_token` 404, `reset_token_expired` 410, `weak_password` 400 via existing PasswordSchema. Audit `password_reset_completed`.
  - `POST /v1/auth/verify-email` — `@Public`, 30/min. Idempotent — first call flips `email_verified=true` and clears the token; subsequent calls with the same token return 404 because the hash is gone. `verify_token_expired` 410.
  - `POST /v1/auth/resend-verification` — `@Public`, 3/h keyed by email. Neutral 200; if user exists + `!email_verified`, regenerates token + sends `email_verification` email. Audit `email_verification_resent`.
  - **Signup flow patched** — `AuthService.signup()` now writes an `email_verification_token_hash` inside the same transaction and fires-and-forgets the `email_verification` email alongside the existing `welcome` email.
- [x] **Email templates** — `password_reset` + `email_verification` added to `apps/api/src/common/email/templates.ts` (EN + AR shells with RTL `<html dir="rtl">`). New typed `PasswordResetVars` + `EmailVerificationVars` interfaces in `email.types.ts`. `SendInput` union widened. Both use the recipient user's locale (personal email convention).
- [x] **Env**: `PASSWORD_RESET_TTL_HOURS=1`, `EMAIL_VERIFICATION_TTL_HOURS=24`, both with sensible defaults. `.env.example` updated.
- [x] **Token storage**: SHA-256 hex of raw token stored in DB so a compromised DB can't issue valid links. Raw token only travels in URL + email body. Recovery codes use argon2id (user-typed, defense against timing leakage).
- [x] **Frontend tenant**:
  - `apps/web/src/app/[locale]/(auth)/forgot-password/page.tsx` — email field → neutral success card ("If an account exists for that email…"). Links back to /login.
  - `apps/web/src/app/[locale]/(auth)/reset-password/{page,reset-password-client}.tsx` — server page extracts `?token=` from `searchParams` and forwards to client. Two-field form (new + confirm) with strength meter, mismatch validation, inline error mapping (`invalid_token`, `reset_token_expired`, `weak_password`). Success → CTA → /login.
  - `apps/web/src/app/[locale]/(auth)/verify-email/{page,verify-email-client}.tsx` — auto-fires verify on mount. Sage check + `successTitle` on 200, amber warning + Resend form on `expired`, rose X on `invalid`.
  - **Password strength meter** at `apps/web/src/app/[locale]/(auth)/_components/PasswordStrengthMeter.tsx` — pure 5-bar based on length≥8/length≥12/lower+upper/digit/symbol. Rose → amber → sage gradient. Mounted in `signup/page.tsx` reserved slot and in `reset-password-client.tsx`.
  - Login page "Forgot password?" link now navigates to `/forgot-password` (was previously `aria-disabled "coming soon"`).
- [x] **`/me` widened** — `AuthService.me()` and `AuthService.toUserDto()` now include `email_verified: boolean` and `mfa_enabled: boolean`. `AuthUser` type widened in `apps/web/src/lib/auth/store.ts`. Bootstrap flow passes through unchanged.

#### 1.5c — MFA (DONE)
- [x] **`MfaService`** at `apps/api/src/tenant/auth/mfa.service.ts` — `otplib` wrapper with the same defaults the admin uses (SHA1, 30s, 6 digits, ±1 window). `generateSecret`, `keyUri`, `verifyTotp`, `generateRecoveryCodes` (10 × `xxxx-xxxx`, lowercase, base32 alphabet sans easily-confused chars `0/1/o/l`), `hashRecoveryCodes` (argon2id), `findRecoveryCodeIndex`. `isRecoveryCode` regex routes the input correctly.
- [x] **`TokenService` extended** — `mintMfaPending` (5-min `mfa_pending` JWT, realm:`tenant`, jti tracked in Redis `mfa-pending:tenant:{jti}` for single-use). `verifyMfaPending` + `isMfaPendingAlive` + `consumeMfaPending`. `revokeAllRefreshTokensForUser` SCANs `refresh:{userId}:*`. New env `JWT_TENANT_MFA_PENDING_TTL=5m`.
- [x] **`TenantMfaGuard`** at `apps/api/src/tenant/auth/tenant-mfa.guard.ts` — mirrors `AdminMfaGuard`, validates header `Bearer <mfa_pending_token>`, checks `isMfaPendingAlive`, surfaces `{userId, tenantId, jti}` via `CurrentMfaChallenger` decorator.
- [x] **Login flow modified** — when `user.mfa_enabled=true`, login mints an mfa_pending token and returns `{ requires_mfa: true, mfa_pending_token, expires_in }` instead of the access pair. No refresh cookie is set. Audit `login_mfa_pending`.
- [x] **New endpoints**:
  - `POST /v1/auth/mfa/verify` (`@Public` + `TenantMfaGuard`) — body `{ code }` accepts TOTP or recovery code. Recovery match removes that hash from the array (single-use). Always consumes the `mfa_pending` jti. Audit `mfa_verify_success` (with `method: "totp" | "recovery_code"`) or `mfa_verify_failure`. 10/min rate limit.
  - `POST /v1/auth/mfa/enroll/start` (`TenantAuthGuard`, `@Idempotent`, owner/manager — actually any authed user; `assertNotImpersonating()`) — generates fresh otplib secret, returns `{ provisioning_uri, secret_b32 }`. Caches candidate secret in Redis `mfa-enroll:{userId}` for 10 min. 409 `mfa_already_enabled` if already on.
  - `POST /v1/auth/mfa/enroll/verify` (`TenantAuthGuard`, `assertNotImpersonating()`) — reads candidate, verifies TOTP, persists `mfa_secret` + `mfa_enabled=true` + 10 hashed recovery codes. Returns `{ recovery_codes: string[] }` **exactly once**. Audit `mfa_enabled`. 410 `enroll_expired` if Redis TTL passed.
  - `POST /v1/auth/mfa/disable` (`TenantAuthGuard`, `assertNotImpersonating()`) — body `{ password }`, argon2-verify, clears `mfa_secret` + `mfa_recovery_codes_hash` + `mfa_enabled=false`. Audit `mfa_disabled`. 401 `invalid_credentials`, 409 `mfa_not_enabled`.
- [x] **Frontend MFA challenge** — `apps/web/src/app/[locale]/(auth)/_components/MfaChallenge.tsx` — 6-digit individual-box pad (auto-advance on digit, Backspace navigates back, arrow-keys navigate, paste-distributes 6 digits, Enter-submits) **plus** "Use a recovery code instead" toggle that swaps to a single `xxxx-xxxx` input. Ported (intentionally duplicated) from the admin login client per CLAUDE.md realm-isolation policy.
- [x] **Login page** rewritten as a `Step = "creds" | "mfa"` state machine. After `loginRequest` returns, `isMfaPending()` discriminator switches to step 2. `mfa_pending_invalid` bounces back to creds.
- [x] **Tenant settings shell** — new minimal `/[locale]/settings` route group at `apps/web/src/app/[locale]/(shell)/settings/`: `layout.tsx` (sidebar with 4 sections, only Security enabled), `page.tsx` (redirects to `/security`), `_components/SettingsShell.tsx`. Sidebar Settings entry flipped `enabled: true` in `apps/web/src/app/[locale]/(shell)/_components/Sidebar.tsx:81`.
- [x] **Security page** at `/[locale]/settings/security` — reads `useAuthStore.user.mfa_enabled`, shows a card with **Enable** or **Turn off** button. Refreshes auth-store via `/me` after either action.
  - **Enable**: 3-step `MfaEnrollWizard` modal — Step 1 QR code (via `qrcode.react@^4`) + manual key in monospace. Step 2 six-digit pad. Step 3 displays all 10 recovery codes in a 2-col mono grid with **Copy all** + **Download (.txt)** + required "I've saved these somewhere safe" checkbox before Close.
  - **Disable**: `DisableMfaModal` — confirm-password modal → POST `/mfa/disable`.
- [x] **API wrappers** at `apps/web/src/lib/api/auth.ts` — `forgotPasswordRequest`, `resetPasswordRequest`, `verifyEmailRequest`, `resendVerificationRequest`, `mfaVerifyRequest` (Bearer-overridden), `mfaEnrollStartRequest`, `mfaEnrollVerifyRequest`, `mfaDisableRequest`. New `LoginResponse = AuthSession | MfaPendingResponse` + `isMfaPending()` discriminator.
- [x] **i18n** — new namespaces: `auth.forgot.*`, `auth.reset.*`, `auth.verify.*`, `auth.passwordStrength.*`, `auth.mfa.challenge.*`, `auth.mfa.enroll.*`, `auth.mfa.disable.*`, `settings.nav.*`, `settings.security.*`. EN + AR in lockstep at **830 keys** (was 743). Glossary terms used: التحقق بخطوتين, كلمة المرور, رموز الاسترداد, التحقق من البريد.
- [x] **Tests** — 7 new spec files / **40 new tests** under `apps/api/test/auth/`:
  - `forgot-password.spec.ts` (5): no-enumeration (unknown returns 200, no audit, no token), known email writes hash + audit, missing email 400, malformed email 400.
  - `reset-password.spec.ts` (6): happy + old password rejected + new accepted, invalid 404, expired 410, replay 404, weak password 400, revoke-all-refresh effect (old cookie now 401 `refresh_replayed`).
  - `verify-email.spec.ts` (4): happy + `email_verified=true` + audit, replay 404, expired 410, invalid 404.
  - `login-with-mfa.spec.ts` (3): regression for `mfa_enabled=false`, `mfa_enabled=true` returns `requires_mfa` + no cookie, mfa_pending claims validate (realm/typ/user_id/tenant_id).
  - `mfa-verify.spec.ts` (6): TOTP happy + cookie + audit method=totp, wrong TOTP 401 `mfa_invalid` + audit failure, valid recovery code consumes from array + audit method=recovery_code, recovery replay 401, mfa_pending single-use (replay rejected), missing Bearer 401 `mfa_pending_missing`.
  - `mfa-enroll.spec.ts` (5): start returns secret + URI, start when already enabled 409, verify happy → enabled + 10 recovery codes + audit, verify with wrong code 401, verify without start 410 `enroll_expired`.
  - `mfa-disable.spec.ts` (4): happy clears columns + audit, wrong password 401 unchanged, not-enrolled 409, impersonation 403 `forbidden_during_impersonation`.
- [x] **Deps**: `qrcode.react@^4` added to `apps/web/package.json` for the enrollment QR.
- [x] **Verification**: `pnpm db:migrate` applied cleanly. `pnpm --filter @madar/api typecheck` + `web` + `admin` all clean. `pnpm test:rls` 43/43. `pnpm test:api` **308/308** (268 baseline + 40 new). `pnpm i18n:check` 830 keys lockstep. `pnpm --filter @madar/web build` clean — new routes: `/forgot-password` (3.42 kB / 185 kB First Load), `/reset-password` (3.42 kB / 185 kB), `/verify-email` (8.93 kB / 184 kB), `/settings` (142 B / 153 kB), `/settings/security` (13.7 kB / 185 kB).
- [x] **Welcome-email test patched** — the new `email_verification` template that fires at signup means filenames containing the test email's "welcome-XXXXXX" local-part are ambiguous. Updated `welcome-email.spec.ts` to filter candidates by the `X-Madar-Template: welcome` header content rather than filename.

#### 1.5 deferred
- [x] **PAGES.md §46 Business Settings (DONE)** — new self-service tenant module `apps/api/src/tenant/business/` exposing `GET /v1/tenant` (any authed reader) and `PATCH /v1/tenant` (owner-only, 30/min). Schema migration `20260527000000_tenant_business_settings` adds `BusinessType` enum (`retail|wholesale|restaurant|pharmacy|services|other`) + 4 Tenant columns: `legal_name` (nullable text), `business_type` (nullable enum), `timezone` (text default `'Africa/Cairo'`), `fiscal_year_start_month` (smallint default 1, `CHECK (1..12)`). Service does only-changed-fields diff audit (`tenant_updated` with `before`/`after`), argon2-pattern timezone validation via `Intl.DateTimeFormat` (returns 400 `invalid_timezone` on bogus zones), catches Prisma errors cleanly. DTO zod `refine` rejects empty bodies; currency code regex-validated `^[A-Z]{3}$`. Owner gate enforced inside the service (mirrors `users.service:assertOwner`). New web route `/[locale]/settings/business` (4 cards: Identity / Operations / Tax defaults / Lifecycle read-only) with bilingual name tabs, currency / timezone / fiscal-month / default-locale pickers, default-tax toggle, sticky-bottom Save bar with dirty tracking. SettingsShell `business` flipped `enabled: true` + `visibleToRoles:["owner"]`. On save: refetches `/me` to refresh the auth store. New typed client `apps/web/src/lib/api/business.ts`. New i18n namespace `settings.business.*` (~44 keys) + `settings.nav.business` EN+AR lockstep at **2324 keys**. Tests `apps/api/test/business/business.spec.ts` **7/7**: GET happy + cashier ok, PATCH cashier 403 / empty body 400 / bad currency regex / invalid_timezone (row unchanged) / happy with diff audit. New route bundle: 8.48 kB / 190 kB First Load.
- [x] **PAGES.md §45 Profile/Account page (DONE)** — three new self-service endpoints `PATCH /v1/auth/me`, `POST /v1/auth/change-password`, `POST /v1/auth/change-email`. PATCH /me accepts `{name?, locale?}` with a zod `refine` rejecting empty bodies + diffs only the changed fields into the audit row (action `profile_updated`). change-password argon2-verifies current → rejects same → argon2id-hashes new → `TokenService.revokeAllRefreshTokensForUser` (mirrors reset-password — every other device session dies) + audit `password_changed` + clears the refresh cookie so the client lands on `/login`. change-email argon2-verifies password, catches Prisma P2002 on `(tenant_id, email)` → 409 `email_taken`, flips `email_verified=false`, mints a fresh `email_verification_token_hash`, fire-and-forgets the existing `email_verification` template to the NEW address, audits `email_changed`. All three rate-limited; PATCH /me 30/min, the password/email mutations 5/min. New web route `/[locale]/settings/profile` (Identity / Password / Security-summary / Notifications-placeholder cards) reusing `PasswordStrengthMeter` + initial-on-gradient avatar; ChangePasswordModal triggers `clearAuth() + window.location.assign('/{locale}/login')` on success; ChangeEmailModal toasts on success + refetches `/me`. SettingsShell `profile` entry flipped `enabled: true`. New i18n namespace `settings.profile.*` (~46 keys) EN+AR lockstep at **2240 keys**. Tests `apps/api/test/auth/profile.spec.ts` **10/10**: PATCH happy×2 + empty 400, change-password happy with refresh-cookie revocation proof + wrong 401 + weak 400 + same 400, change-email happy with `.eml` on disk and verified flipping false + duplicate 409 + wrong password 401. Avatar upload + Notifications matrix remain explicitly deferred (no schema for either; both are independent slices). `readAuditLog` test helper extended to surface `before` alongside `after` so the diff assertion works (also benefits future tests). New route bundle: 8.77 kB / 185 kB First Load.
- [ ] Gating tenant-app access on `email_verified` (column ships unused — no behavior depends on it yet).
- [ ] Owner-resetting another user's MFA from a future Users settings page.
- [ ] Email-verification flow on email-change after signup. — Done as part of §45 above (the new email goes through the existing `email_verification` template + `/verify-email?token=` route).
- [x] **Recovery-code regenerate flow** — backend `POST /v1/auth/mfa/recovery-codes/regenerate` (password-gated, audit-logged as `mfa_recovery_codes_regenerated`, rate-limited 5/min, replaces `mfa_recovery_codes_hash` atomically — old codes invalidated the moment the new set is written). Frontend `RegenerateRecoveryCodesModal` on `/settings/security` next to "Turn off"; two-stage flow (password → display fresh 10 codes once with Copy-all). New i18n namespace `auth.mfa.regenerate` + `settings.security.regenerateButton` (en+ar, 13 new keys × 2; lockstep at 893).
- [ ] SMS / WebAuthn second factors.

### 1.6 Auth (admin) — separate realm
#### 1.6a — backend (DONE)
- [x] `apps/api/src/admin/auth/` module: login → mfa_pending token (5min) → mfa/verify → access (8h) + refresh (8h) cookie. Endpoints: `POST /v1/admin/auth/{login,mfa/verify,refresh,logout}` + `GET /v1/admin/auth/me`. JWT signed with **distinct** `JWT_ADMIN_SECRET`, audience `madar.admin`, realm `admin`, three typs (`mfa_pending`, `access`, `refresh`). Refresh cookie name `madar_admin_refresh` (distinct from `madar_refresh`).
- [x] `AdminAuthGuard` (typ:`access`) + `AdminMfaGuard` (typ:`mfa_pending`) — applied per-route via `@UseGuards`, NOT as `APP_GUARD`. `TenantAuthGuard.canActivate` early-returns for `/v1/admin/*` paths so the two realms don't collide (`apps/api/src/tenant/auth/tenant-auth.guard.ts`).
- [x] MFA via `otplib` (SHA1, 6 digits, 30s step, ±1 window — matches the seed's `otpauth://` URL). `mfa_enabled=false` users blocked at login with `code: "mfa_not_enrolled"` (enrollment lands in a later slice).
- [x] `platform_audit_log` writes via `adminPrisma` for `admin_login_password_ok`, `admin_login_password_fail`, `admin_login_mfa_ok`, `admin_login_mfa_fail`, `admin_token_refreshed`, `admin_logout`.
- [x] env vars added: `JWT_ADMIN_SECRET`, `JWT_ADMIN_ACCESS_TTL=8h`, `JWT_ADMIN_REFRESH_TTL=8h`, `JWT_ADMIN_MFA_PENDING_TTL=5m`. `.env` + `.env.example` updated.
- [x] Vitest integration suite — 27 tests across 6 spec files in `apps/api/test/admin-auth/` (login 6, mfa-verify 7, refresh 4, logout 3, me 3, realm-canary 4). Plus 1 new case in `apps/api/test/auth/realm-canary.spec.ts` (tenant guard rejects a real admin-secret-signed access token).
- [x] `pnpm test:api` 63/63 green (35 tenant + 27 admin + 1 reverse canary), `pnpm test:realm` 8/8 (both realm-canary specs), `pnpm test:rls` 43/43 (no regression), typecheck clean.
- [x] Seed demo super-admin `admin@platform.test / Admin123!` + stable TOTP secret — already in `packages/db/prisma/seed.ts:57-71`.

#### 1.6b — frontend (DONE)
- [x] Scaffolded `apps/admin/` (Next.js 14, English-only, port 3001) — `package.json`, `next.config.mjs`, `postcss.config.mjs`, `tsconfig.json`, `.eslintrc.json`, `next-env.d.ts`. `<html lang="en" data-theme="light" class="theme-admin">` pins the slate-teal accent via the existing `packages/ui/src/tokens.css:103-113` swap.
- [x] Admin login at `/login` — two-step state machine in `apps/admin/src/app/(auth)/login/login-client.tsx`: credentials (RHF + zod, show/hide password, "Keep me signed in" — currently visual) → 6-box MFA code (auto-advance on digit, backspace navigates back, paste handler distributes digits, Enter submits when complete, helper links "← Back to password" + "Use recovery code" parked as `aria-disabled`). Calls `POST /v1/admin/auth/login` then `POST /v1/admin/auth/mfa/verify`. Error mapping per code: `invalid_credentials`, `mfa_not_enrolled`, `mfa_invalid`, `mfa_pending_invalid`, `rate_limited`, network.
- [x] Auth client infra under `apps/admin/src/lib/`: `api/client.ts` (apiFetch with 401-refresh **mutex** pointing at `/v1/admin/auth/refresh`), `api/admin-auth.ts` (login/verifyMfa/me/logout wrappers), `auth/store.ts` (Zustand — `{ accessToken, user, bootstrapped }`, no tenant context), `auth/bootstrap.tsx` (mount-time refresh-if-cookie), `auth/server.ts` (`requireAdminAuth()` server guard reading `madar_admin_refresh` cookie), `query/{client,provider}.ts` (TanStack Query), `validation/admin-auth-schemas.ts` (zod), `copy.ts` (English-only labels, no next-intl).
- [x] Protected home placeholder at `/` (server-gated by `requireAdminAuth`, renders `Welcome, {name}` + role + MFA badge + sign-out button). Real dashboard ships with 1.14.
- [x] Backend touchups: `apps/api/src/env.ts` `API_CORS_ORIGIN` accepts comma-separated list (transform + pipe to `z.array(z.string().url())`); `.env` + `.env.example` updated to `"http://localhost:3000,http://localhost:3001"`. NestJS `enableCors` already accepts string[]. Root `package.json` gained `dev:admin` script.
- [x] Verification: `pnpm --filter @madar/admin typecheck` clean. `pnpm --filter @madar/admin build` succeeds (login route 29 kB / 116 kB First Load, home route 5.2 kB). `pnpm test:api` still 63/63 (CORS regression check). Dev server on :3001 confirmed: `GET /` → 307 → `/login`, `GET /login` → 200 with `<html class="theme-admin">`.

#### 1.6c — hardening (deferred)
- [ ] Optional IP allowlist per super-admin — docs flag "optional, not v1".
- [ ] **TOTP enrollment endpoint + QR display** — needs a flow redesign because admin login currently demands `mfa_enabled=true`. Two options to unblock: (a) extend `admin-auth.login` to return a short-lived `must_enroll_mfa` session when `mfa_enabled=false`, scoped to only the enroll endpoints; (b) ship a CLI / admin-of-admins bootstrap that flips MFA on for a new platform user with a one-time secret printed. Today's seed uses option (b) implicitly. The TOTP secret generation + key URI utilities (`AdminMfaService.generateSecret/keyUri`) and recovery-code helpers are needed before either approach.
- [ ] **Admin recovery codes** — depends on the enrollment flow above. The tenant `MfaService.generateRecoveryCodes/hashRecoveryCodes/findRecoveryCodeIndex` is the template; just needs an admin equivalent + `PlatformUser.mfa_recovery_codes_hash` column (already in schema? — check before wiring).

### 1.7 Multi-tenancy data layer
- [x] `tenantScoped(tenantId)` Prisma client — `$transaction([set_config('app.current_tenant_id', …, TRUE), query])` pattern (transaction-local — survives `SET LOCAL`'s scope) in `packages/db/src/tenant.ts`
- [x] `adminPrisma` — same pattern, sets `app.is_super_admin = 'true'` — singleton in `packages/db/src/admin.ts`, **never used in tenant app**
- [x] **ESLint realm-boundary rule** — `apps/api/.eslintrc.json` (`no-restricted-imports`): tenant code (`src/tenant/**`) cannot import `adminPrisma` or `basePrisma`; admin code (`src/admin/**`) cannot import `tenantScoped` or `basePrisma`. `apps/web/.eslintrc.json` + `apps/admin/.eslintrc.json` ban `@madar/db` / `@prisma/client` outright — frontends talk to the API. Pre-existing legitimate cross-realm uses (signup audit, pre-auth lookups in `auth.service`, platform-table reads in billing/branches) carry justification comments + `eslint-disable-next-line`. The shared `payment-proofs-shared` module is exempt by path (it dispatches by `actor.realm`).
- [x] **Per-call tenant scoping (effectively the "middleware")** — every tenant service does `tenantScoped(req.user.tenantId)` with `tenantId` from the `@CurrentUser()` decorator off `TenantAuthGuard`. No separate NestJS middleware: each tenant query wraps itself in a `$transaction` that sets `app.current_tenant_id` transaction-locally (a long-lived session middleware can't, since Prisma reuses pool connections). `pnpm test:rls` proves the wrapper holds — 43 assertions across 13 tenant-scoped models.
- [x] **Impersonation JWT minting (admin → tenant)** — `TokenService.mintImpersonationAccess` issues a 1h access token carrying `impersonator_id` + `impersonator_email`, jti tracked in Redis for revoke-on-exit. `ImpersonationService` is gated to `owner` / `support` admin roles, writes `impersonation_started` to `platform_audit_log`, and `TenantAuthGuard` rejects revoked jtis with `impersonation_revoked`. Shipped in 1.14b.

### 1.8 Product catalog
#### 1.8a — read-only catalog backend + inventory page wired to real API (DONE)
- [x] `apps/api/src/tenant/catalog/` module: `GET /v1/categories`, `GET /v1/products?[search&category_id&only_low_stock&limit]`. Both authed via global `TenantAuthGuard`; rate-limited 60/min/IP. Service uses `tenantScoped(req.user.tenantId).product.findMany(...)` + a `$queryRawUnsafe` GROUP BY against `branch_stock` for SUM(qty_on_hand) and MIN(reorder_point). Velocity stub = 0 for now (1.8d).
- [x] Money over wire: `BigInt.prototype.toJSON = String()` shim in `apps/api/src/main.ts` — `price_cents`/`cost_cents` ride as JSON strings ("3500").
- [x] `apps/web/src/lib/api/catalog.ts` typed wrappers + `catalog-adapter.ts` that maps API shape → legacy `Product`/`Category` so the existing `inventory-client.tsx` filter/sort/bulk-edit code is untouched (color derived from deterministic hash of `product.id`, `reorder_point ?? Infinity` so unset means no Low badge).
- [x] `inventory-client.tsx` rewired: `useQuery(['catalog','products'])` + `useQuery(['catalog','categories'])` in parallel, `<InventorySkeleton/>` / `<InventoryEmpty/>` / `<InventoryError onRetry/>` states.
- [x] `inventory.empty.*`, `inventory.error.*`, `inventory.skeleton.*` keys added to `en.json` + `ar.json` (270 keys parity).
- [x] End-to-end verified via curl + browser fetch: 401 without token, categories (6 with product_count), products (21 with bilingual names + cents-as-string + SUM(qty_on_hand) across 5 branches), `?search=esp` filter, `?only_low_stock=true` returns just `BNS-001 Yirgacheffe 250g` (14<20). `pnpm test:rls` still 43/43 green; typecheck + lint + i18n:check green.
- [x] Translatable names (`{ en, ar }`) flow through the API — Arabic product names render in `/ar/inventory`.

#### 1.8b — mutations (DONE)
- [x] `POST /v1/products`, `PATCH /v1/products/:id`, `DELETE /v1/products/:id` + `/categories` mutations — `apps/api/src/tenant/catalog/`. All authed via global `TenantAuthGuard`, role-gated to `owner|manager` (cashier 403), POSTs `@Idempotent()` + rate-limited 30/min. Service writes via `tenantScoped(tenantId).$transaction` and per-mutation `auditLog` rows via `AuditService.writeTenantScoped` (actions: `product_created|_updated|_deleted`, `category_created|_updated|_deleted`). Soft-delete sets `deleted_at=now()` + `is_active=false`. Category delete rejects with `400 category_in_use` if any non-deleted product still references it.
- [x] **`POST /v1/products` accepts optional `initial_stock`** — array of `{branch_id, qty, reorder_point?, reorder_qty?}`. For each entry with `qty > 0` writes one `stock_movement` (`kind='adjustment'`, `note='initial_stock'`) and upserts `branch_stock`. Branch IDs validated against `tenantScoped.branch.findMany` first.
- [x] **`GET /v1/branches`** — new tiny `apps/api/src/tenant/branches/` module so the product form can populate the per-branch stock table. RLS-scoped, 60/min rate limit.
- [x] **Impersonation stub** — new `apps/api/src/tenant/auth/impersonation.helper.ts` exporting `assertNotImpersonating(user, opName)`. Wired into `softDeleteProduct` + `softDeleteCategory`. No-ops today (`req.user.impersonator_id` always undefined); 1.14b will plumb the real claim. Returns `403 forbidden_during_impersonation` once active — CLAUDE.md mandate.
- [x] Product add/edit pages (PAGES.md §15) — `apps/web/src/app/[locale]/(shell)/inventory/products/{new,[id]/edit}/`. Both render the same `<ProductForm>` (new at `_components/ProductForm.tsx`, 600 lines). Sectioned cards: Basics (EN/AR name tabs, category dropdown), Identification (SKU uppercase-coerced, Barcode), Pricing (price/cost in major units, live Margin readout), Initial stock (per-branch table, create-only), Status (Active toggle). Sticky footer Save/Cancel. RHF-style controlled inputs + zod-equivalent client-side validation; surfaces 409 `sku_taken` as inline field error.
- [x] Bulk action bar shows the existing Edit price / Adjust stock / Print labels buttons but they remain no-ops — sheet implementations deferred. The row context menu (`⋯`) however now opens a real popover with `Edit` (links to `/inventory/products/[id]/edit`) and `Delete` (confirm → soft-delete API call with toast on success, `forbidden_during_impersonation` localized error path wired). New `<RowActionsMenu>` component handles click-outside.
- [x] Typed API wrappers in `apps/web/src/lib/api/catalog.ts`: `productCreateRequest`, `productUpdateRequest`, `productDeleteRequest`, `categoryCreateRequest`, `categoryUpdateRequest`, `categoryDeleteRequest`, `productGetRequest`, `branchesListRequest`. `apiFetch` already mints `Idempotency-Key` for non-GET requests.
- [x] i18n: `inventory.form.*` + `inventory.row.{edit,delete,confirm*}` + `auth.common.remove` namespaces added to `en.json` + `ar.json` (386 keys parity, `pnpm i18n:check` green).
- [x] Tests: 3 spec files / 17 integration tests in `apps/api/test/catalog/` — product mutations (happy create, initial_stock writes branch_stock + stock_movement, duplicate SKU 409, missing AR 400, cashier 403, happy PATCH + audit, 404 PATCH, soft-delete + idempotent, cashier-DELETE 403), category mutations (create + duplicate 409, self-parent 400, `category_in_use` 400, soft-delete happy), branches (RLS canary 2-tenant + 401 anonymous).
- [x] **Bug fix uncovered**: `apps/api/src/admin/payment-proofs/payment-proofs.module.ts` was missing the `AdminAuthModule` import — `AdminAuthGuard` couldn't resolve `AdminTokenService` at runtime. The 1.11a admin tests were never actually run (Docker offline at time of write); this slice's `pnpm test:api` exercised them for the first time and surfaced the broken DI graph. Fix: imported `AdminAuthModule` (matching pattern in `DashboardModule` / `TenantsModule`). All 27 admin payment-proof tests now green.
- [x] Verification: `pnpm test:api` **154/154** (was 110 baseline before counting the broken 27 admin-payment-proof specs + the 17 new catalog specs), `pnpm test:rls` **43/43**, `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/web typecheck` + `build` clean (new routes: `/inventory/products/new` 379 B / 136 kB First Load, `/inventory/products/[id]/edit` 961 B / 137 kB), `pnpm i18n:check` green.
- [x] **Bulk Edit Price / Adjust Stock / Print Labels sheets** — `apps/web/src/app/[locale]/(shell)/inventory/_components/{BulkEditPriceModal,BulkAdjustStockModal,PrintLabelsSheet}.tsx`. **Edit Price:** two modes (Set absolute / Adjust by %), live preview against first row, loops `PATCH /v1/products/:id`, progress counter, impersonation guard. **Adjust Stock:** branch picker + signed `qty_delta` + `adjustment | waste` kind toggle + required note, loops `POST /v1/stock-adjustments` (the 1.9 endpoint), surfaces per-row `negative_stock` with product name. **Print Labels:** full-screen 3-col grid with SKU/name/price tiles, `@media print` hides toolbar, `window.print()` drives output. New i18n namespace `inventory.bulk.{editPriceModal,adjustStockModal,printLabels}` in EN+AR (934 keys lockstep). Barcode SVGs + thermal-printer adapter park for a later slice.
- [x] **Search expansion to `name_i18n->>'en'` and `name_i18n->>'ar'`** — `catalog.service.ts:listProducts` now prefilters matching IDs via `$queryRawUnsafe` ILIKE across `sku`, `name_i18n->>'en'`, and `name_i18n->>'ar'`, then runs the typed `findMany` with `id IN (…)`. Prisma's JSON `string_contains` is case-sensitive so raw ILIKE was the only clean option; pattern escapes `\ % _` to keep user-supplied wildcards literal. Limit applied to the prefilter to bound the IN-list.

#### 1.8c — product detail page (DONE, PAGES.md §14)
- [x] Route `/[locale]/inventory/products/[id]` — two-column layout (main content + sticky 280px sidebar). Header: image header (image or gradient swatch fallback) + display-serif name + uppercase kicker (category · sku) + active/inactive status pill + branch count + Edit button linking to the existing edit page.
- [x] Three tabs (Overview / Stock / Activity) — defers Pricing chart + Variants + Suppliers to Phase 2 since they need new schema (price_history, variants, supplier_products).
  - **Overview tab** — Description card (en/ar pick by locale, fallback en, no-description placeholder), Category + Barcode mini cards, Pricing card with display-serif Price / Cost / Margin %.
  - **Stock tab** — Per-branch table (Branch code+name, On hand, Reorder at, Available, Last movement relative time) + stock-movements timeline below (last 50, paginated server-side at limit=50, kind chip color-coded sale=rose / receive=sage / adjustment=amber / transfer_in|out=accent / waste=rose).
  - **Activity tab** — Audit log feed filtered to `entity=product AND entity_id=:id`. Each row shows actor name + i18n action label + relative time. Rose `via impersonation` note when `impersonator_id` is set.
- [x] Sticky sidebar with 4 KPI cards (display serif values): Total stock value (cost × on-hand summed across branches), Units sold (30d), Velocity / day (1 decimal), Days of cover ("∞" when velocity = 0).
- [x] Inventory list `ProductsTable` now wraps the product name in a `<Link href="/inventory/products/[id]">` — the row context menu (`⋯`) still goes to `/edit`. Both navigation entry points behave correctly.
- [x] Backend: new `GET /v1/products/:id/detail` (extends `ApiProduct` with `per_branch_stock[]` + `kpis`), `GET /v1/products/:id/movements?[page,limit]` (paginated, max 200), `GET /v1/products/:id/activity?[page,limit]`. All RLS-scoped via `tenantScoped`; movements uses the `(tenant_id, branch_id, product_id, occurred_at DESC)` index. Reserved-stock column doesn't exist yet — `available = qty_on_hand` with a `1.9` TODO comment for the stock-transfers slice.
- [x] i18n: `inventory.detail.*` namespace (~60 new keys: tabs, headers, kicker labels, kpi labels, movement kinds, empty states, activity action labels). EN + AR in lockstep (515 keys total, `pnpm i18n:check` green).
- [x] Tests: `apps/api/test/catalog/product-detail.spec.ts` (7 cases): detail shape with per_branch_stock + kpis, KPI computation after a sale (units_sold_30d > 0, velocity_per_day correct, days_of_cover > 0), unknown id 404, movements list (qty_delta sign correct, branch_code joined), activity list (product_created + product_updated present, ordered DESC), RLS canary (tenant B → 404), pagination skip correctness.

#### 1.8d — telemetry (DONE)
- [x] Real velocity computation in `apps/api/src/tenant/catalog/catalog.service.ts` — `listProducts()` runs a parallel `$queryRawUnsafe` against `stock_movements` (`SUM(ABS(qty_delta))` over the last 7 days, filtered by `kind='sale'` and the product ID list). `getProduct()` does the same for one product. `qty_delta` is negative for sales (per `apps/api/src/tenant/sales/sales.service.ts:182-195`), so `ABS` gives the unit count. `velocity_per_week` is no longer hardcoded `0`.
- [x] Tests: `apps/api/test/catalog/product-velocity.spec.ts` (4 cases): zero baseline before any sale, after a 3-unit sale velocity is 3 for that product and 0 for siblings, `getProduct` agrees with `listProducts`, 8-day-old movement does not count (window cutoff).

#### 1.8e — images (DONE)
- [x] Migration `packages/db/prisma/migrations/20260516000000_add_product_image_url/migration.sql` — `ALTER TABLE products ADD COLUMN image_url TEXT`. Hand-written. Inherits the existing `tenant_isolation` RLS policy.
- [x] `ImageProcessor` extended for `image/webp` — sharp `.webp({ quality: 85 })` re-encode. JPEG/PNG/PDF flows unchanged. `SupportedMime` union now includes `image/webp`.
- [x] **Backend `apps/api/src/tenant/catalog/catalog.service.ts`** — three new methods reusing the 1.11d image pipeline (file-type magic bytes → virus scan → sharp resize+EXIF strip → storage):
  - `setProductImage(tenantId, productId, actorId, file, ctx)` — 5MB max, MIME allow-list `["image/jpeg", "image/png", "image/webp"]`, magic-byte detection, virus scan, resize, writes to `tenants/{tid}/products/{pid}.{ext}`, updates `Product.image_url`, audits `product_image_set` with `before`/`after`. Deletes the previous file best-effort if its extension differs.
  - `clearProductImage(tenantId, productId, ctx)` — nulls `image_url`, best-effort `storage.delete()` of old bytes, audits `product_image_cleared`.
  - `streamProductImage(tenantId, productId)` — reads `image_url`, fetches via storage interface, returns `{buffer, mime, filename}`. MIME derived from extension.
- [x] **Controller routes** at `apps/api/src/tenant/catalog/catalog.controller.ts`:
  - `POST /v1/products/:id/image` (`@Idempotent()` + `FileInterceptor` 5MB, owner|manager role) — multipart upload, returns updated product.
  - `DELETE /v1/products/:id/image` (owner|manager) — clears.
  - `GET /v1/products/:id/image` (any authed tenant user) — Bearer-streamed binary, 5-min private cache.
  - `GET /v1/public/tenants/:tenantId/products/:id/image` (`@Public()`, rate-limited 120/min/IP, 10-min public cache) — no-auth read for `<img src>` tags. Intentional security tradeoff vs Bearer streaming, documented in code; signed URLs land in a hardening slice.
- [x] **Frontend** — `ProductForm` replaces the previously-disabled drop zone with a working `<ImageBlock>`: 96×96 preview + Upload/Replace + Remove buttons. Create flow: image is held client-side (`URL.createObjectURL` preview) and uploaded after `productCreateRequest` succeeds; failure does not roll back the product. Edit flow: replace/remove fire immediately via `productSetImageRequest` / `productClearImageRequest`. MIME accept attr `image/jpeg,image/png,image/webp` + size check at 5MB before submit.
- [x] `ProductsTable.tsx` — product cell renders an `<img>` thumbnail via `productImagePublicUrl(tenantId, id, image_url)` when `image_url` is set; falls back to the existing deterministic gradient swatch when null. Product name now wraps in a `<Link>` to `/inventory/products/:id`.
- [x] Typed API wrappers at `apps/web/src/lib/api/catalog.ts`: `productSetImageRequest` (multipart), `productClearImageRequest`, `productImagePublicUrl(tenantId, productId, imageUrl)` — returns absolute `${API_URL}/v1/public/...?v=<ext>` URL (extension acts as a content hash for cache-busting on replace), null when no image.
- [x] i18n: `inventory.form.image.*` namespace (section/upload/replace/remove/hint/tooLarge/uploadFailed) added EN + AR.
- [x] Tests: `apps/api/test/catalog/product-image.spec.ts` (7 cases): happy upload (image_url set + audit row), missing file 400 `image_required`, non-image MIME (text/plain) 400 `file_mime_unsupported`, cashier 403, public GET returns `image/*` Content-Type + `Cache-Control: max-age`, DELETE clears + audits `product_image_cleared`, RLS canary (tenant B uploading to tenant A's product → 404).
- [x] **Verification across 1.8c/d/e**: `pnpm test:api` **204/204** (was 186 + 18 new), `pnpm test:rls` 43/43, `pnpm db:migrate` applied the new migration cleanly, `pnpm --filter @madar/web build` succeeds with `/inventory/products/[id]` at 10.7 kB / 134 kB First Load, `/inventory/products/[id]/edit` 962 B / 138 kB (image upload added), `/inventory/products/new` 379 B / 138 kB (image upload added). `pnpm i18n:check` green at 515 keys lockstep. API + admin + web typecheck all clean.

##### Cross-slice cleanup
- [x] POS rewired to catalog API in 1.10b. `apps/web/src/lib/mock-data/products.ts` + `categories.ts` are now type-only consumers (their array values are tree-shaken); a follow-up cleanup can rename the modules to `types/product.ts` if desired.
- [x] Branch switcher binds nothing today (chain-wide aggregate only). Branch-scoped catalog view lands with stock-transfers (Phase 2). — Shipped in Phase 2.1: switcher is wired via `useBranchScopeStore` + `branchesListRequest`, and `/v1/products?branch_id=` returns per-branch `qty_on_hand`.
- [x] Image upload pipeline (1.8e shipped)
- [x] Products list page at `/[locale]/inventory` (dense table with sort, filter, bulk-edit, AI nudge)
- [x] Product detail page at `/[locale]/inventory/products/[id]` (1.8c — Overview/Stock/Activity tabs + sticky KPI sidebar)
- [x] Create page at `/[locale]/inventory/products/new` + Edit page at `/[locale]/inventory/products/[id]/edit` (1.8b)
- [x] Design reference followed: `docs/design/project/screen-inventory.jsx` + PAGES.md §14
- [x] **Categories tree view + add/edit pages (PAGES.md §16)** — `apps/web/src/app/[locale]/(shell)/inventory/categories/{page.tsx,categories-client.tsx,categories.css}`. Two-column layout: left tree (recursive, collapsible, sorted by `sort_order` then EN name; `product_count` badge per node), right sticky detail panel (name EN/AR, parent, product count, sort order). Header `Add category` button + per-node `Add child / Edit / Delete` actions. Inline modal dialog for create/edit (code, name EN/AR, parent select with cycle-prevention, sort order); inline confirm dialog for delete. Surfaces backend errors `category_code_taken`, `self_parent`, `category_in_use`, `forbidden_during_impersonation`. New i18n namespace `categoriesPage` in en/ar (50 keys × 2 — 878 total in lockstep). Inventory header gains a `Categories` link (`<FolderTree>` icon). Drag-to-reorder/reparent from PAGES.md is deferred to a polish slice — sort order is editable from the form.

### 1.9 Single-branch inventory
- [x] **`branch_stock.qty_on_hand` (cache) + `stock_movements` (ledger)** — schema shipped in 1.4 (`packages/db/prisma/schema.prisma`); write hooks live in `catalog.service` (initial_stock), `sales.service` (sale completion), `stock-transfers.service` (transfer commit/receive). Cache + ledger always advance in the same transaction so they cannot drift.
- [x] **Movement types** — `enum StockMovementKind { sale | return_in | transfer_in | transfer_out | adjustment | receive | waste }`. Manual back-office adjustments only emit `adjustment` or `waste`; the rest are driven by their parent module so surrounding state stays consistent.
- [x] **Manual stock-adjustment endpoint** — `apps/api/src/tenant/stock/`: `POST /v1/stock-adjustments` (owner/manager, idempotency-keyed, rate-limited 30/min). Body: `{ branch_id, product_id, qty_delta, kind?, note (required), unit_cost_cents? }`. Service validates branch + product exist under tenant, rejects deltas that would push qty negative (`409 negative_stock`), writes one `stockMovement` + upserts `branchStock` in a single transaction, then `AuditService.writeTenantScoped(action='stock_adjusted')` with before/after qty.
- [x] **Stock-movements ledger endpoint (PAGES.md §18)** — `GET /v1/stock-movements` (owner/manager/auditor/accountant). Filters: `branch_id`, `product_id`, `kind`, `from`/`to` (YYYY-MM-DD or ISO). Paginated (default 50, max 200), ordered by `occurred_at DESC`. Hydrates `branch_code` + `product_sku` + `product_name_en` in-memory from a second batched query so the index scan stays cheap.
- [x] Inventory list page with stock levels, hover left-border, bulk-edit action bar, low-stock badges (visual)
- [x] Reorder-point detection in mock data; low-stock rose stripe on rows + Low badge
- [ ] **Stock-levels pivot view (PAGES.md §17)** — backend pivot endpoint + Next.js page deferred (substantial UI work; per-branch view already exists on the inventory page via the branch-scope switcher).
- [x] **Dedicated stock-movements ledger UI (PAGES.md §18) (DONE)** — new route `/[locale]/inventory/movements` (`apps/web/src/app/[locale]/(shell)/inventory/movements/{page,movements-client}.tsx` + `movements.css`). Filter strip: From/To dates (default last 7d), Branch dropdown (defaults from `useBranchScopeStore`), User dropdown (active owners + managers via the existing `approversListRequest`), single-select kind chip (`All | Sale | Return | Transfer in/out | Adjust | Receive | Waste`). Paginated 50/page table per PAGES §18: When · Product (SKU mono + bilingual name) · Branch · Type-chip (sage receive/return, rose sale/waste, accent transfer, amber adjust) · signed Qty (sage `+N` / rose `-N` tabular) · truncated Reason · Reference deep-link (`sales/refunds → /sales/[id]/receipt`, `stock_transfers → /transfers/[id]`, `purchase_orders → /purchases/[id]`, `supplier_returns → /returns/[id]`) · User name. Row click → side drawer (overlay + inset-block-end-anchored panel) with all fields + currency-formatted unit cost + "Open linked entity →" button. Reached via a new "Movements" link in `InventoryHeader.tsx` (next to Categories) — no sidebar entry per PAGES spec. New typed client `apps/web/src/lib/api/stock-movements.ts`. Backend extensions: `list-movements.dto.ts` accepts `created_by` + `reference_table` filters; `stock.service.ts:listMovements` adds a third batched `user.findMany` to hydrate `created_by_name`, returns `product_name_i18n: {en, ar}` alongside the legacy `product_name_en` (back-compat for the product-detail page that already consumes it). 5 new tests in `apps/api/test/stock/movements.spec.ts` (first spec for this endpoint): cashier 403, happy + new fields populated, kind filter narrows + zero on `kind=receive` when no receives exist, `created_by` filter narrows + zero for an unrelated user, RLS canary (tenant B sees 0). `pnpm test -- stock sales catalog` **118/118** green. i18n: `inventory.movements.*` (~38 keys) + `inventory.actions.movements` (1) EN+AR lockstep at **2279 keys**. New route ships at 6.39 kB / 188 kB First Load.
- [ ] **Reorder alert pipeline** — depends on BullMQ + Resend (1.15 infra pass). Schema (`branch_stock.reorder_point`, `reorder_qty`) is in place; the daily worker that scans for `qty_on_hand <= reorder_point` + emits notifications lands once Redis-backed jobs ship.

### 1.10 POS sell screen (priority slice)
#### 1.10a — visual / mock-data first-slice (DONE)
- [x] Route `/[locale]/pos` — slim 48px header
- [x] 60/40 split: products left, cart right (`--paper` background on right)
- [x] Search bar 56px, autofocus, ESC clears, barcode-scanner friendly
- [x] Category pills row (horizontal scroll, accent active state)
- [x] Product grid `auto-fill, minmax(150px, 1fr)` — gradient swatch tiles + serif monogram + serif price 22px
- [x] Cart with qty steppers, line discount, kitchen note via `LineEditSheet` with number pad
- [x] Hero total — display serif 56px with currency code
- [x] Pay button — full width, 64px tall, accent-tinted shadow
- [x] Held-sales tray (parked tickets, Resume / Discard) — in-memory only; server-side persistence deferred
- [x] Empty cart state with illustration + Esc kbd hint
- [x] Customer attach (mock customer toggle) — wiring to real customers API is its own slice
- [x] PaymentSheet — Cash / Card / Transfer (visual)
- [x] Design reference followed: `docs/design/project/screen-checkout.jsx` and `docs/PAGES.md` §7–§9

#### 1.10b — backend wiring (DONE)
- [x] `apps/api/src/tenant/sales/` module: `POST /v1/sales` (Idempotent) + `GET /v1/sales/:id`. SaleService.completeSale runs one `tenantScoped.$transaction`: validate branch + products (RLS-scoped), snapshot `unit_price_cents` + `cost_cents` from current product rows (never trust client price), compute line totals server-side with discount clamp, aggregate subtotal/discount/tax (tax=0 for 1.10b)/total, generate `TX-${base36}` code with collision retry, create Sale + SaleLines, write one StockMovement (`kind='sale'`, negative qty_delta) per line + upsert BranchStock cache (negative-stock sales complete per CLAUDE.md), write `audit_log` `action='sale_completed'`.
- [x] Idempotency double-fenced: HTTP `Idempotency-Key` UUID via existing `IdempotencyInterceptor` (24h Redis cache) AND server-side dedupe on `(tenant_id, client_uuid)` unique key — any duplicate POST returns the original sale.
- [x] Payment methods: cash (validates `cash_tendered_cents >= total`, computes change, sets `payment_status='paid'`) and bank_transfer (sets `payment_status='payment_pending'`, inventory STILL commits — CLAUDE.md rule). Card/store_credit/split deferred to 1.10c.
- [x] Vitest integration suite — 21 tests across 6 spec files under `apps/api/test/sales/`: happy cash (3 assertion clusters covering sale, lines, movements, branch_stock, audit), bank_transfer (`payment_pending` + inventory commits), idempotency (3 replay cases), validation (7 cases incl. unknown_product, unknown_branch, insufficient_tendered), RLS (2-tenant canary on product + branch + cross-tenant 404), get-sale (line name expansion + 404 + 400 malformed). New helpers `makeTenantWithCatalog`, `readSaleWithLines`, `readStockMovements`, `readBranchStock`.
- [x] Frontend rewired: `pos-client.tsx` loads catalog via `useQuery(['catalog', 'products'/'categories'])` + existing `adaptProduct`/`adaptCategory`. New `apps/web/src/lib/api/sales.ts` typed `createSale` wrapper. PaymentSheet now async — `onSubmit(payment)` resolves single-method (cash or bank_transfer; card disabled). Cart percentage discount → cents at submit. Per-ticket `client_uuid` minted on first item; cleared on success. Toast confirms `TX-${code}` on completion.
- [x] Mock-data cleanup: `apps/web/src/app/[locale]/pos/_mock-data.ts` deleted. `Cart.tsx` + `ProductGrid.tsx` now import `Product`/`Category` from `@/lib/mock-data/{products,categories}` (still hosting the legacy types — array values are tree-shaken since nothing imports them now; full file deletion is a follow-up cleanup).
- [x] Verification: `pnpm --filter @madar/api typecheck` + `pnpm --filter @madar/web typecheck` clean. `pnpm --filter @madar/web build` succeeds (POS route 10.2 kB / 134 kB First Load). `pnpm test:api` 84/84 (35 tenant auth + 27 admin auth + 1 reverse canary + 21 sales). `pnpm test:realm` 8/8, `pnpm test:rls` 43/43.

#### 1.10c — payment methods + split (DONE)
- [x] **Card payment (slice 3)**: new `Sale.approval_code` column; DTO accepts `approval_code` (4–20 chars, required when `payment_method='card'`); `CardPaymentBody` component (`apps/web/src/app/[locale]/pos/_components/CardPaymentBody.tsx`) renders approval-code input + terminal hint + own submit button. Audit `after.payments[i].approval_code_last4` is masked to last 4. 4 vitest cases in `apps/api/test/sales/payment-card.spec.ts`. No payment gateway involved — manual entry only.
- [x] **Store credit (slice 4)**: append-only `store_credit_ledger` (RLS) + denormalized `customers.store_credit_balance_minor/store_credit_currency_code` cache. New tenant module `apps/api/src/tenant/store-credit/` exposes `GET /v1/customers/:id/store-credit` (paginated 100, reader = `owner|manager|accountant`) and `POST /v1/customers/:id/store-credit/adjust` (owner|manager, `@Idempotent`). Adjust runs inside an interactive `basePrisma.$transaction` that `SET LOCAL`s the RLS tenant id and `SELECT ... FOR UPDATE`s the customer row — necessary because `tenantScoped()`/`adminPrisma`'s `$extends` wrap each op in its own implicit tx, which would release the lock and let parallel adjusts double-spend. Same pattern adopted in `completeSale()` for store-credit payment slices. Signed `amount_minor` as string on the wire; currency locked on first credit (subsequent mismatches → 400 `currency_mismatch`); negative-result adjusts → 400 `insufficient_balance`; ledger row + customer cache always update atomically; `store_credit_adjusted` audit row with before/after balance. New FE: customer store-credit page (`apps/web/src/app/[locale]/(shell)/customers/[id]/store-credit/`) with balance card, ledger table, owner/manager `AdjustCreditModal`. POS `StoreCreditBody` renders available/after-sale rows and disables Apply when insufficient. New `apps/web/src/lib/api/customers.ts` typed client. 8 vitest cases in `apps/api/test/store-credit/store-credit.spec.ts`.
- [x] **Split tender (slice 5)**: new tenant-scoped `sale_payments` table — every sale (split or not) writes one or more rows. `Sale.payment_method` becomes a denormalized hint (`'split'` when count ≥ 2, else the single method). DTO accepts both legacy single-method shape and new `payments: SalePaymentInput[]` array (1–8 slices). `completeSale()` refactored into a per-slice loop that runs inside one `basePrisma.$transaction` (cash → compute change; card → check approval_code; store_credit → lock + insert ledger; bank_transfer → mark payment_pending). Sum-equals-total enforced exactly (off-by-1 → 400 `split_total_mismatch`). `SplitTenderBody` component renders 1–8 payment slices with method picker + amount input + method-specific field, footer shows paid/total/remaining and disables Complete until remaining = 0 AND slices ≥ 2. 6 vitest cases in `apps/api/test/sales/payment-split.spec.ts`. Backward compat: old clients sending `payment_method` + scalars still work — `normalizePayments()` synthesizes a single-element array.
- [x] **Reconciliation**: sales.service.completeSale() rewritten as the integration of all four method handlers + tax (slice 1 below) inside a single `basePrisma.$transaction` with `SET LOCAL` for RLS. `PaymentSheet.tsx` extended with 5 tabs (cash, card, transfer, store-credit, split) — store-credit and split each own their own submit button. `apps/web/src/lib/api/sales.ts` `CreateSaleInput` accepts both shapes. i18n at 1677 keys EN/AR lockstep.
- [ ] Real receipt-ref capture + S3 upload pipeline lands with 1.11 payment-proof module.

#### 1.10d — tax classes (DONE)
- [x] Schema + migration: `tax_classes` table (tenant-scoped, RLS), `products.tax_class_id` FK, `tenants.default_tax_class_id` + `tax_inclusive_default` + `tax_registration_number`. Applied in `20260521000000_pos_completeness`.
- [x] Backend module `apps/api/src/tenant/tax-classes/` — `GET/POST/PATCH/POST :id/set-default/DELETE /v1/tax-classes`. Reader = `owner|manager|accountant`, mutations owner-only. 409 `tax_class_code_taken` on dup, 409 `tax_class_in_use` when a product references it or it's the default. All audit-logged. Idempotency on POST. 11 vitest cases all green (`apps/api/test/tax-classes/tax-classes.spec.ts`).
- [x] Frontend: Settings → Tax classes tab (owner-only via `useAuthStore`), list + add/edit modal + set-default/delete actions. New `apps/web/src/lib/api/tax-classes.ts` typed client. ProductForm now exposes a "Tax class" select that defaults to "Use tenant default". Product DTOs (create/update) extended with `tax_class_id`; `ApiProduct` includes it. i18n keys added in EN+AR (`settings.taxClasses.*`, `inventory.productForm.taxClass.*`, `pos.payment.taxBreakdown.*`).
- [x] RLS test extended — `taxClass` added to canary model list.
- [x] **Sales-service tax computation**: `completeSale()` now loads each product's `tax_class_id` (or falls back to `tenants.default_tax_class_id`), pulls rates from `tax_classes` in one query, and computes per-line tax with the inclusive/exclusive formula. `sale_lines.tax_cents` and `sales.tax_cents` populated. POS `pos-client.tsx:TAX_RATE = 0` removed — cart preview shows 0 until the bootstrap exposes default_tax_class_id (deferred to a small follow-up); server returns the real tax_cents on completion. `PaymentSheet.tsx` header shows a tax row when `tax > 0`.
- [ ] **Deferred**: client-side tax preview in the cart (needs `AuthTenant.default_tax_class_id` + a `/v1/tax-classes?active_only=true` query result wired through to the cart computation).

#### 1.10e — held-sales persistence (DONE)
- [x] Server-side `held_sales` + `held_sale_lines` tables (RLS-enforced), tenant API at `GET/POST/POST :id/resume/DELETE /v1/held-sales`, cashier-scoped reads, owner/manager branch-wide, resume idempotency, audit events (`held_sale_created/resumed/discarded`). POS UI now hydrates from server and rehydrates the cart on Resume; HeldSalesTray renders real data (name, line count, total, time-ago, customer/cashier pill). i18n keys extended in EN+AR. RLS canaries added in `packages/db/test/rls.test.ts`. 24-hour visibility window. 8 vitest cases all green.

### 1.11 Payment + payment-proof module (shared)
#### 1.11a — backend (code complete; pending live test run)
- [x] `payment_proofs` table already in Prisma schema (Phase 1.4).
- [x] Shared `apps/api/src/payment-proofs-shared/` module — realm-agnostic service with submit/list/getOne/verify/reject/streamReceipt. State machine `pending → verified | rejected`. Side effects per context: sale → `Sale.payment_status` paid/disputed; subscription → `SubscriptionInvoice.status` paid + paid_at / awaiting_payment.
- [x] Tenant routes at `/v1/payment-proofs/*` (cashier may submit; verify/reject restricted to `owner|manager`). Admin routes at `/v1/admin/payment-proofs/*` (verify/reject restricted to Platform Owner / Finance). Realm + context guards: tenant calls only act on `sale`, admin only on `subscription` (cross-context → 403 `wrong_realm`).
- [x] Upload pipeline: Multer `FileInterceptor` 5MB cap → magic-byte MIME check via `file-type@16` (JPG/PNG/PDF) → NoopScanner (real ClamAV → 1.11d) → `sharp` resize ≤2000px long edge + EXIF strip + re-encode (JPG q=85 / PNG lossless; PDF pass-through) → LocalDiskStorage writes to `${STORAGE_ROOT}/tenants/{tid}/payment-proofs/{id}.{ext}`. Real S3/MinIO + signed URLs → 1.11d.
- [x] Receipt streaming via `GET /v1/payment-proofs/:id/receipt` (Bearer auth on read; signed URLs in 1.11d).
- [x] Audit: tenant `audit_log` for `payment_proof_submitted | _verified | _rejected`; `platform_audit_log` for `admin_proof_verified | _rejected`.
- [x] Idempotency: `@Idempotent()` on submit; rate-limit 30/min on multipart, 60/min on reads.
- [x] Env additions: `STORAGE_ROOT` (default `./var/storage`). `.gitignore` excludes `apps/api/var/`. Three new `@Global()` common modules: Storage / VirusScan / Image.
- [x] New deps: `sharp@^0.33.5`, `file-type@^16.5.4`, `@types/multer`.
- [x] 11 spec files under `apps/api/test/payment-proofs/` + `apps/api/test/admin-payment-proofs/` (submit happy × 2 contexts, validation, list, verify-sale, reject-sale, cross-context/role, receipt-stream, tenant realm-canary, admin list, admin verify, admin reject + tenant→admin realm canary).
- [x] `pnpm --filter @madar/api typecheck` clean.
- [ ] **Pending live `pnpm test:api` run** — requires Docker Desktop running so Postgres is reachable. Tests not exercised this session (Docker daemon offline).

#### 1.11b — tenant verifier UI (DONE)
- [x] POS PaymentSheet bank-transfer step: real `<input type=file>` (JPG/PNG/PDF, ≤5MB client-side check) + payer-name field. PaymentSubmit type extended to carry `{ receipt_file, transfer_reference, payer_name }`.
- [x] POS flow: after `createSale`, chain `submitPaymentProof` (multipart) with `context=sale`, the sale id, `bank_account_kind=tenant`, and the default `tenant_bank_account.id` from `/v1/tenant-bank-accounts`. **Decoupled failure** — if proof upload fails, sale stays in `payment_pending` and a fallback toast tells the cashier to re-upload from `/sales/verification`.
- [x] `/[locale]/sales/verification` supervisor queue — two-pane (380px list + flex detail), filter chips (Pending default / Verified / Rejected / All), URL-state `?selected=`, client-side oldest-first sort, `context=sale` only. Approve → Sale.payment_status = paid; Reject (reason code + notes) → payment_status = disputed.
- [x] `/[locale]/sales/verification/[id]` — sharable single-proof view with back link.
- [x] Sidebar nav entry "Verification" added under the **money** section. Role-gated via new `roleGuard: (role) => role === 'owner' || role === 'manager'` field on NavItem — cashiers see no link. The page itself also enforces with an access-denied state; backend 1.11a returns 403 if the role bypass is attempted.
- [x] Four verifier components **ported** (not imported) to tenant app at `apps/web/src/app/[locale]/(shell)/sales/verification/_components/`: `MatchIndicators.tsx` (Account pill flips: expected_kind=tenant for sale-context), `ReceiptViewer.tsx` (Bearer-authed blob URL via tenant `apiFetch`), `RejectModal.tsx` (i18n via next-intl), `ProofActionBar.tsx`.
- [x] Typed API wrappers `apps/web/src/lib/api/{payment-proofs,tenant-bank-accounts}.ts`. `apiFetch` already handles `FormData` (no client changes).
- [x] i18n: `verification.*` namespace + `pos.payment.upload.*` keys added to EN + AR (40 new keys, 340 total). `pnpm i18n:check` green. Arabic glossary root family: التوثيق / موثّق / رفض / اعتماد.
- [x] Logical CSS (`inset-inline-*`, `text-align: start`) throughout `verification.css` (~400 lines) — RTL-correct.
- [x] Backend touch: tiny new `apps/api/src/tenant/bank-accounts/` module exposing `GET /v1/tenant-bank-accounts` so POS knows which bank account to send proofs to. Tenant-scoped, rate-limited 60/min.
- [x] Verification: `pnpm --filter @madar/web typecheck` + `pnpm --filter @madar/api typecheck` clean. `pnpm --filter @madar/web build` succeeds (POS 11.3 kB / 135 kB First Load; /sales/verification 2.44 kB / 131 kB; verification/[id] 1.83 kB / 130 kB). Existing 110 API tests still green.
- [x] **Bank Transfer QR code (`qrcode.react`)** — `apps/web/src/app/[locale]/(shell)/billing/invoices/[id]/pay/pay-client.tsx` step 1 now shows a 192px QR before the bank details. Payload is a plain-text block (`Bank · Account holder · IBAN •••• <last4> · SWIFT · Amount · Reference`) — the customer scans into notes / banking app and types the IBAN from the account agreement they have on file. We can't encode the full IBAN because platform bank accounts store the account number AES-encrypted (per docs/billing-flow.md §4.5); only the last-4 mask is exposed by the API. New i18n key `billing.pay.bank.qrHint` (en/ar). EPC-format QR for full-IBAN scan-and-pay lands when 1.14e A7 ships the encrypted-reveal flow.

#### 1.11c — admin verifier UI (DONE)
- [x] **A4** Subscription verification queue at `/verification` — two-pane layout (380px list + flex detail pane), filter chips (Pending/Verified/Rejected/All; Pending default), client-side oldest-first sort, URL-state for `selected=` proof. Hardcodes `context=subscription` so admin never sees sale-context proofs.
- [x] **A5** Payment proof detail at `/verification/[id]` — sharable single-proof view with back link to queue.
- [x] Shared components in `apps/admin/src/app/(shell)/_components/`: `MatchIndicators.tsx` (4-pill row with client-side checks: date ≤14d, reference present, account kind matches; amount neutral until invoice endpoint ships), `RejectModal.tsx` (6 reason codes per design + notes textarea), `ReceiptViewer.tsx` (Bearer-authed blob fetch via `useReceiptBlob` → object URL → image or PDF iframe, click-to-fullscreen modal, revokes URL on unmount), `ProofActionBar.tsx` (Approve/Reject buttons, status-aware — shows resolved pill once verified/rejected).
- [x] Typed API wrapper `apps/admin/src/lib/api/admin-proofs.ts` — list/get/approve/reject + the binary blob fetcher with auth-token attach.
- [x] Sidebar "Verification queue" entry enabled, dynamic pending-count badge fed by the existing `/v1/admin/dashboard/kpi` query (TanStack Query cache shared with Dashboard).
- [x] Dashboard "Verification queue" panel now has an "Open queue →" link.
- [x] CSS extensions in `admin-shell.css` (~290 lines): two-pane grid, list rows with days-color-coded (green ≤1d / amber 2-3d / rose ≥4d), receipt frame with checkered background + fullscreen modal, match-pill variants, primary/danger buttons, reject modal, toast.
- [x] Verification: `pnpm --filter @madar/admin typecheck` clean. `pnpm --filter @madar/admin build` succeeds (verification queue 2.48 kB / 113 kB First Load; detail 1.71 kB / 113 kB).
- [x] Design reference followed: `docs/design/project/admin-verification.jsx` (queue layout, reason codes, action bar pattern). Keyboard shortcuts (J/K/A/R/E), match-amount comparison, in-place zoom/rotate controls, tenant context sidebar, and bulk approve deferred to a v2 polish slice.

#### 1.11d — production storage (DONE)
- [x] MinIO container added to `docker-compose.yml` (ports 9000 API + 9001 console, healthcheck `/minio/health/live`, persists to `madar_minio` named volume, dev creds `madar / madar-dev-only`).
- [x] `@aws-sdk/client-s3` dep added (skip presigner until 1.11e).
- [x] New `apps/api/src/common/storage/s3.storage.ts` — implements the `StorageService` interface (put/get/exists/delete) against S3-compatible APIs. Lazy `ensureBucket` on first put via HeadBucket → CreateBucket fallback. `forcePathStyle` so MinIO works out of the box.
- [x] `StorageModule` factory provider — reads `STORAGE_PROVIDER` env (`local` default; `s3` activates S3Storage). Existing consumers (`PaymentProofsService`) unchanged — they only call the interface.
- [x] New env vars: `STORAGE_PROVIDER`, `S3_ENDPOINT`, `S3_REGION`, `S3_BUCKET`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `S3_FORCE_PATH_STYLE`. `.env` + `.env.example` updated.
- [x] `apps/api/test/setup.ts` pins `STORAGE_PROVIDER=local` so tests continue using LocalDiskStorage via the existing `makeStorageRoot` helper. No S3 mocking required.
- [x] Receipt streaming endpoint (`GET /v1/payment-proofs/:id/receipt`) works identically against both backends — service is storage-agnostic.

#### 1.11e — hardening (deferred — infra + schema)
- [ ] Real ClamAV daemon + `ClamAVScanner` impl (replace NoopScanner). **Infra:** needs ClamAV container in `docker-compose.yml` + `CLAMAV_HOST/PORT` env.
- [ ] HMAC-signed S3 URLs surfaced as `receipt_url` for direct `<img src>` from S3 (skip server roundtrip). **Infra:** needs real S3/MinIO with credentials.
- [ ] Resubmit linkage (`previous_proof_id` FK) so rejected proofs can be chained. **Schema:** Prisma migration on `payment_proofs` (self-FK, index). Migrations need Docker Desktop online to apply.
- [ ] Email notifications (submitted / verified / rejected) — Resend wiring (1.15).
- [ ] "Request more info" action (paired with email infra).
- [ ] 7-year retention policy enforcement + receipt PDF generation on verify (1.15 cross-cutting).

### 1.12 Basic reports
- [x] Owner Dashboard (editorial variant) at `/[locale]` — AI digest headline, 3 KPI cards, revenue hero chart (SVG), AI insights rail, branch leaderboard, hour×day heatmap, recent transactions feed
- [x] **Owner Dashboard backend (Task 1)** — chain-wide `GET /v1/dashboard` shipped at `apps/api/src/tenant/dashboard/` (module/controller/service/insights). One read endpoint under `TenantAuthGuard`, role-gated `owner|manager|accountant` (cashier 403), rate-limited 60/min. Service runs 10 parallel `$queryRawUnsafe` against `tenantScoped(tenantId)`: weekMetrics (this+prev week revenue/transactions/items/gross via UNION ALL), revenue_30d (`generate_series`), 7-day sparklines (revenue/transactions/gross-profit), leaderboard (subquery for prev-week pct), 7×12 heatmap (ISODOW × hour 8–19, normalized to peak), 7 most-recent transactions, top-product, stale-payment-proof count, low-stock count, mixed-currency canary. `revenue_cents` filtered to `tenant.default_currency_code`; transaction counts include all currencies. **Insights engine** `dashboard.insights.ts` — pure deterministic rules (no LLM): `branch_decline` (≤-10% wow, urgency high, max 2), `concentration` (top product ≥30% of week, medium), `stale_payment_proof` (>48h pending, high), `low_stock_critical` (qty_on_hand≤0, medium), `growth_winner` (≥+20% wow, low, max 1), `week_recap` (fallback when <4 fire, low). Top 4 by urgency ship to client; each insight gets a 16-hex SHA-256 stable `id` (`tenantId:kind:primary_entity:weekISO`) so the client can persist dismiss state. Bilingual `headline_i18n`/`body_i18n` interpolated server-side via `dashboard.insights.i18n.ts` template map. Concentration insight links to `/inventory/products/{product_id}` (real route exists), branch insights link to `/branches/{branch_id}/dashboard`, stale-proof links to `/sales/verification`. **Tests** `apps/api/test/dashboard/dashboard.spec.ts` — 10 specs all green: cashier 403, owner 200, RLS canary (tenant B empty), empty tenant → week_recap-only insight, one-sale-today metrics, prev-vs-this-week pct, foreign-currency sale (`mixed_currency_warning=true` + excluded from revenue + counted in transactions), leaderboard DESC ordering, branch_decline at -15%, stale_payment_proof at 49h backdate. **Verification**: `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/api test -- dashboard/dashboard` 10/10, `pnpm --filter @madar/api test -- branches sales` 55/55 (no regression). Wired in `apps/api/src/tenant/tenant.module.ts`. Frontend still on mock-data (`_dashboard/_mock-data`) — Task 2 wires the API client.
- [x] App shell (sidebar + topbar) wrapping all routes via `(shell)` route group; POS opts out
- [x] Locale switcher in topbar (working — flips EN↔AR and preserves pathname)
- [x] Branch switcher dropdown (visual only — doesn't filter mock data yet)
- [ ] Sales summary (today/week/month, by branch, by cashier) — deferred
- [ ] Top products — deferred
- [ ] Other dashboard variants (KPI-first, Newspaper) — deferred
- [ ] AI chat panel drawer — deferred
- [x] Design references followed: `docs/design/project/Madar.html`, `components.jsx`, `screen-dashboard.jsx` (editorial), `data.js`

### 1.13 Subscription billing (tenant side)
#### 1.13a — billing module + pay-invoice flow (DONE)
- [x] **Backend `apps/api/src/tenant/billing/`** — new module exposing 5 read endpoints, all RLS-scoped + rate-limited 60/min:
  - `GET /v1/plans` — list active platform plans (4 seeded: starter $49, growth $149, business $399, enterprise $999). Money rides as JSON strings.
  - `GET /v1/subscription` — current tenant subscription view `{ tenant, plan, usage, next_invoice }`. Usage aggregates: transactions (sales last 30d), users (excluding soft-deleted), branches. Storage stub = 0 (deferred). `next_invoice` is the soonest-due unpaid invoice across statuses `draft|awaiting_payment|in_review|overdue`.
  - `GET /v1/subscription-invoices?[status]` — list tenant invoices. Special filter `status=pending` collapses to the 4 unpaid statuses; otherwise pass-through to enum.
  - `GET /v1/subscription-invoices/:id` — single invoice + attached `proofs[]` (proof id, status, created_at, transfer_reference).
  - `GET /v1/platform-bank-accounts?currency=&country_code=` — tenant-authed list of active platform receiving accounts. Returns last-4 + IBAN-last-4 + SWIFT but **never** the encrypted full number.
- [x] Tenant payment-proofs submit path already accepts `context=subscription` (1.11a shipped both contexts); the shared service validates `bank_account_kind='platform'` + invoice owned by tenant + status pending. No controller changes needed for 1.13.
- [x] **Seed update** — added a second invoice `INV-2026-06-001` in `awaiting_payment` state for the Bayt Coffee tenant (alongside the existing `INV-2026-05-001 in_review`) so the demo pay-invoice flow has a target invoice end-to-end.
- [x] **Frontend `apps/web/src/app/[locale]/(shell)/billing/`** — `/billing` route with 3 tabs (Plan & usage / Invoices / Payment history):
  - **Plan & usage tab** ports `docs/design/project/screen-billing.jsx:165-311` — trial banner when `status='trialing'` (days-left countdown + "Pay first invoice" deep link), current plan card (display-serif price, accent border), usage bars (amber at ≥90%), next-invoice card with status chip + `[Pay by bank transfer]` button, 4-plan comparison grid (current plan highlighted with accent border).
  - **Invoices tab** — status-toned chip table with `Pay` action for `awaiting_payment|overdue`, `View status` link for `in_review`. Status colors: sage (paid), amber (awaiting), accent (in-review), rose (overdue).
  - **Payment history tab** — chronological timeline of paid invoices + lifetime-spend display-serif card + count subtitle. PDF receipts ("coming soon") deferred to 1.15 cross-cutting.
- [x] **Pay-invoice wizard at `/billing/invoices/[id]/pay`** — 3-step flow ports `docs/design/project/screen-billing.jsx:449-814`:
  - **Step 1 (Make the transfer)** — bank picker chips (multi-bank when tenant currency has multiple platform accounts), bank-details card with copy-to-clipboard buttons (bank name, holder, masked IBAN, SWIFT, currency), highlighted reference-code section (monospace + copy + "must include this" note), display-serif amount.
  - **Step 2 (Upload receipt)** — drop-zone for PDF/JPG/PNG ≤5MB, form fields (payer name pre-filled with tenant name, transfer date defaulting to today, bank reference, amount read-only). Submit calls `POST /v1/payment-proofs` (multipart, context=subscription), the shared module handles MIME validation + virus scan + resize + EXIF strip + storage and atomically transitions invoice `awaiting_payment → in_review`.
  - **Step 3 (Awaiting)** — sage check icon + serif heading + tenant-friendly body referencing the amount and ref code. Auto-jump to step 3 if the invoice is already `in_review` or `paid` when the page loads (resume-friendly).
- [x] **Typed API wrappers** at `apps/web/src/lib/api/billing.ts` — plans/subscription/invoices/platform-bank-accounts/`submitSubscriptionProof` (multipart FormData via existing `apiFetch`).
- [x] **Sidebar nav** — enabled the previously-disabled `billing` entry under the **money** section; role-gated to `owner|accountant` so cashiers/managers don't see it.
- [x] **i18n** — `billing.*` namespace added to `en.json` + `ar.json` (~70 new keys, 457 total in lockstep). `pnpm i18n:check` green. Glossary-aligned terms (الفواتير / التحويل البنكي / الإيصال / الفترة التجريبية).
- [x] **Tests** — `apps/api/test/billing/billing.spec.ts` (8 integration tests): list plans, get-subscription happy + next_invoice populated, list-invoices RLS canary (2-tenant), status filter `paid`, get-invoice happy + proofs[] empty array shape, unknown id 404, platform-bank-accounts currency filter + no encrypted-field leak, anonymous 401.
- [x] Verification: `pnpm test:api` **176/176** (was 168 + 8 new), `pnpm test:rls` 43/43, `pnpm i18n:check` 457 keys lockstep, `pnpm --filter @madar/web build` clean (`/billing` 10.6 kB / 134 kB First Load, `/billing/invoices/[id]/pay` 6.4 kB / 136 kB), API + admin + web typecheck all clean.

#### 1.13b — automation
- [x] **`BillingTrackerService.runDailyTick()`** — `apps/api/src/admin/billing-tracker/`. Idempotent daily tick that (1) graduates `trialing` tenants past `trial_ends_at` to `active` and inserts an `awaiting_payment` `subscription_invoices` row (period_start=now, period_end=+30d, due_date=+7d, amount = plan.monthly_price_cents, reference_code = `INV-<8 hex>`); (2) flips `awaiting_payment` invoices past `due_date` to `overdue`; (3) advances tenants through `active → grace_period (1-7d past due) → suspended (>7d) → cancelled (≥31d)` against the oldest unpaid invoice. Concurrent-safe via in-transaction status re-check on bootstrap. Returns a structured report with per-step counters + collected errors. Every transition writes `platform_audit_log`.
- [x] **`POST /v1/admin/billing/tick`** (Platform Owner / Finance, idempotency-keyed, 4/min rate limit) — manual trigger for verification. Cron / BullMQ scheduler that calls this lands in 1.15.
- [ ] **Trial-ending reminders (3 days before)** — needs Resend (1.15).
- [ ] **PDF receipt generation on payment_verified** — needs PDF infra (1.15). Browser print covers the human-facing case today.
- [ ] **Email notifications** (invoice issued, payment received, suspended) — needs Resend (1.15).
- [ ] **Suspension banners + access cutoff in tenant app** — backend status is now correct; UI gating is a separate slice. Plan upgrade/downgrade with proration → Phase 2.

### 1.14 Super-admin MVP — 10 pages
#### 1.14a — Dashboard (A1) + Tenants list (A2) + admin shell (DONE)
- [x] **A1** Dashboard at `/` — 5-card KPI strip (MRR, Active tenants + 7d delta, Trials ending in 7 days, Pending verifications + oldest_days, System health) + Verification snapshot panel + Recent activity feed (last 50 events, kinds: `tenant_signup`, `sale_completed`, `verification_pending`). Backed by `GET /v1/admin/dashboard/{kpi,activity}` via `adminPrisma` cross-tenant queries.
- [x] **A2** Tenants list at `/tenants` — status chip filter (All/Trial/Active/In grace/Suspended/Cancelled) + country dropdown + 300ms debounced search (name + slug, case-insensitive) + plan filter capability + pagination (default 50, max 200, page indicator + Prev/Next). Backed by `GET /v1/admin/tenants` with parallel GROUP BY queries for branch_count + user_count + last_activity_at.
- [x] Admin shell scaffolded at `apps/admin/src/app/(shell)/` — sidebar (3 sections: Operations / Billing / Security; only Dashboard + Tenants enabled, rest show "Soon" pill) + topbar (breadcrumb + search stub + "System nominal" pill + sign-out) + 1440px max-width content area. Slate-teal accent via existing `theme-admin` class swap.
- [x] Backend endpoints: `apps/api/src/admin/dashboard/` + `apps/api/src/admin/tenants/` modules, both per-route `@UseGuards(AdminAuthGuard)` (never APP_GUARD per 1.6a's pattern). 26 new integration tests across 7 spec files (kpi 3, activity 3, dashboard-canary 2, tenants-happy 3, tenants-filters 8, tenants-pagination 5, tenants-canary 2) — all green.
- [x] Verification: `pnpm test:api` 110/110 (35 tenant + 27 admin auth + 1 reverse canary + 21 sales + 26 dashboard/tenants), `pnpm test:realm` 12/12 (4 spec files), `pnpm test:rls` still 43/43. `pnpm --filter @madar/admin build` succeeds (Dashboard 2.9 kB / 101 kB First Load, Tenants 3.77 kB / 102 kB, Login 28.9 kB / 116 kB).
- [x] Placeholder home + `home-client.tsx` deleted — replaced by real `(shell)/page.tsx`.

#### 1.14b — Tenant detail + impersonation (DONE)
- [x] **A3** Tenant detail at `/tenants/[id]` (admin) — port of `docs/design/project/admin-screens.jsx:267-395`. Header (gradient avatar, name, status chip, slug, joined date, last activity, **Log in as** button), 4-card KPI strip (Last 30d revenue, Last 30d sales, Active branches, Active users), Recent invoices table (5 most recent with reference code, period, due, amount, status chip), Branches table (code, name, currency, status), Users sidebar (name, email, role chip), amber warning panel summarizing double-logging + destructive-op blocking. Backed by new `GET /v1/admin/tenants/:id` returning `{ tenant + plan + kpis + branches + users + recent_invoices }`. Tenants list rows now navigate to the detail page (was 404 before).
- [x] **Impersonation flow** — end-to-end vertical slice across both apps:
  - **Backend `apps/api/src/admin/impersonation/`** — new module exposing `POST /v1/admin/tenants/:id/impersonate` (role-gated to `owner|support`; `finance|developer|readonly` → 403 `impersonation_forbidden_role`). Body `{ user_id, reason: min 3 / max 280 chars }`. Validates target user belongs to the tenant + is active. Writes `platform_audit_log` `action='impersonation_started'` with `target_tenant_id`, `reason`, and metadata `{ target_user_email, jti, expires_at }`. Returns `{ access_token, jti, expires_at, target_tenant, target_user }`.
  - **`TokenService.mintImpersonationAccess`** — hard 1h TTL tenant-realm access token carrying `impersonator_id` + `impersonator_email` claims. Audience `madar.tenant` + `typ:'access'` so existing routes accept it unchanged. Registers the jti in Redis (`imper:<jti>`) so it can be revoked on exit. No refresh half is minted — when access expires, admin re-impersonates.
  - **`TenantAuthGuard`** now async — surfaces `impersonator_id` and `impersonator_email` onto `req.user` and calls `TokenService.isImpersonationAlive(jti)` for impersonation tokens. Revoked jti → 401 `impersonation_revoked` even though the JWT itself hasn't expired.
  - **`AuditService` + `AuditCtx` + `SaleCtx` + `SubmitProofCtx` + `VerifierActor`** all carry optional `impersonatorId`; when set, the `audit_log.impersonator_id` column gets populated. Wired through every tenant-side mutation call site (catalog, sales, payment-proofs submit/verify/reject).
  - **`assertNotImpersonating(user, opName)` helper** (from 1.8b stub) now reads `req.user.impersonatorId` for real. Wired into `softDeleteProduct` + `softDeleteCategory`; throws `403 forbidden_during_impersonation` per CLAUDE.md mandate.
  - **Tenant exit + me endpoints** at `/v1/impersonation/{me,exit}` — both authed via `TenantAuthGuard` because the impersonation token is a tenant-realm token. `GET /me` returns `{active, impersonator_id, impersonator_email}` for the banner. `POST /exit` revokes the jti via `TokenService.revokeImpersonation` and writes `platform_audit_log` `action='impersonation_ended'`. Rejects with 400 `not_impersonating` if the caller's token doesn't carry `impersonator_id`.
  - **Admin `<LoginAsModal>`** at `apps/admin/src/app/(shell)/tenants/[id]/_components/` — select target user (defaults to first owner), type reason (3–280 chars, char counter), submit → `POST /v1/admin/tenants/:id/impersonate` → open tenant app in a new tab at `${NEXT_PUBLIC_TENANT_WEB_ORIGIN}/en/impersonation-handoff?token=…&tenant_id=…&tenant_name=…&user_email=…&expires_at=…`. Danger button styled with `var(--rose)`.
  - **Tenant handoff page** at `apps/web/src/app/[locale]/impersonation-handoff/` — client component reads `?token=` and `?tenant_name=` etc. from the URL, hydrates the auth Zustand store with `{ accessToken, ...placeholder user+tenant }`, then calls `GET /v1/auth/me` to replace the placeholder with the real tenant principal, sets `sessionStorage.madar_impersonation = { admin_email, target_tenant_name, expires_at }`, then `window.location.replace("/en")`. Error path renders a rose banner with the API message.
  - **`<ImpersonationBanner>`** at `apps/web/src/app/[locale]/(shell)/_components/` — mounted above `<Sidebar />` in the tenant Shell. Reads `sessionStorage.madar_impersonation`, renders a sticky full-width rose-gradient bar (matching `docs/design/project/admin-app.jsx:67-86`): `● Impersonating · Viewing Madar as [tenantName] — logged in as [adminEmail]. Every action is logged.` + white "Exit impersonation" button → calls `POST /v1/impersonation/exit`, clears sessionStorage + auth store, navigates to `${NEXT_PUBLIC_ADMIN_WEB_ORIGIN}/tenants`.
  - **Tests**: `apps/api/test/admin-impersonation/` — 14 integration tests across 2 spec files. `start-impersonation.spec.ts` (11): happy path owner role + support role allowed + finance 403 + unknown tenant 404 + cross-tenant user 404 + missing reason 400 + minted token validates against `TenantAuthGuard` with impersonator claim + destructive product-delete blocked 403 + non-destructive sale allowed with `audit_log.impersonator_id` populated + exit revokes jti (subsequent requests 401 `impersonation_revoked`, `impersonation_ended` audited) + exit with non-impersonation token 400 `not_impersonating`. `tenant-detail.spec.ts` (3): happy + 404 + 401 anonymous.
- [x] **Bug fix uncovered:** `apps/api/src/admin/payment-proofs/payment-proofs.module.ts` was missing `AdminAuthModule` import — `AdminAuthGuard` couldn't resolve `AdminTokenService` at runtime. Fixed (mirrors `DashboardModule` / `TenantsModule` pattern). Surfaced the first time `pnpm test:api` ran end-to-end this session; previously masked by an offline Docker daemon in 1.11a's test pass.
- [x] Verification: `pnpm test:api` **168/168** (was 154 + 14 new impersonation), `pnpm test:rls` 43/43, all three apps typecheck clean, `pnpm --filter @madar/admin build` clean (tenant detail 5.51 kB / 112 kB First Load), `pnpm --filter @madar/web build` clean (handoff route added).
- [ ] **Internal notes textarea on A3** — schema gap (no `Tenant.internal_notes` column yet); the plan deferred it as a v2 polish item.
- [ ] **Resource cleanup on impersonation exit** — when the admin closes the tenant tab without explicitly clicking Exit, the jti stays alive in Redis until the 1h TTL. Could add `beforeunload` handler to call exit best-effort, but defer to a hardening slice.

#### 1.14c — Verification queue (DONE)
- [x] **A4** Subscription verification queue — `apps/admin/src/app/(shell)/verification/{page.tsx,verification-client.tsx}`. Two-pane layout: status-filter chips (Pending/Verified/Rejected/All, default = pending sorted oldest-first), left list (payer name + amount + bank ref + days-since chip with fresh/warn/stale tone), right detail pane (`ReceiptViewer`, `MatchIndicators` pills, transfer dl, `ProofActionBar`). Calls `adminListProofs({ context: 'subscription' })` + `adminGetProof` + `adminApproveProof` + `adminRejectProof` against `/v1/admin/payment-proofs/*` (shipped in 1.11a). Toast on success/failure, invalidates dashboard KPI on action.
- [x] **A5** Payment proof detail — `apps/admin/src/app/(shell)/verification/[id]/{page.tsx,proof-detail-client.tsx}`. Linked from the queue ("Open detail →"). Full-page proof viewer with receipt, match indicators, transfer info table, approve/reject action bar, reject-reason modal.
- [x] **Keyboard shortcuts** — global `keydown` listener in `verification-client.tsx`: `J` selects next, `K` selects previous, `A` approves the focused pending proof, `R` opens the reject modal. Suppressed when focus is in a form field or when the reject modal is open. Hint chip in the header sub.
- [ ] "Request more info" action — deferred to 1.11e (paired with Resend email infra).

#### 1.14d — Remaining MVP pages + dark-mode toggle (A6/A9/A10 + dark mode DONE)
- [x] **A6** Cross-tenant invoices list at `/invoices` (admin). Backend: `GET /v1/admin/invoices?[status,currency,search,page,limit]` in new `apps/api/src/admin/invoices/` module. Schema has no `SubscriptionInvoice → Tenant` relation, so search filter pre-resolves matching tenant IDs and then runs the main query with `tenant_id: { in: [...] }`. Status filter chips (`All / awaiting_payment / in_review / paid / overdue`); `overdue` enriches the filter with `due_date < now()`. Computes `days_overdue` server-side. Frontend: filter chips + debounced 300ms search + dense table (invoice ref / tenant / plan / amount / issued+due / status chip / days_overdue with color tone — green 0d, amber 1-7d, rose ≥8d) + pagination. Tenant column links to `/tenants/:id`.
- [x] **A9** Login-as audit at `/login-audit` (admin). Backend: `GET /v1/admin/login-as-audit?[platform_user_id,target_tenant_id,page,limit]` materializes sessions from `platform_audit_log` by pairing `impersonation_started` rows with matching `impersonation_ended` rows via the `jti` in metadata. For each session, counts `audit_log` rows tagged with the impersonator's id between start and end timestamps. Frontend: amber explainer banner + dense table (timestamp / super-admin / tenant link / duration formatted h-m-s, "Active" if open / actions count / reason / IP) + pagination.
- [x] **A10** Platform audit log at `/platform-audit` (admin). Backend: `GET /v1/admin/platform-audit?[platform_user_id,action_prefix,target_tenant_id,from,to,page,limit]` queries `platform_audit_log` with the standard server-side filters. Joins users + target tenants in a single secondary query (no Prisma relations defined; `(deleted)` placeholders for missing rows). Frontend: amber "append-only · 7-year retention" banner + debounced action-prefix filter + dense table (timestamp / super-admin / monospace action code chip / target tenant link / IP / reason).
- [x] **Dark-mode toggle** wired in `AdminTopbar` — sun/moon button flips `<html data-theme="light|dark">` and persists via `localStorage.madar_admin_theme`. Tokens already support both modes via `packages/ui/src/tokens.css`. Initial value reads localStorage on mount.
- [x] **Sidebar** — `Invoices`, `Login-as audit`, `Platform audit` entries now enabled (were `disabled: true` placeholders). Bank accounts and Super-admin team remain disabled — see 1.14e.
- [x] Backend wiring: new modules under `apps/api/src/admin/{invoices,audit}/`, both imported via `AdminModule`. Both use per-route `@UseGuards(RateLimitGuard, AdminAuthGuard)` (60/min rate limit). All read endpoints; no audit writes from this slice. Cross-tenant queries via `adminPrisma` exclusively.
- [x] **Tests**: 10 integration tests across 2 spec files. `apps/api/test/admin-invoices/` (4): happy multi-tenant with shape assertions, `status=paid` filter, search by tenant slug, anonymous 401. `apps/api/test/admin-audit/` (6): platform-audit list happy, `action_prefix=impersonation` filter, login-as-audit lists sessions with `platform_user` + `target_tenant` + `actions_count`, `platform_user_id` filter narrows, action-count grows after impersonated sale (validates end-to-end double-logging from 1.14b), anonymous 401.
- [x] Verification: `pnpm test:api` **186/186** (was 176 + 10 new), `pnpm test:rls` 43/43, `pnpm --filter @madar/admin typecheck` + `build` clean (`/invoices` 2.8 kB / 109 kB First Load, `/login-audit` 2.64 kB / 109 kB, `/platform-audit` 3.35 kB / 110 kB), `pnpm --filter @madar/api typecheck` clean.

#### 1.14e — Remaining admin pages (deferred)
- [ ] **A7** Platform bank accounts CRUD with AES-256-GCM encryption for full account numbers + `reveal` endpoint gated to Platform Owner. Needs new `PLATFORM_BANK_ENCRYPTION_KEY` prod secret.
- [ ] **A8** Super-admin team CRUD with MFA dots + role chips + invitation tokens (needs Resend wiring from 1.15).
- [ ] Growth charts on Dashboard (tenant growth + MRR trend over 90 days, Recharts).
- [ ] Export tenants to CSV + Send announcement (broadcast email to all tenants).
- [ ] Internal notes textarea on tenant detail page (A3 schema gap — needs `Tenant.internal_notes` migration).
- [ ] Design reference: `docs/design/project/admin-screens.jsx`, `admin-verification.jsx`, `admin-app.jsx`

### 1.15 Cross-cutting Phase 1
- [x] **`audit_log` + `platform_audit_log` append-only triggers** — already in place since `packages/db/prisma/migrations/20260514000000_init/migration.sql:426-441` defines `fn_audit_log_append_only()` and four triggers (`{audit_log,platform_audit_log}_block_{update,delete}`). Verified by existing assertions in `packages/db/test/rls.test.ts:90-109`. CLAUDE.md's "Both append-only — no updates, no deletes, ever" contract is enforced at the Postgres layer (not just application code) and was already correct.

- [x] **Sentry projects** — wired across all three apps with DSN-conditional no-op init:
  - `apps/api/src/sentry.ts` exports `initSentry()` returning `false` when `SENTRY_DSN_API` is empty; otherwise `Sentry.init({ dsn, environment, tracesSampleRate: 0.1, release })`. `apps/api/src/main.ts` calls it before `NestFactory.create()` and registers `Sentry.setupExpressErrorHandler` only when the DSN is set.
  - `apps/web` + `apps/admin` each gain `sentry.{client,server,edge}.config.ts` (all DSN-conditional) plus `withSentryConfig(...)` wrapping in their `next.config.mjs`. Source-map upload disabled for now (`sourcemaps: { disable: true }`) — CI auth tokens land in a follow-up.
  - Env: `SENTRY_DSN_API`, `SENTRY_ENVIRONMENT`, `NEXT_PUBLIC_SENTRY_DSN_TENANT`, `NEXT_PUBLIC_SENTRY_DSN_ADMIN` — all optional. `.env.example` documents the defaults.
  - Test: `apps/api/test/sentry/init.spec.ts` asserts no-op when DSN is empty.

- [x] **Email infrastructure** — `apps/api/src/common/email/` ships a provider-abstracted email layer with bilingual templates:
  - **`EmailModule`** is `@Global()`; factory picks `ResendEmailProvider` when `EMAIL_PROVIDER=resend`, else `DiskEmailProvider` (default `disk` locally + in tests so CI never hits network). Wired into `CommonModule.imports`.
  - **`DiskEmailProvider`** writes `{ts}-{template}-{to-local}.eml` files (RFC 822-style) to `EMAIL_LOG_DIR` (default `./var/sent-emails/`). `ResendEmailProvider` lazy-inits the Resend SDK on first send.
  - **5 templates ship**, each authored bilingually (EN + AR with RTL `<html dir="rtl">` shell): `welcome`, `trial_ending`, `payment_received`, `suspended`, `admin_invite`. Templates live in `apps/api/src/common/email/templates.ts` (dependency-free string substitution via `{{var}}`).
  - **Owner-email helper** at `apps/api/src/common/email/recipient.helper.ts`: `getTenantPrimaryRecipient(tenantId)` returns the earliest-created active owner's email + name + locale via `adminPrisma`. Centralizes the lookup for every future caller.
  - **2 templates wired at their natural call sites today**: `welcome` after `apps/api/src/tenant/auth/auth.service.ts:signup()` (fire-and-forget, failure doesn't roll back signup); `payment_received` after admin verifies a subscription proof in `apps/api/src/payment-proofs-shared/payment-proofs.service.ts:verify()` (looks up owner + invoice + tenant in parallel, formats the amount with `Intl.NumberFormat`).
  - **3 templates ready but not wired** (their wiring belongs to other slices): `trial_ending` + `suspended` ship with 1.13b BullMQ jobs; `admin_invite` with 1.14e A8 super-admin team CRUD.
  - **Locale resolution rule** documented inline in `email.service.ts`: tenant-context emails (welcome / trial_ending / payment_received / suspended) prefer the tenant's `default_locale`; personal emails (admin_invite, future password_reset) prefer the recipient user's `locale`.
  - **Env**: `EMAIL_PROVIDER` (`resend|disk`), `RESEND_API_KEY` (only required when `=resend`), `EMAIL_FROM` (default `Madar <hello@madar.dev>`), `EMAIL_LOG_DIR` (default `./var/sent-emails`), `TENANT_WEB_ORIGIN` (default `http://localhost:3000`).
  - **Tests** (5 spec files / 9 cases): disk-provider writes file with subject/body/headers; i18n renders EN + AR (Arabic subject + RTL dir); payment_received renders with reference code + amount; suspended renders with deadline; welcome integration via real signup → disk file appears; recipient-helper happy + soft-deleted skip + inactive skip.

- [x] **Bilingual printable receipt** at `/[locale]/sales/[id]/receipt`:
  - **Backend**: new `getSaleForReceipt(tenantId, saleId)` method on `SalesService` extends `findOne()` with tenant (via `adminPrisma`) + branch + cashier + (for bank-transfer sales) the tenant's default bank account. Never returns the encrypted account number column — only `last4` + IBAN-last4 + SWIFT. New route `GET /v1/sales/:id/receipt-data` (60/min rate limit, same auth).
  - **Frontend**: `/[locale]/sales/[id]/receipt/page.tsx` is OUTSIDE the `(shell)` route group so it renders without the sidebar/topbar (clean for print). `receipt-doc.tsx` reads `?size=80mm|58mm` (defaults 80mm), reads bilingual product names from `name_i18n[locale]`, applies `direction: rtl` for `locale=ar`, color-stamps `PAID` (sage) or `PENDING VERIFY` (amber), shows bank-transfer details when applicable. `receipt.css` defines `@page { margin: 0 }` + width classes so the browser's "Save as PDF" / native print dialog produces a thermal-width receipt. POS success toast (`apps/web/src/app/[locale]/pos/pos-client.tsx`) now carries the sale id and surfaces an "Open receipt →" link in a new tab.
  - **i18n**: top-level `receipt.*` namespace added to en.json + ar.json (~25 keys: meta, totals, tender, bank, status, thanks, footer, buttons). `pos.payment.openReceipt` key for the link copy. 538 total keys EN/AR lockstep.
  - **Tests**: `apps/api/test/sales/receipt-data.spec.ts` (4 cases): happy cash sale shape (tenant + branch + cashier present, bank_account null), bank-transfer sale exposes default bank account (`last4` only, no encrypted field), unknown id 404, RLS canary (tenant B → 404).
  - **No PDF library**: per plan, ships HTML + `@page` CSS. Browser handles `Ctrl+P → Save as PDF`. Real binary PDFs (for email attachments) defer to a hardening slice once Resend wiring is verified in production.

- [x] **Verification across 1.15**: `pnpm test:api` **218/218** (was 204 + 14 new — 9 email + 4 receipt-data + 1 sentry-init), `pnpm test:rls` 43/43 unchanged, `pnpm i18n:check` 538 keys EN/AR lockstep, all three apps typecheck clean, `pnpm --filter @madar/web build` succeeds (`/sales/[id]/receipt` route at 4.2 kB / 186 kB First Load; bundle size grew across the board ~50 kB First Load due to `@sentry/nextjs`), `pnpm --filter @madar/admin build` succeeds. `pnpm db:migrate` unchanged (no schema changes this slice).

#### 1.15 deferred
- [ ] Source-map upload for Sentry — requires `SENTRY_AUTH_TOKEN` + CI wiring. Init code is shipped; sourcemaps add later.
- [ ] Resend account + DSN provisioning — defaults to disk-writer locally; switch via env when Resend account exists.
- [ ] Real binary PDF receipts (server-side rendering) — for email attachments. Browser print covers the human-facing case today.
- [ ] OpenTelemetry tracing — listed alongside Sentry in CLAUDE.md:41 stack, separate slice.

---

## Phase 2 — Multi-Branch

### 2.1 Branch CRUD + per-branch stock (DONE)
- [x] **Backend `apps/api/src/tenant/branches/`** — extended from list-only to full CRUD. New endpoints (all rate-limited 30/min on writes / 60/min on reads, idempotent on POST, role-gated):
  - `GET /v1/branches?[include_inactive]` — extended shape: now returns `address_i18n`, `timezone`, `opened_at`, `today_revenue_cents` (per-branch TZ aware, sums paid + payment_pending sales), `staff_count` (active users with `branch_id`), and `product_count` (distinct products in `branch_stock`).
  - `GET /v1/branches/:id` — detail with KPIs `{ today_revenue, week_revenue (7d rolling, per-branch TZ), transactions_today/week, top_product_id/name/units }`, assigned users array, and a `recent_activity[]` feed merging the last 10 `audit_log` rows for `entity=branch AND entity_id=:id` with the last 10 sales (mapped to `kind: 'audit' | 'sale'`).
  - `GET /v1/branches/:id/stock?[search,low_only,page,limit]` — paginated per-branch stock list joining `branch_stock` ↔ `products` ↔ `categories`. Search hits `sku` + EN/AR product names. `low_only=true` filters to rows where `qty_on_hand < reorder_point`.
  - `POST /v1/branches` — **owner only**. Zod-validated body: `code` (uppercase coerced, `^[A-Z0-9_-]{2,16}$`, unique per tenant), `name_i18n {en,ar}` required, `address_i18n {en?,ar?}` optional, `currency_code` (defaults to `tenant.default_currency_code`), `timezone` (defaults `Africa/Cairo`), `opened_at` optional ISO date. Audits `branch_created`. Returns full detail shape.
  - `PATCH /v1/branches/:id` — **owner or manager**. Manager gate: `user.branch_id === id` else `400 forbidden_branch`. **`currency_code` change locked once any sale has been recorded against the branch** (`409 currency_locked_after_sales`). Audits `branch_updated` with before/after.
  - `DELETE /v1/branches/:id` — **owner only**, soft-delete via `deleted_at + is_active=false`. Guards: `409 branch_has_stock` if any `branch_stock.qty_on_hand > 0`, `409 branch_has_users` if any active user assigned. `assertNotImpersonating()` → `403 forbidden_during_impersonation`. Audits `branch_deleted`.
- [x] **Catalog branch-scoping** — `apps/api/src/tenant/catalog/dto/list-products.dto.ts` + `catalog.service.ts:listProducts` extended with optional `branch_id` (uuid). When set, the `branch_stock` GROUP BY is replaced by a per-branch lookup so `qty_on_hand` and `reorder_point` reflect a single branch instead of chain-wide aggregates. Omitted = behaviour unchanged.
- [x] **Frontend tenant app** — new `/[locale]/branches/*` surface under `apps/web/src/app/[locale]/(shell)/branches/`:
  - **List page** (`page.tsx` + `branches-client.tsx`) — PAGES.md §21 card grid. Each `BranchCard` shows name (display serif), address (muted), status pill, today's sales (Intl currency), product/staff counts, and an Edit link. Owners see an `AddBranchCard` at the end of the grid. Empty/loading/error states all handled.
  - **Detail page** (`[id]/page.tsx` + `detail-client.tsx`) — header with display-serif name + status meta, three-tab switcher: **Overview** (4 display-serif KPIs + top product + assigned users + recent activity), **Stock** (paginated per-branch table with search + `Low only` toggle), **Settings** (read-only summary of currency/timezone/opened_at + Edit + Delete branch action with confirmation step). Staff/Registers/Banking tabs deferred (2.1b–d).
  - **New + Edit pages** — both render `<BranchForm>` (RHF-style controlled inputs; Zod-equivalent client validation; EN/AR tab strip for bilingual name + address; currency/timezone selects; opened-at date; active toggle on edit only; sticky footer Save/Cancel). Inline error mapping for `code_taken`, `validation_failed`, `currency_locked_after_sales`, `forbidden_role`, `forbidden_branch`.
- [x] **Topbar `BranchSwitcher` rewired** — dropped the hardcoded `BRANCHES` mock. Now hydrates from `useQuery(['branches','list','active'], branchesListRequest)` with bilingual `name_i18n[locale]`, persists selection via `useBranchScopeStore` (Zustand + `localStorage.madar_selected_branch`), hides itself when `branches.length <= 1`, shows `formatNumberShort(today_revenue/100)` per branch.
- [x] **Branch-scope propagation** — `apps/web/src/lib/api/catalog.ts:productsListRequest` accepts optional `branch_id`. Inventory page (`inventory-client.tsx`) now reads `useBranchScopeStore`, includes the selection in its `useQuery` key, and passes the branch id into the products request. Switching branches refetches the catalog with branch-scoped `qty_on_hand`. POS is unchanged — it correctly stays bound to the cashier's `user.branch_id`.
- [x] **Sidebar** — `apps/web/src/app/[locale]/(shell)/_components/Sidebar.tsx:54` `branches` entry flipped `enabled: true`.
- [x] **i18n** — new top-level `branches.*` namespace (~100 keys: list, form, detail/tabs, errors, empty). EN + AR in lockstep at **641 total keys** (was 538). `pnpm i18n:check` green.
- [x] **API library cleanup** — duplicate `branchesListRequest` removed from `apps/web/src/lib/api/catalog.ts`; now re-exports from the canonical `apps/web/src/lib/api/branches.ts` along with `ApiBranch`/`ApiBranchesList` aliases so existing consumers (`ProductForm` initial-stock picker) keep working unchanged.
- [x] **Tests** — `apps/api/test/branches/` 4 spec files / **27 new integration tests**:
  - `branch-mutations.spec.ts` (15): create happy + audit, duplicate code 409, missing AR 400, cashier 403, manager 403 on POST, PATCH happy + audit, currency lock 409 after a seeded sale, manager edits own branch ok, manager rejected on another branch (forbidden_branch), 404 on unknown id, DELETE blocked by stock 409, DELETE blocked by users 409, DELETE happy + idempotent + audit, cashier DELETE 403, impersonation DELETE 403.
  - `branch-detail.spec.ts` (4): full shape, KPI bump after a seeded sale, 404, RLS canary.
  - `branch-stock.spec.ts` (5): per-branch list shape, `low_only` filter, pagination, RLS canary, 404 on unknown branch.
  - `branch-scoping.spec.ts` (3): chain-wide aggregate, branch-scoped per-branch qty for two distinct branches.
- [x] **Verification**: `pnpm test:api` **245/245** (was 218 + 27 new), `pnpm test:rls` 43/43, `pnpm test:realm` unchanged, `pnpm i18n:check` 641 keys lockstep, `pnpm --filter @madar/web typecheck` + `build` clean (new routes: `/[locale]/branches` 4.93 kB / 186 kB First Load, `/[locale]/branches/[id]` 8 kB / 189 kB, `/[locale]/branches/[id]/edit` 539 B / 188 kB, `/[locale]/branches/new` 221 B / 182 kB). API + admin + web typecheck all clean.
- [x] Design reference followed: `docs/PAGES.md` §21–§23 + `docs/design/project/screen-suppliers-branches.jsx` (visual cues only; map-led layout deferred).

#### 2.1 deferred
- [ ] **2.1b** Staff tab on branch detail (depends on 2.x roles & permissions UI).
- [ ] **2.1c** Registers tab (depends on cash-drawer/shift module).
- [x] **2.1d** Banking tab — per-branch `tenant_bank_account` override + payment-proof bank picker wiring. **Schema**: `tenant_bank_accounts.branch_id` already shipped in init; migration `20260518000000_branches_extras` adds partial-unique `(tenant_id, COALESCE(branch_id, '00…0'::uuid), currency_code) WHERE is_default = true AND deleted_at IS NULL` so exactly one default per (branch | chain, currency) is enforced, plus `(tenant_id, branch_id)` lookup index. **API**: `GET /v1/tenant-bank-accounts?branch_id=:id` returns the union of (chain-default rows, this branch's rows), sorted with branch overrides first. Response now exposes `iban_last4`, `swift`, `branch_id`. **Bank picker resolution**: `apps/api/src/tenant/sales/sales.service.ts` receipt enrichment now first tries `branch_id = sale.branch_id AND is_default = true`; falls back to `branch_id IS NULL AND is_default = true`. Per-branch override beats chain default. **UI**: new `BankingTab` rendered on the branch detail tab strip; shows branch overrides in their own list, chain defaults in a secondary list, with a "Manage chain-wide accounts in settings →" deep link. Bank rows display `bank_name · account_holder · currency · IBAN •••• {last4} · SWIFT {…}` with `Default`/`Inactive` pills.
- [x] **2.1e** Branch Performance Dashboard (PAGES.md §24) — new route `/[locale]/branches/[id]/dashboard`. **API**: `GET /v1/branches/:id/dashboard` returns `today_cents` + `yesterday_cents` + `vs_yesterday_pct`, `transactions_today`, `items_sold_today`, `avg_basket_cents`, `returns_today`, plus three breakdowns: `hourly[]` (24-bucket array, branch-tz aware), `top_categories[]` (rolling 7d, top 6 by revenue), `leaderboard[]` (all active branches today, ranked). All branch-tz aware via `(occurred_at AT TIME ZONE b.timezone)::date`. **UI**: hero KPI in display serif `clamp(48px, 8vw, 72px)` with `+/-N% vs yesterday` trend chip (green up / red down). 4-card KPI row (Transactions / Avg basket / Items sold / Returns). Hand-rolled SVG `HourlyChart` (24 bars, single-color accent gradient, dashed midline, 5 hour labels) — kept off Recharts to stay calm per CLAUDE.md design discipline; matches the `screen-dashboard.jsx` SVG prototype pattern. Hand-rolled `CategoriesDonut` (single-hue accent ramp at 95/78/62/48/34/22% opacity, legend with % shares). Leaderboard list highlights current branch in bold. Empty/single-branch states handled. Reachable via a `<TrendingUp/>` button next to Edit in the branch header.
- [x] **2.1f** Map-led visualization from `screen-suppliers-branches.jsx` + branch dossier panel. **Library decision**: MapLibre GL JS 5.x with OpenFreeMap's free vector tiles (no API key). **Schema**: migration `20260518000000_branches_extras` adds `branches.geo_lat` / `branches.geo_lng` as `DECIMAL(10,7)` with `CHECK` constraints (-90..90 / -180..180). **API**: `geo_lat` + `geo_lng` flow through `ApiBranchSummary` + `ApiBranchDetail`, write paths on POST/PATCH, with partial-fields validation (both or neither). **Form**: new "Map coordinates" section in `BranchForm` with min/max-bounded number inputs; client validation rejects half-filled coords. **List page**: new `List | Map` view toggle pill. **Map view**: dynamic import of `maplibre-gl` (keeps it out of the list-route bundle), Positron style from `tiles.openfreemap.org`, framed to bounds when `geocoded.length > 1`, fallback center on Cairo. Custom `.br-map-pin` markers in accent token. Branches without coords surface in an inline list with deep links to their edit page. **Dossier panel**: opens on pin click — kicker (branch code) + display-serif name + address + a 3-cell stat grid (today's sales / staff count / product count) + three actions (Open / Performance / View stock). Sticky on wide screens via `.br-map-shell:has(.br-map-dossier)` grid swap.
- [x] **2.1g** Operating hours editor + holiday calendar (PAGES.md §22 Settings). **Schema**: migration `20260518000000_branches_extras` adds `branches.operating_hours` JSONB + `branches.holidays` JSONB. **DTO**: new `apps/api/src/tenant/branches/dto/hours.dto.ts` defines `OperatingHoursSchema` (mon..sun × `{ open: "HH:MM", close: "HH:MM", closed: boolean }`, strict objects), `HolidaysSchema` (array<= 200 of `{ date: "YYYY-MM-DD", label_i18n: {en,ar} }`), and `DEFAULT_HOURS`. Both create + update Zod schemas now accept the fields; update uses `nullable()` so a clear is one PATCH away. **Service writes**: branches.service handles `Prisma.DbNull` correctly when clearing JSONB. **UI**: new `HoursTab` tab (replaces the "hours stub" copy). Day-by-day editor with an Open/Closed toggle and two `<input type="time">` per day; client-side rejects close ≤ open. "Use suggested hours" button resets to defaults. Holiday calendar with `+ Add holiday` → row of `date / EN label / AR label / Remove`. AR input flips `dir="rtl"`. Both sections save independently via PATCH `/v1/branches/:id` and surface their own save state.
- [x] **Dashboard branch scope** — Task 2 of the owner-dashboard slice: new typed client at `apps/web/src/lib/api/dashboard.ts` (mirrors `ApiOwnerDashboard`), and `apps/web/src/app/[locale]/(shell)/_dashboard/Dashboard.tsx` is now a `"use client"` orchestrator that subscribes to `useBranchScopeStore`, redirects real-branch scopes to `/[locale]/branches/{id}/dashboard`, and calls `useQuery(["dashboard","owner"], ownerDashboardRequest, { staleTime: 30_000 })` for "all".
- [x] **Dashboard children reshape (Task 3)** — adapter ripped out. `DashboardClient` now takes a single `data: ApiOwnerDashboard` prop and routes slices to children that consume the API shape directly: `DashboardHeader` (week/vs_prev_week/leaderboard — editorial headline picked from top-mover via `revenueUp/Down/firstWeek` keys), `KpiRow` (week + sparklines + currency_code, handles null deltas), `RevenueHeroChart` (revenue_30d points, week-delta chip, branch="All branches" subtitle, empty state), `AIInsightsRail` (real `ApiOwnerDashboardInsight[]`, urgency labels instead of legacy `kind` enum, dismiss-state in `localStorage` keyed `madar.dashboard.dismissed_insights` with 14d TTL prune on mount), `BranchStrip` (`leaderboard` rows + `rank` ICU + null pct → em-dash), `HeatmapCard` (raw `number[][]` 7×12), `RecentTxCard` (uses `tx.code` for display, `tx.branch_code`, `tx.cashier_name`, `Intl.RelativeTimeFormat` for `occurred_at`, expanded method icons including `bank_transfer/store_credit/manual_card`, four payment-status pills `paid|payment_pending|disputed|refunded` with distinct token colors). Loading/error/redirecting states moved off English literals onto `dashboard.loading/error/redirecting` keys. `mixedCurrencyWarning` banner shown when `mixed_currency_warning=true`. `_dashboard/_mock-data.ts` deleted (confirmed no other consumers via Grep). Per-branch `today` surface dropped from leaderboard (API exposes weekly only). i18n: ~+10 net keys to dashboard namespace (1475 EN+AR lockstep). **Verification**: `pnpm --filter @madar/web typecheck` clean, `pnpm --filter @madar/web build` clean, route `/[locale]` 13.2 kB / 195 kB First Load, `pnpm i18n:check` 1475 keys parity.

### 2.2 Stock transfers (in-transit state) — DONE
- [x] **Schema migration `20260516010000_stock_transfers`** — hand-edited immutable SQL adding:
  - `StockTransferStatus` enum: `draft | in_transit | received | cancelled`.
  - `stock_transfers` table: header with `code` (TXR-xxxxx, unique per tenant), `from_branch_id`, `to_branch_id` (CHECK distinct), `status`, lifecycle timestamps `sent_at/received_at/cancelled_at` + corresponding `*_by` actor columns, `notes`, plus the standard tenant/audit/soft-delete columns.
  - `stock_transfer_lines` table: one row per product with `qty_sent` (CHECK > 0), nullable `qty_received` (CHECK ≥ 0), `discrepancy_note`, FK to `stock_transfers` ON DELETE CASCADE, unique `(transfer_id, product_id)`.
  - Indexes: `(tenant_id, status, created_at DESC)`, `(tenant_id, from_branch_id)`, `(tenant_id, to_branch_id)`, `(tenant_id, deleted_at)` on the header; `(tenant_id, transfer_id)` + `(tenant_id, product_id)` on lines.
  - `updated_at` triggers wired via `fn_set_updated_at()` (reuses init migration's function).
  - **RLS**: `tenant_isolation` policies (USING + WITH CHECK, `NULLIF(...)` cast for safety) enabled + forced on both tables.
- [x] **Backend `apps/api/src/tenant/stock-transfers/`** — full state-machine module:
  - **Endpoints** (all role-gated `owner|manager`, manager additionally scoped to branches they're assigned to as sender or receiver):
    - `GET /v1/stock-transfers?[status,from_branch_id,to_branch_id,page,limit]` — paginated list.
    - `GET /v1/stock-transfers/:id` — detail with line product names + computed `has_discrepancy` flag.
    - `POST /v1/stock-transfers` (`@Idempotent()`) — create a draft. Validates same-tenant branches + products, refuses duplicate products across lines, 5-attempt code-collision retry on `TXR-{6}`.
    - `PATCH /v1/stock-transfers/:id` — only allowed while `status='draft'`. Replaces lines wholesale.
    - `POST /v1/stock-transfers/:id/send` — `draft → in_transit`. Inside one `$transaction`: write one `stock_movement` (`kind='transfer_out'`, `qty_delta=-qty_sent`, `reference_table='stock_transfers'`, `reference_id=:id`) per line, upsert `branch_stock` at sender with decrement. Manager must be at the sender branch.
    - `POST /v1/stock-transfers/:id/receive` — `in_transit → received`. Body `{ lines: [{line_id, qty_received, discrepancy_note?}] }` — **every** line must be confirmed (`incomplete_receive` 422 otherwise). For each line: write `transfer_in` movement at receiver + upsert `branch_stock` increment. When `qty_received ≠ qty_sent` the line gets the `discrepancy_note` set. **No auto-adjustment at sender** — discrepancies are flagged; manager opens a manual adjustment afterward. Manager must be at the receiver branch.
    - `POST /v1/stock-transfers/:id/cancel` — only from `draft` (in-transit cancels are blocked per CLAUDE.md spirit: ledger movements must be unwound by an explicit adjustment, not silently cancelled).
    - `DELETE /v1/stock-transfers/:id` — owner|manager, `assertNotImpersonating()`, soft-delete via `deleted_at`. Only from `draft` or `cancelled`. Idempotent (second DELETE returns 200).
  - **Audit log entries**: `stock_transfer_created`, `stock_transfer_updated`, `stock_transfer_sent`, `stock_transfer_received` (with `discrepant` flag), `stock_transfer_cancelled`, `stock_transfer_deleted`. All carry `impersonator_id` when set.
- [x] **Frontend tenant app** — new `/[locale]/transfers/*` surface:
  - **List page** with status tabs (Draft default / In transit / Received / Cancelled / All), table row click → detail. Empty-state copy is per-status.
  - **3-step wizard** at `/transfers/new`: ① pick from/to branches, ② search products (branch-scoped stock visible, click to add, qty editor + remove), ③ review summary + notes textarea, then **Save as draft** or **Save and send** (chains POST + send). Same-branch source/dest blocked client-side.
  - **Detail page** with status-aware actions: `draft` → Cancel transfer / Send / Delete; `in_transit` → per-line receive form with qty input + discrepancy note (auto-enabled when qty differs); `received` → read-only with discrepancy banner if any line differs; `cancelled` → Delete. Timeline section shows created/sent/received/cancelled events.
  - Sidebar `/transfers` entry enabled (gated to `owner|manager`).
- [x] **API library wrapper** `apps/web/src/lib/api/stock-transfers.ts` — typed wrappers for list/get/create/update/send/receive/cancel/delete.
- [x] **i18n** — new `transfers.*` namespace (~110 keys: list, tabs, status pills, wizard steps, detail actions, errors). EN + AR in lockstep at **743 total keys** (was 641). `pnpm i18n:check` green.
- [x] **Tests** — `apps/api/test/stock-transfers/` 3 spec files / **23 new integration tests**:
  - `transfer-crud.spec.ts` (8): happy POST + audit, same-branch 400, unknown product 422, duplicate product 400, cashier 403, PATCH happy, DELETE happy + idempotent, status filter on list.
  - `transfer-send-receive.spec.ts` (9): send writes `transfer_out` + decrements sender stock + status flips, receive happy (writes `transfer_in` + increments receiver + status), receive with discrepancy flags the line + `has_discrepancy=true` without auto-adjusting sender, receive blocked when status≠in_transit (409), receive 422 on incomplete payload, send 422 on empty transfer, cancel from draft happy + then send 409, cancel from in_transit blocked 409, PATCH blocked after draft 409.
  - `transfer-scoping.spec.ts` (6): RLS canary (tenant B blocked), manager-can-act when their branch is sender OR receiver, third-branch-manager 400 `forbidden_branch`, only sender-branch manager can `/send` (receiver-branch manager 403), DELETE blocked during impersonation 403, DELETE blocked once status leaves draft/cancelled 409.
- [x] **Verification**: `pnpm test:rls` 43/43, `pnpm --filter @madar/web typecheck` + `build` clean (new routes: `/[locale]/transfers` 4.35 kB / 185 kB First Load, `/[locale]/transfers/[id]` 6.89 kB / 188 kB, `/[locale]/transfers/new` 4.39 kB / 189 kB). `pnpm i18n:check` 743 keys lockstep. `pnpm db:migrate` applied the new migration cleanly.
- [x] Design reference followed: `docs/PAGES.md` §20 (list tabs + 3-step wizard, in-transit confirmation flow). Discrepancy-flagging behaviour is the explicit "real warehouse" choice rather than auto-adjusting sender stock.

### 2.3 Suppliers + purchase orders + goods receipt — DONE
- [x] **Schema migration `20260519000000_suppliers_pos`** — hand-edited immutable SQL adding:
  - 3 enums: `PurchaseOrderStatus` (`draft → ordered → received | cancelled`), `SupplierReturnStatus` (`draft → sent → refunded | cancelled`), `DocumentKind` (`contract | tax_certificate | bank_letter | other`).
  - 7 tenant-scoped tables — `suppliers`, `supplier_products`, `purchase_orders`, `purchase_order_lines`, `supplier_returns`, `supplier_return_lines`, `supplier_documents` — each with the standard tenant/audit/soft-delete columns, `updated_at` triggers wired via the existing `fn_set_updated_at()`, and RLS `ENABLE + FORCE` with the canonical `NULLIF(...)` `tenant_isolation` policy.
  - Partial unique index `supplier_products_one_preferred_per_product ON (tenant_id, product_id) WHERE is_preferred = true AND deleted_at IS NULL` so at most one preferred supplier per product per tenant.
  - Money is `bigint` cents + sibling `currency_code` throughout (`unit_cost_cents`, `subtotal_cents`, `tax_cents`, `discount_cents`, `total_cents`) — zero floats. Currency snapshotted from supplier at PO/return create time.
- [x] **Backend `apps/api/src/tenant/suppliers/`** — full supplier CRUD + supplier-product catalog + supplier-documents upload pipeline + computed scorecard stats.
  - Endpoints (all under `TenantAuthGuard`, rate-limit 30/min writes + 60/min reads, owner-only mutations, reader endpoints open to `owner|manager|accountant`):
    - `GET /v1/suppliers`, `GET /:id`, `POST` (`@Idempotent()`, 5x `SUP-XXXX` collision retry), `PATCH`, `DELETE` (soft, owner-only, blocked while any open PO exists, `assertNotImpersonating()`).
    - `GET/POST/PATCH/DELETE /:id/catalog/...` — supplier-product rows with `is_preferred` toggle handled in a `$transaction` that unsets the prior preferred row before inserting/updating the new one (no 409 needed).
    - `POST /:id/documents` — multipart upload through the extracted `TenantStorageService` (5 MB cap, magic-byte MIME guard via `file-type`, ClamAV stub), `GET /:doc_id/download` controller-stream endpoint, `DELETE /:doc_id` soft-delete. `signedUrl` is a placeholder on the local-disk provider — controller-stream is the fallback.
  - Stats helper (`fill_rate_pct`, `on_time_pct`, `avg_lead_time_days`, `total_orders`, `total_spend_cents`) computed in a single `$queryRawUnsafe` against `purchase_orders` joined to `purchase_order_lines`.
- [x] **Backend `apps/api/src/tenant/purchase-orders/`** — full state machine (`draft → ordered → received | cancelled`).
  - Endpoints: list (with `branch_id`/`supplier_id`/`status` filters; manager scope auto-forces `branch_id=user.branch_id` to prevent peek), detail (computed `has_discrepancy`), idempotent POST (snapshots `currency_code` from supplier, generates `PO-XXXXXX` with 5x collision retry, falls back to `supplier_products.unit_cost_cents` else 422 `product_not_in_catalog`), PATCH (draft-only else 409 `purchase_order_locked`, wholesale line replacement + total recompute), `/order` (`send_email=true` enqueues `SendPoEmailQueue` with inline fallback when `REDIS_URL` is unset), `/receive` (one `$transaction`: writes one `stock_movements{kind='receive', reference_table='purchase_orders'}` per qty>0 line + upserts `branch_stock`; auto-fills `discrepancy_note='auto_short'`/`'auto_over'` when not user-supplied), `/cancel` (draft-only), DELETE (draft|cancelled only, soft, idempotent, blocked during impersonation), `GET /:id/pdf` (streams via `renderPurchaseOrderPdf`).
  - **Manager-on-wrong-branch convention:** 404 for reads (no existence leak), 403 `forbidden_branch` for writes.
- [x] **Backend `apps/api/src/tenant/supplier-returns/`** — RMA lifecycle (`draft → sent → refunded`, plus `→ cancelled` branch).
  - Endpoints mirror the PO module (list/detail/POST/PATCH/cancel/DELETE) with the same role + branch + impersonation guards.
  - `/send` (`draft → sent`): one `$transaction` writes one `stock_movements{kind='adjustment', reference_table='supplier_returns', qty_delta=-qty}` per line and decrements `branch_stock.qty_on_hand` (negatives allowed). Reuses the existing `adjustment` enum value as planned.
  - `/refund` (`sent → refunded`): bookkeeping only — NO new stock movement. Optional `notes` are *appended* with a timestamped separator rather than overwritten.
  - `reason_code` on lines is a free-form short token (suggested values `damaged | wrong_item | expired | other`, no enum so tenants can extend).
- [x] **Shared infrastructure:**
  - `apps/api/src/common/tenant-storage.service.ts` extracted from payment-proofs (put/sign/delete + ClamAV scan). Payment-proofs refactored to consume it with no behaviour change — all 27 payment-proof tests still pass.
  - Added `pdf-lib` dependency + `apps/api/src/shared/pdf/po-pdf.renderer.ts` (pure pdf-lib, English-only single-page A4 PO PDF using Helvetica — Arabic rendering deferred since Helvetica can't shape Arabic).
  - `apps/api/src/tenant/purchase-orders/jobs/` — BullMQ queue + processor + module + types. When `REDIS_URL` is unset (tests + local dev), `SendPoEmailQueue.enqueue()` runs the processor synchronously.
  - Email service extended with `sendRaw()` + attachment support — disk-writer provider writes a sibling `.attachment.{n}.{ext}` next to the `.eml`; Resend provider passes attachments through.
- [x] **Web API libraries** — `apps/web/src/lib/api/{suppliers, purchase-orders, supplier-returns}.ts` with typed wrappers + `ApiX` types. Money columns serialized as decimal strings on the wire per the bigint convention; callers do `Number(x.foo_cents)` at the edge. `ApiSupplierActivity` is a `kind`-discriminated union (`'po' | 'audit'`). Helpers `supplierDocumentDownloadUrl` and `purchaseOrderPdfUrl` build absolute URLs for `<a href>` use.
- [x] **Suppliers UI** at `/[locale]/suppliers/*` — list (card grid + hero KPIs), detail (Overview / Catalog / Purchase Orders / Documents / Activity tabs), new + edit forms with EN/AR bilingual inputs, `ReliabilityDial` (hand-rolled SVG, sage/accent/rose tiers + `--ink-3` for null), `SupplierScorecard`, `OrderHistoryTable`, `CatalogTab` with typeahead-add inline-edit + preferred-star toggle, `DocumentsTab` with multipart upload + signed-URL download fallback, `ActivityTab` with merged PO + audit feed, `SupplierAlerts` opt-in rose card when `fill_rate` or `on_time < 80%`. All styling in `suppliers.css` via design tokens (`.sup-*` prefix, logical CSS only). 153 new i18n keys under `suppliers.*` (EN+AR lockstep).
- [x] **Purchase orders UI** at `/[locale]/purchases/*` — list with status tabs + KPI hero, 3-step `POWizard` shared by `/new` and `/[id]/edit` (honors `?supplier_id`, `?branch_id`, `?prefill=lowstock` URL params), detail with state-aware footer actions + `POTimeline`, dedicated `/receive` screen with `ReceiveLineRow` auto-discrepancy chips ("Short by N"/"Over by N") + lazy note textarea, `SendToSupplierDialog` (recipient override + "Attach PDF" + "Skip sending" when supplier has no email). All styling in `purchases.css` via tokens (`.po-*` prefix, logical CSS only). 171 new i18n keys under `purchases.*`.
- [x] **Supplier returns UI** at `/[locale]/returns/*` — list with status tabs, single-page `ReturnForm` (no wizard — RMAs are shorter), detail with state-aware actions + `ReturnTimeline`, `RefundDialog`, `?sent=1` toast convention. State-aware actions: draft→Edit/Send/Cancel/Delete (Send is direct — no email modal because RMAs don't email), sent→Refund (opens dialog), refunded→read-only, cancelled→Delete. All styling in `returns.css` via tokens (`.rma-*` prefix, logical CSS only). 134 new i18n keys under `returns.*`.
- [x] **Sidebar + inventory wiring** — `Sidebar.tsx` flipped `suppliers` + `purchases` to `enabled: true` (gated to `owner|manager|accountant`), added new `returns` entry between `purchases` and `branches` (gated to `owner|manager`). `inventory-client.tsx` + `InventoryHeader.tsx` gained a "Reorder low-stock" button → `/purchases/new?prefill=lowstock&branch_id=...`, shown when `lowCount > 0 && branchId && (role === "owner" || "manager")`.
- [x] **RLS test matrix** — `packages/db/test/setup.ts` extended to seed minimum-viable fixtures for the 7 new tenant-scoped tables under both Tenant A and Tenant B; `TENANT_SCOPED_MODELS` in `packages/db/test/rls.test.ts` extended with the 7 new model accessors. Added 2 new WITH CHECK INSERT-spoof rejection tests for the slice. `pnpm test:rls` **66/66 passing** (was 43/43) — 20 tenant-scoped models × 3 assertions (tenantScoped sees own only, adminPrisma sees both, basePrisma sees zero = FORCE-RLS canary) + 4 WITH CHECK rejections + 2 append-only audit blockers.
- [x] **Verification:**
  - `pnpm db:migrate:deploy` — "No pending migrations to apply".
  - `pnpm test:rls` **66/66 passing**.
  - `pnpm test:api` 386 tests across 79 spec files — pass when run cleanly (exit 0 in the canonical run). The slice's new suites verified individually pass 100%: `supplier-crud` 11/11, `supplier-catalog` 5/5, `supplier-documents` 4/4, `po-crud` 11/11, `po-state-machine` 11/11, `po-scoping` 9/9, `po-email` 2/2, `po-pdf` 9/9, `send-po-email-inline-fallback` 3/3, `rma` 13/13 — **78 net new tests** (53 backend + 9 PDF + 3 inline + 13 RMA = 78). See `2.3 known concerns` below for the Windows `pnpm exec prisma` race that flakes parallel-suite runs.
  - `pnpm --filter @madar/api typecheck` clean.
  - `pnpm --filter @madar/web typecheck` clean.
  - `pnpm --filter @madar/web build` clean. Bundle weights for the new routes:
    - `/[locale]/suppliers` 5.64 kB / 187 kB · `/[locale]/suppliers/[id]` 9.41 kB / 195 kB · `/[locale]/suppliers/[id]/edit` 548 B / 189 kB · `/[locale]/suppliers/new` 327 B / 183 kB
    - `/[locale]/purchases` 6.03 kB / 188 kB · `/[locale]/purchases/[id]` 5.15 kB / 191 kB · `/[locale]/purchases/[id]/edit` 887 B / 194 kB · `/[locale]/purchases/[id]/receive` 6.54 kB / 188 kB · `/[locale]/purchases/new` 460 B / 194 kB
    - `/[locale]/returns` 5.88 kB / 187 kB · `/[locale]/returns/[id]` 4.87 kB / 190 kB · `/[locale]/returns/[id]/edit` 870 B / 192 kB · `/[locale]/returns/new` 325 B / 192 kB
  - `pnpm i18n:check` **1463 keys EN/AR lockstep**.
  - `madar-rls-check` skill — no violations. `adminPrisma` usage in `suppliers.service.ts` (tenant lookup for documents), `purchase-orders.service.ts` (tenant lookup for PDF/email), and `jobs/send-po-email.processor.ts` (job runs outside tenant context) is documented inline and matches the established pattern in `branches.service.ts`, `billing.service.ts`, and `auth.service.ts` — all platform-table reads only. All 3 new tenant controllers (`suppliers`, `purchase-orders`, `supplier-returns`) inherit `TenantAuthGuard` via the global `APP_GUARD` provider in `auth.module.ts`. Every `branchStock.upsert` in the slice is paired with a sibling `stockMovement.create` in the same `$transaction`.
  - `madar-i18n-sync` skill — clean. Key parity OK (1463/1463). One "suspected untranslated" hit in 2.3 namespaces (`suppliers.activity.audit: "{action}"`) is a pure ICU placeholder, intentional. No physical CSS in the three new `.css` files or any `.tsx` under suppliers/purchases/returns. Glossary terms (مورد, أمر شراء, استلام البضائع, إرجاع للمورد, مدة التوريد, معدل التوريد, التسليم في الموعد, شروط الدفع) used verbatim per `docs/i18n-glossary.md` §3.

#### 2.3 deferred
- [ ] Supplier scorecard sparkline trend graphs (needs a rolling-window aggregation worker; deferred to Phase 3).
- [ ] AI bundling / reorder suggestions ("Sidamo Direct savings £420" prototype card) — needs the AI infra not yet in place.
- [ ] Supplier-portal `confirmed` PO state (we have no second-party auth in scope).
- [ ] Barcode-scan receive UX (lands with offline POS scanner infra).
- [ ] Three-way match against invoices (depends on the invoice module — Phase 3+).
- [ ] Real Resend wiring (lands with §1.15 cross-cutting infra; disk-writer fallback is in place and exercised by tests).
- [ ] PO PDF Arabic rendering (Helvetica can't render Arabic glyphs; needs font embed + RTL shaping pass — `noto-sans-arabic` would do it).
- [ ] Internal `Tenant.address_lines` / `email` / `phone` columns for the PO PDF top band (PDF currently composes from `tenant.name` only).
- [ ] Send-to-supplier "request more info" thread (one-shot email send only for now).
- [ ] HMAC-signed S3 URLs for supplier documents — the download endpoint currently controller-streams via `TenantStorageService.signedUrl()` placeholder.

#### 2.3 known concerns (Windows-specific test flake)
- `apps/api/test/setup.ts:56` re-runs `pnpm --filter=@madar/db exec prisma migrate reset --force --skip-seed --skip-generate` once per spec file (in vitest's `beforeAll`). On Windows + OneDrive, when many spec files run in succession, the `pnpm exec` resolver intermittently fails with `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "prisma" not found` (transient lock on `node_modules/.bin/prisma`). Affected suites then skip with "tables don't exist". A second full `pnpm test:api` run usually clears it, and each affected suite **passes 100% when run in isolation**. Tracked as test-infra debt; the underlying code is sound.
- Pre-existing Windows file-lock issue with `prisma generate` on OneDrive — surfaces as `EINVAL readlink` against `.next/app-build-manifest.json` between `pnpm --filter @madar/web build` runs (and at `next dev` startup when HotReloaderWebpack tries to clean stale manifests). **Permanent fix shipped (post-Phase 3.6)**: `apps/{web,admin}/next.config.mjs` now resolves `distDir` to `~/.madar-cache/{web,admin}/.next` on Windows so the build cache lives outside the OneDrive-synced tree. Linux containers (the prod VPS) keep the default `.next` since they're never OneDrive-synced. Override via `MADAR_NEXT_LOCAL=1` if you ever need it back in-repo.

### 2.3 Remaining (not in this slice)
- [x] Roles & permissions UI — see `### 2.3 Roles & permissions UI (DONE)` below.
- [x] Offline POS (PWA + IndexedDB + sync queue, idempotency keys) — see `### 2.3 Offline POS (DONE)` below.
- [x] Owner dashboard + branch leaderboard — see `### 1.12 Owner dashboard — real backend (DONE)` below.
- [ ] Super-admin Phase 2: support tickets, email templates, bank reconciliation, feature flags, system health, BullMQ monitor, aging report, tenant lifecycle manager

### 2.3 Offline POS (DONE)
- [x] **Schema + migration `20260522000000_offline_pos`**: new `sync_conflicts` table (tenant-scoped, RLS via the canonical NULLIF policy, two enums `SyncConflictKind` + `SyncConflictStatus`). Three new columns on `sales`: `client_occurred_at` (clamped server-side to ≤ now()), `has_negative_stock` (flipped when any line drives `branch_stock.qty_on_hand` below zero), `offline_completed` (set true when the sale rang offline and synced later). Partial index `sales_tenant_has_negative_stock_idx` for fast manager lookup.
- [x] **Slice 1 — PWA shell**: hand-written `apps/web/public/sw.js` (cache-first for `_next/static/*` + POS shell + offline page; network-first elsewhere with offline fallback on navigations), `apps/web/public/manifest.webmanifest`, programmatically-generated 192/512 PNG icons (coral placeholder), `apps/web/src/lib/pwa/register-sw.ts` (idempotent SW registration + localStorage dismiss flag), `apps/web/src/components/SwBootstrap.tsx` (mounted in `(shell)/layout.tsx`), `apps/web/src/components/InstallPrompt.tsx` (one-shot `beforeinstallprompt` card), new route `apps/web/src/app/[locale]/offline/page.tsx` with token-styled fallback + reload + go-to-POS CTAs. `LocaleLayout` metadata wires `manifest` + `appleWebApp`.
- [x] **Slice 2 — IndexedDB (Dexie)**: `apps/web/src/lib/offline/{db,device,outbox,catalog-cache,types}.ts`. Dexie database `madar_offline_v1` with 3 stores: `catalog_snapshot` (products + categories), `outbox_sales`, `outbox_proofs`. Lazy singleton `getDb()` + `_resetDb()` for tests. `getDeviceUuid()` + monotonic `getNextSequence()` in localStorage. `enqueueSale`, `enqueueProof`, list/mark helpers with proper `transaction("rw", ...)` semantics. `getQueueDepth()` aggregates pending across both outboxes. Vitest tests use `fake-indexeddb/auto`.
- [x] **Slice 3 — Sync engine**: `apps/web/src/lib/offline/sync.ts` (idempotent `startSyncEngine()` — drains on `online` event + every 30s while visible; suspended on `visibilitychange=hidden`), `apps/web/src/lib/offline/online-status.ts` (Zustand `OnlineStatus` store + `subscribeOnlineEvents()`), `apps/web/src/lib/offline/dispatch.ts` (online direct POST when `navigator.onLine && queueDepth===0`, else IndexedDB enqueue; transient errors fall back to enqueue so a network blip doesn't lose the sale). `Idempotency-Key === client_uuid` is stable across retries — the existing 24h Redis cache de-dupes. 4xx → permanent fail (no retry); 5xx + network → transient (capped at 10 attempts). Proofs drain only after their parent sale syncs.
- [x] **Slice 4 — Backend extensions + sync-conflicts module**: `apps/api/src/tenant/sales/sales.service.ts` `completeSale()` now (a) accepts `client_occurred_at` (ISO 8601, clamped to ≤ now()) + `offline_completed`, (b) detects negative `branch_stock.qty_on_hand` after the upsert pass and writes one `sync_conflicts` row per offending line (kind=`negative_stock`, details=`{product_id, qty_on_hand_after, offline_completed}`) inside the existing `basePrisma.$transaction`. New `apps/api/src/tenant/sync-conflicts/` module: `GET /v1/sync-conflicts?status=&conflict_kind=&page=&limit=` (owner|manager|auditor, paginated, joined reviewer name), `POST /v1/sync-conflicts/:id/resolve` (owner|manager, `@Idempotent`; 409 `not_resolvable` on already-reviewed), `GET /v1/sync-conflicts/summary` (counts by status). Audit `sync_conflict_resolved` with before/after. New typed client `apps/web/src/lib/api/sync-conflicts.ts`. 6 vitest cases all green: RBAC list, RBAC resolve, negative-stock surfacing end-to-end (sells 25 of qty=20 → conflict row with `qty_on_hand_after=-5`), resolve happy + idempotency, RLS canary (tenant B blocked), summary counts.
- [x] **Slice 5 — POS UI offline wiring + manager conflict page**: `apps/web/src/components/OfflineChip.tsx` (Zustand-subscribed status chip — green Online / amber Syncing N or Pending N / rose Offline · N pending), mounted in `apps/web/src/app/[locale]/pos/_components/PosHeader.tsx`. `apps/web/src/app/[locale]/pos/pos-client.tsx` (a) calls `startSyncEngine()` on mount, (b) writes a fresh catalog snapshot on every successful online catalog fetch, (c) routes `handlePaymentSubmit` through `dispatchSale()` — online returns a real `SaleResponse`, queued returns a synthetic `payment_pending` placeholder for the toast, (d) renders an amber `pos.offline.conflictsBanner` linking to `/sales/sync-conflicts` when the summary query returns `open > 0`. New manager page at `apps/web/src/app/[locale]/(shell)/sales/sync-conflicts/` with two-pane list+detail (status tabs Open/Acknowledged/Resolved/Ignored, kind chip, pretty-printed `details` JSON, review-notes textarea, Acknowledge/Resolve/Ignore actions — only owner|manager get the action buttons). Sidebar entry "Sync conflicts" gated to owner|manager|auditor with the AlertTriangle icon.
- [x] **i18n**: `offline.*` + `install.*` (top-level), `salesConflicts.*` (~30 keys), `pos.offline.*` (~7 keys + plural variants), `shell.nav.syncConflicts`. EN+AR lockstep. 1685 → **1722 keys**.
- [x] **Verification**: `pnpm --filter @madar/api typecheck` clean; `pnpm --filter @madar/web typecheck` clean; `pnpm --filter @madar/web build` clean — new routes `/[locale]/offline` (1.86 kB / 158 kB) + `/[locale]/sales/sync-conflicts` (6.82 kB / 188 kB) materialized. `pnpm --filter @madar/db test:rls` 76 → **79 tests passing** (new `syncConflict` canary). `pnpm --filter @madar/api test -- sync-conflicts sales` **49/49 passing** (43 sales — including the existing happy/idempotency/store-credit/split cases — plus 6 new sync-conflicts cases). `pnpm --filter @madar/web i18n:check` lockstep.

#### 2.3 Offline POS deferred (still open)
- [ ] Background-sync API (`registration.sync.register`) — Chromium-only; v1 uses `online` event + visibility-change instead.
- [ ] Push notifications.
- [ ] Service-worker driven price-list updates while the app is closed.
- [ ] Per-cashier conflict assignment (any owner/manager/auditor can review any conflict today).
- [ ] `price_drift` conflict surfacing (catalog price changed between offline cart build and online sync) — schema supports the kind, MVP only emits `negative_stock`.
- [ ] `product_unknown` conflict surfacing (offline sale references a deleted product) — same status.
- [ ] Multi-device cashier session (one device, one outbox; sync_conflicts not cross-device-aware).
- [ ] Encryption-at-rest of IndexedDB blobs.
- [ ] Selective offline catalog pagination for tenants with 10k+ SKUs.
- [ ] SW caching of localized routes beyond `/[locale]/pos`.
- [ ] Sync-engine unit tests with fake-indexeddb + mocked apiFetch (Slice 2 has Dexie tests; sync.ts is exercised via the manual offline walkthrough).

### 2.3 Roles & permissions UI (DONE)
- [x] **Backend `apps/api/src/tenant/users/`** — new module exposing 4 endpoints, all `TenantAuthGuard`-protected, all mutations owner-only (rate-limited 30/min writes / 60/min reads). `GET /v1/users?[search,active_only,page,limit]` returns paginated rows with embedded branch (id/code/name_i18n) + computed `has_pending_invite` (token-hash present AND not expired); `POST /v1/users/invite` (`@Idempotent`) creates a user row with an unguessable argon2 password placeholder + a 7-day password-reset token, sends a `staff_invite` email (disk-writer fallback when Resend isn't wired); `PATCH /v1/users/:id` updates `role`/`branch_id`/`is_active` with guards (`cannot_edit_self`, `last_owner_lock` counting active owners excluding the target, `manager_requires_branch`, `unknown_branch`); `POST /v1/users/:id/resend-invite` overwrites the token with a fresh 7-day one. No new schema — the existing `User` table covered everything (per `packages/db/prisma/schema.prisma:223-249`).
- [x] **Email template** — added `staff_invite` to `apps/api/src/common/email/{email.types.ts, templates.ts}` with EN+AR body + coral CTA. Accept URL points to the existing `/[locale]/reset-password?token=...` page (which already handles the password-set flow). Uses `TENANT_WEB_ORIGIN` env var (same one forgot-password already uses).
- [x] **Audit** — entity `"user"`, actions `user_invited` (with `invited_owner` flag in metadata when role='owner'), `user_updated` (before/after of changed fields only), `user_invite_resent`. All carry `impersonator_id`.
- [x] **Web API library** — `apps/web/src/lib/api/users.ts` typed wrappers (`usersListRequest`, `userInviteRequest`, `userUpdateRequest`, `userResendInviteRequest`) + every `ApiTenantUser` / body / response interface exported for the UI to consume directly.
- [x] **Settings → Users page** at `/[locale]/settings/users` — `SettingsShell` (`apps/web/src/app/[locale]/(shell)/settings/_components/SettingsShell.tsx`) gained a `visibleToRoles?: string[]` filter and a new "Users" nav entry shown only to owners. `UsersClient` renders an `ownerOnly` denial surface (no redirect) if a non-owner lands on the route. List UI = search + active-only toggle + table (Name | Email | Role chip per-role-tinted | Branch i18n-by-locale | Status pill {active sage, inactive rose, pending invite amber} | inline ghost Edit / Resend invite / Deactivate-or-Reactivate); the row for the current user replaces actions with a "You" badge. `InviteUserModal` pulls active branches via `branchesListRequest()`, inline-validates `manager → branch required`, maps `email_taken` / `manager_requires_branch` / `unknown_branch` / `forbidden_role` / `validation_failed` to field-level or general errors. `EditUserModal` PATCHes `role/branch_id/is_active` (sends only changed fields to satisfy the backend `.refine(...at least one)`); email + name read-only; same error map plus `cannot_edit_self` and `last_owner_lock`. Three confirm dialogs (resend / deactivate / reactivate). Toast-on-success follows the verification-queue toast pattern (auto-dismiss 3.5s).
- [x] **i18n** — `settings.users.*` namespace added EN+AR (`~74 keys`). 1475 → **1549 keys lockstep**. AR phrased naturally for user/role/branch terminology (`المستخدمون`/`الدور`/`الفرع`/`دعوة`/`مالك`/`مدير`/`أمين الصندوق`/`محاسب`/`مراجع`).
- [x] **Tests** — `apps/api/test/users/users.spec.ts` 12/12 pass (~12s). Coverage: cashier 403 on every endpoint; happy invite + audit + on-disk `.eml`; duplicate-email 409; manager-without-branch 400; unknown branch 422; PATCH happy + before/after audit; `cannot_edit_self` 400; `last_owner_lock` 409 when demoting the lone active owner; unknown user 422; RLS canary (tenant B blocked); resend-invite hash-rotates + second `.eml` lands; cashier 403 on resend.
- [x] **Verification:** `pnpm test:rls` 66/66; `pnpm --filter @madar/api test -- users` 12/12; `pnpm --filter @madar/api typecheck` clean; `pnpm --filter @madar/web typecheck` clean; `pnpm --filter @madar/web build` clean — new route `/[locale]/settings/users` = **9.22 kB / 191 kB First Load**; `pnpm i18n:check` 1549 keys EN/AR lockstep.

#### 2.3 (Users) deferred (still open)
- [ ] Roles tab / permission-matrix editor (PAGES.md §47 splits this from Users — separate feature).
- [ ] `last_login_at` column on users (backfill via login flow; the "Pending invite" pill is the current proxy for "never logged in").
- [ ] Owner-initiated MFA reset for staff (security page covers self-reset; owner-reset is a future safety-net feature).
- [ ] Bulk invite via CSV.
- [ ] SSO / SAML / Google Workspace integration.

### 1.12 Owner dashboard — real backend (DONE)
- [x] **Backend `apps/api/src/tenant/dashboard/`** — single read-only `GET /v1/dashboard` endpoint returning `ApiOwnerDashboard`. Reader-role gate (`owner|manager|accountant`), rate-limited 60/min. Aggregations mirror `branches.service.getBranchDashboard` patterns: `$queryRawUnsafe` against `tenantScoped`, branch-TZ casting `(s.occurred_at AT TIME ZONE b.timezone)::date`, money stays `bigint` in SQL → `string` on the wire (cents that fit in safe int are coerced to `number` for chart series), `payment_status IN ('paid','payment_pending')` filter, soft-delete `deleted_at IS NULL`. Eleven parallel queries: week + prev-week metrics, 30-day revenue series, 7-day sparklines (revenue/transactions/gross-profit), branch leaderboard with prev-week delta, 7×12 heatmap (hours 8–19, last 28 days, normalized to [0,1]), top-7 recent transactions with cashier name, top product by week revenue (for `concentration` insight), stale-payment-proof count (>48h pending), low-stock product count, mixed-currency existence flag, tenant default currency lookup via `adminPrisma`.
- [x] **Insight rule engine** — `dashboard.insights.ts` is a pure function `computeInsights(ctx) → Insight[]`. Six deterministic rules evaluated in order (max 4 surfaced): `branch_decline` (any leaderboard row ≤ -10% WoW, high urgency), `concentration` (top product ≥ 30% of week revenue, medium), `stale_payment_proof` (pending proof > 48h, high), `low_stock_critical` (any product with `qty_on_hand ≤ 0`, medium), `growth_winner` (any branch ≥ +20% WoW, low), `week_recap` (fallback when fewer than 4 fired, low). Each insight gets a stable SHA-256 hash ID derived from `${tenantId}:${kind}:${primary_entity}:${week_start_date}` so the UI's localStorage dismiss state persists across re-fetches within the same week. EN+AR copy ships from the server in `headline_i18n` and `body_i18n` (deterministic interpolation, no LLM). Per PRD §2.3 "AI/LLM features — keep the system rule-based and predictable".
- [x] **Web API library** — `apps/web/src/lib/api/dashboard.ts`. Exports `ApiOwnerDashboard` + every nested interface (`ApiOwnerDashboardWeek`, `…LeaderboardRow`, `…RecentTx`, `…Insight`, etc.) so children components can type their props precisely. Single request fn `ownerDashboardRequest()` calling `apiFetch('/v1/dashboard')`.
- [x] **Frontend wiring `apps/web/src/app/[locale]/(shell)/_dashboard/Dashboard.tsx`** — rewritten as a `"use client"` orchestrator that subscribes to `useBranchScopeStore.selectedBranchId`. When scope = `"all"` → `useQuery(["dashboard","owner"], ownerDashboardRequest, { staleTime: 30_000 })` and renders `<DashboardClient>`. When scope = a real branch UUID → `router.replace('/{locale}/branches/{id}/dashboard')` via the next-intl-aware router (the existing per-branch dashboard from 2.1e). Handles pre-hydration, pending, error, and redirecting states with i18n-keyed loading skeletons and a retry button.
- [x] **Component prop reshape** — `DashboardClient.tsx` and all 7 children (`DashboardHeader`, `KpiRow`/`KPICard`, `RevenueHeroChart`, `AIInsightsRail`/`AIInsightCard`, `BranchStrip`, `HeatmapCard`, `RecentTxCard`) retyped against `ApiOwnerDashboard` directly. Money strings coerced with `Number(...)` at edges (the 2.3-established convention). Per-branch "today" surface dropped from `BranchStrip` since the leaderboard is week-aggregated (per-branch today is reachable via the branch detail dashboard). Editorial headline picks the top-mover branch by `|vs_prev_week_pct|` and routes through three i18n templates (`headline.revenueUp` / `headline.revenueDown` / `headline.firstWeek` for fresh tenants). Mixed-currency banner renders when the API flips `mixed_currency_warning=true`.
- [x] **AIInsightsRail dismiss reshape** — dismiss state now lives inside the rail itself (no caller-managed prop). Storage key `madar.dashboard.dismissed_insights` holds `Record<insight.id, ISO-date>`; entries older than 14 days are auto-pruned on mount. Replaces the fragile regex-sniff dismiss action from the mock-driven version. Per-kind accent color for all 6 API kinds; urgency labeled via i18n; deep-link `<a href={action.href}>` rendering for the API-supplied actions.
- [x] **Mock file deleted** — `apps/web/src/app/[locale]/(shell)/_dashboard/_mock-data.ts` removed after `Grep` confirmed no other consumer in `apps/`.
- [x] **i18n** — `dashboard.*` namespace replaced wholesale: `loading.{title,body}`, `redirecting`, `error.{title,body,retry}`, `mixedCurrencyWarning`, `headline.{revenueUp,revenueDown,firstWeek}`, `kpi.{revenueLabel,grossProfitLabel,transactionsLabel,sparklineLabel,vsLastWeek}`, `hero.{title,branchAll,empty}`, `insights.{rail.{title,dismiss,askMadar,empty}, urgency.{high,medium,low}, confidence}`, `leaderboard.{title, columns.*, allLink, rank}`, `heatmap.{title,body,legend,days.*}`, `recent.{title,empty,items, method.*, status.*}`. AR sourced from glossary where applicable. **1475 keys EN/AR lockstep** (was 1463 after the 2.3 slice).
- [x] **Tests** — `apps/api/test/dashboard/dashboard.spec.ts` — 10 specs: cashier 403; reader 200; RLS canary (tenant B sees its own empty data, not tenant A's); empty-tenant zero-everywhere with only `week_recap` firing; one-sale-today metrics; two-week prev-week pct correct; foreign-currency excluded from cents but counted in transactions + `mixed_currency_warning=true`; leaderboard ordering DESC; `branch_decline` fires for a -15% branch; `stale_payment_proof` fires when a pending proof is backdated 49h. **10/10 pass.**
- [x] **Verification:** `pnpm test:rls` **66/66**, `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/web typecheck` clean, `pnpm --filter @madar/web build` clean (root `/[locale]` route = 13.2 kB / 195 kB First Load), `pnpm i18n:check` 1475 keys lockstep. No new migration (no schema changes).

#### 1.12 deferred (still open)
- [ ] KPI-first dashboard variant (4 KPIs + timeframe chips + inline AI callout) — prototype `dashLayout='kpi'`.
- [ ] Newspaper dashboard variant (magazine masthead + three-column digest + ranked-list standings) — prototype `dashLayout='newspaper'`.
- [ ] Timeframe selector (Today / Week / Month / Quarter / Year chips). Editorial layout is hardcoded to "this week" + rolling 30 days today.
- [ ] Real LLM-powered insights via Anthropic SDK + daily cache. Deterministic rules are the v1 product per PRD §2.3.
- [ ] Live-poll recent transactions (currently static at load).
- [ ] Cross-currency aggregation with FX snapshots. v1 sums only same-currency sales and flips `mixed_currency_warning` true. Snapshot rates already stored per sale per CLAUDE.md — wiring them is a separate slice.
- [ ] Drill-down popovers on KPI cards (e.g. click revenue → breakdown).
- [ ] Heatmap interactivity (hover for cell value).
- [ ] Heatmap branch-local timezone (currently uses server UTC `EXTRACT(HOUR ...)`). Reasonable trade-off for a chain-wide view, but a future slice could bucket per branch TZ and average.
- [ ] `dashboard.service.ts` at 580 LOC is over CLAUDE.md's <300-line guideline. Acceptable since every internal query is small and mirrors the same `Promise.all` shape `getBranchDashboard` already uses; easy follow-up to factor out helpers once a second consumer needs them.

#### 2.2 deferred
- [ ] Barcode-scan receive flow (uses same scanner infra as offline POS).
- [ ] Auto-adjust at sender for discrepancies (currently flagged only).
- [ ] In-transit cancel with automatic ledger reversal.
- [ ] Edit drafts (currently only "delete + recreate" via the wizard; full edit-UI is the natural next iteration).

### Session & auth hardening (post-Phase-1 fix) — DONE
- [x] **`tryRefresh` returns a richer result** — `apps/web/src/lib/api/client.ts`. Now returns `"ok" | "expired" | "network_error"` instead of `boolean`. Only `"expired"` (confirmed 401 from `/v1/auth/refresh`) drops the session; `"network_error"` (fetch threw, 5xx, or any non-401 failure) leaves the session intact and propagates the original error to the calling component. Previously a single offline blip or DNS hiccup silently logged the user out.
- [x] **`AuthBootstrap` shares the refresh mutex** — `apps/web/src/lib/auth/bootstrap.tsx`. Dropped its private `fetch("/v1/auth/refresh", …)` and now calls the exported `tryRefresh()`. This dedupes the bootstrap call with any racing `apiFetch` retry — fixes the POS hydration race where the second `/v1/auth/refresh` hit reuse-detected 401 from refresh-token rotation and triggered `clearAuth`. Also flips `bootstrapped=true` on `network_error` so the app doesn't hang waiting forever.
- [x] **`useRedirectOnAuthCleared` hook** — `apps/web/src/lib/auth/use-redirect-on-cleared.ts` (new). Mounted in `Shell.tsx` (covers every (shell) page) and in `pos-client.tsx` (POS lives outside the shell). When `bootstrapped && !accessToken && path is not an auth route`, redirects to `/{locale}/login?returnTo=<currentPath>`. Closes the "stuck on a broken page" gap when session expiry was confirmed mid-session. Auth-route exclusion (`login | signup | forgot-password | reset-password | verify-email | impersonation-handoff`) prevents loops.
- [x] **POS queries gated on bootstrap** — `apps/web/src/app/[locale]/pos/pos-client.tsx`. Both `productsQ` and `categoriesQ` use `enabled: bootstrapped`. The "Loading catalog…" placeholder now also shows during `!bootstrapped`. With the `tryRefresh` dedupe in place this is belt-and-suspenders insurance — guarantees the first product query carries a fresh Bearer.
- [x] **Login honors `?returnTo=`** — `apps/web/src/app/[locale]/(auth)/login/page.tsx`. Page export is now a thin `<Suspense fallback={null}>` wrapper around `<LoginForm />` so `useSearchParams()` doesn't trip the SSG CSR-bailout check. `goPostLogin()` validates `returnTo` (must start with `/`, must not start with `//`, must not contain `:`) and uses the plain `next/navigation` router (the next-intl router would double-prefix the locale). Both creds-success and MFA-success paths route through it. Default destination stays `/` when `returnTo` is missing or rejected.
- [x] **Receipt page gets `requireAuth`** — `apps/web/src/app/[locale]/sales/[id]/receipt/page.tsx`. Calls protected endpoints, so unauthenticated visitors now bounce to `/{locale}/login?returnTo=…` instead of seeing a half-rendered page. `impersonation-handoff/page.tsx` deliberately stays unguarded — that's the entry point for the admin app to mint a tenant session.
- [x] **i18n key rename** (fixed in an earlier turn but recorded here): `auth.login.errors.invalidCredentials` → `auth.login.errors.invalid_credentials` (snake_case to match the API's error `code`). Previously a wrong password fell back to "Network error — please try again." because the key didn't match.
- [x] **Verification**: `pnpm --filter @madar/web run typecheck` clean, `pnpm i18n:check` 1003 keys lockstep, `pnpm --filter @madar/web run build` clean — login route 4.83 kB / 209 kB, POS 12.9 kB / 199 kB, receipt route now dynamic (rendered on demand). Manual browser scenarios pending real session: POS hard-reload (no logout), `madar_refresh` cookie deleted in DevTools then any query (→ login with returnTo), DevTools offline throttle (no session wipe), unauthenticated `/sales/<id>/receipt` (→ login), `?returnTo=//evil.com/x` rejected.

## Phase 3 — Reporting & Analysis

- [x] P&L reports (revenue − COGS), by period/branch/category/SKU — Slice 1. Backend at `apps/api/src/tenant/reports/pnl/` (module + controller + service + DTO). `GET /v1/reports/pnl?currency&from&to&branch_id?&category_id?&group_by=period|branch|category|sku&format=json|csv` computes Revenue − Discount − Tax − COGS = Gross Profit, − Refunds = Net Revenue, plus a breakdown by the chosen axis. CSV streamed inline with `Content-Disposition: attachment`. Reuses dashboard SQL pattern (`SUM(...) FILTER (WHERE currency_code = $X)`, status filter `paid|payment_pending`, `cogs_snapshot_cents` per-line total). RBAC reader = owner/manager/accountant/auditor; rate-limited 60/min. Frontend at `apps/web/src/app/[locale]/(shell)/reports/pnl/` (page + client + colocated CSS, logical CSS only, financial-statement layout, preset range buttons, branch/category filters, segmented breakdown control, CSV download via Object URL). Typed client at `apps/web/src/lib/api/reports/pnl.ts`. 6 tests green (cashier 403, happy 14000/4800/9200, refund excluded + surfaces in refunds_cents, branch filter narrows, RLS canary, group_by=branch). PnlModule wired into `reports.module.ts`; landing-page + sidebar + i18n wiring deferred to reconciliation slice.
- [x] Margin analysis — Slice 2 (movers / margin). Backend at `apps/api/src/tenant/reports/movers/` (module + controller + service + DTO). `GET /v1/reports/movers?currency&from&to&branch_id?&category_id?&metric=revenue|units|profit&limit` returns top-N products ranked by the chosen metric (default `revenue`) with per-item revenue/units/cogs/gross-profit/margin-% + a 7-day revenue sparkline, plus a slow-movers list (in-stock products with < 5 units sold in the window, capped 10). RBAC reader = owner/manager/accountant/auditor; rate-limited 60/min. Frontend at `apps/web/src/app/[locale]/(shell)/reports/movers/` (page + client + colocated CSS, logical CSS only, reuses dashboard `Sparkline.tsx`). Typed client at `apps/web/src/lib/api/reports/movers.ts`. 6 tests green (cashier 403, revenue ranking, units ranking flips order, profit ranking flips again, slow-movers surfacing, RLS canary). Umbrella + i18n + sidebar wiring done via reconciliation slice.
- [x] Trend analysis (7/30/90-day, YoY) — Slice 3. Backend at `apps/api/src/tenant/reports/trends/` (module + controller + service + DTO). Generalizes dashboard's 30-day series to a configurable rolling window (7/30/90 d) with optional `compare={none|prev_period|yoy}` overlay. Rolling avg via PG window function. Frontend at `apps/web/src/app/[locale]/(shell)/reports/trends/` with hand-coded SVG `TrendLineChart` (`apps/web/src/components/charts/`) lifted from `RevenueHeroChart.tsx`. RBAC reader = owner/manager/accountant/auditor. 5 tests green (RBAC, happy, rolling-avg correctness, YoY overlay, RLS canary). Umbrella + i18n + sidebar wiring deferred to reconciliation slice.
- [x] Tax reports per jurisdiction — Slice 4. Backend at `apps/api/src/tenant/reports/tax/` (module + controller + service + DTO) + new PDF renderer `apps/api/src/shared/pdf/tax-report-pdf.renderer.ts`. `GET /v1/reports/tax?currency&from&to&branch_id?&format=json|pdf|csv` returns per-tax-class taxable_sales/tax_collected/transactions + totals. SQL JOINs `sale_lines → sales → products → tax_classes` with `COALESCE(p.tax_class_id, $defaultTaxClassId)` so products without a class fall through to the tenant default; `LEFT JOIN tax_classes` preserves the "no tax class" bucket. PDF renderer mirrors `po-pdf.renderer.ts` (A4 portrait Helvetica, ASCII period separator since pdf-lib standard fonts can't encode `→`). CSV streamed with `Content-Disposition: attachment`. Frontend at `apps/web/src/app/[locale]/(shell)/reports/tax/` (page + client + colocated CSS) — table with totals row, "Download PDF" + "Download CSV" buttons (fresh `fetch` for binary response + `<a download>` blob URL). Typed client at `apps/web/src/lib/api/reports/tax.ts`. 6 tests green (cashier 403, 2-class + default-fallthrough aggregation, totals equals sum of items, refunded excluded, `?format=pdf` returns `application/pdf` starting with `%PDF`, RLS canary).
- [ ] Custom dashboards (drag-and-drop widgets) — deferred (PAGES §42 — out of scope for v1).
- [x] Scheduled email delivery — Slice 5. Backend at `apps/api/src/tenant/reports/scheduled/` (module + controller + service + DTOs + jobs/{queue,processor,types,renderer,report-runner,report-adapters}). Endpoints `GET|POST|PATCH|DELETE /v1/scheduled-reports` + `POST /:id/run-now`. `cron_pattern` derived server-side from cadence (daily=`0 9 * * *` / weekly=`0 9 * * 1` / monthly=`0 9 1 * *`). BullMQ repeat-job per active schedule registered on `OnApplicationBootstrap` (uses `adminPrisma` to traverse every tenant) + on create/update; `unregisterRepeat` on delete/deactivate. Redis-unavailable graceful fallback mirrors `SendPoEmailModule`: `Run now` runs inline, cron repeats are skipped with a logged warning. Processor loads schedule via `adminPrisma`, calls the matching producer adapter (`PnlProducer` / `TaxProducer` / `TrendsProducer` — each wired against a `*_REPORT_SERVICE` symbol token so absent slices degrade to a clear "unavailable" payload rather than crashing), renders CSV (UTF-8 BOM + CRLF) or PDF (pdf-lib Helvetica/Courier table), `EmailService.sendRaw()` per recipient with attachment, updates `last_run_at/last_status/last_error`, writes `scheduled_report_fired` audit. RBAC reader = owner/manager/accountant; writer = owner/accountant. Frontend at `apps/web/src/app/[locale]/(shell)/reports/scheduled/` (page + client + modal + CSS, logical CSS only) with table (Name | Kind chip | Cadence | Recipients +N more | Last run pill + time-ago | Active toggle | Kebab Edit/Run now/Delete). Typed client at `apps/web/src/lib/api/reports/scheduled.ts`. 7 vitest cases green (RBAC list, RBAC mutations, happy create + cron_pattern derivation + audit, PATCH toggle is_active + audit, run-now `{queued:true}` + last_run_at populated + double audit, RLS canary, soft-delete excludes from list). Umbrella + landing + Sidebar + i18n wiring deferred to reconciliation slice.
- [ ] Super-admin Phase 3: MRR/ARR dashboard, cohort retention, churn, geographic distribution, announcements, tenant export/delete

### Phase 3 Reporting (DONE)

Shipped as 5 parallel subagent slices + foundation + reconciliation:

- **Foundation** — `ScheduledReport` model + 4 enums in `schema.prisma`. Migration `20260523000000_scheduled_reports` applied with RLS. Umbrella `apps/api/src/tenant/reports/reports.module.ts` aggregator registered in `tenant.module.ts`.
- **Slice 1 — P&L** (PAGES §38) — `apps/api/src/tenant/reports/pnl/`, `apps/web/src/app/[locale]/(shell)/reports/pnl/`. Revenue − Discount − Tax − COGS = Gross Profit, − Refunds = Net Revenue. Breakdown by period/branch/category/sku. CSV export. 6 tests.
- **Slice 2 — Movers / Margin** (PAGES §39) — `apps/api/src/tenant/reports/movers/`, `apps/web/.../reports/movers/`. Top-N ranking by revenue/units/profit + slow-movers section. 7-day sparkline per row. 6 tests.
- **Slice 3 — Trend analysis** (PAGES §41) — `apps/api/src/tenant/reports/trends/`, `apps/web/.../reports/trends/` + new shared `apps/web/src/components/charts/TrendLineChart.tsx`. 7/30/90-day rolling avg + YoY / prev-period overlay. 5 tests.
- **Slice 4 — Tax report** (PAGES §40) — `apps/api/src/tenant/reports/tax/`, `apps/web/.../reports/tax/`, new `apps/api/src/shared/pdf/tax-report-pdf.renderer.ts`. Per-tax-class breakdown + PDF + CSV. 6 tests.
- **Slice 5 — Scheduled email delivery** — `apps/api/src/tenant/reports/scheduled/` with BullMQ repeat-job per schedule (Redis-aware graceful fallback), `apps/web/.../reports/scheduled/` with CRUD + Run-now + recipient chip input. 7 tests.
- **Reconciliation** — Reports Center landing at `/[locale]/reports` (PAGES §36) with 5 role-gated cards. Sidebar "Reports" entry (owner/manager/accountant/auditor). All 5 inner modules registered in the umbrella + `ReportsModule` registered in `tenant.module.ts`. 5 i18n namespaces (`reports.{pnl,movers,trends,tax,scheduled}`) + `reports.center.*` + `shell.nav.reports` merged.
- **Verification** — `pnpm --filter @madar/api typecheck` clean; `pnpm --filter @madar/web typecheck` + `pnpm --filter @madar/web build` clean — 6 new routes (Reports Center + 5 report pages). `pnpm --filter @madar/api test -- reports` **30/30** (P&L 6 + Movers 6 + Trends 5 + Tax 6 + Scheduled 7). `pnpm --filter @madar/api test -- sales` **43/43** (no regressions). `pnpm --filter @madar/db test:rls` **82/82** (was 79 → +3 for `scheduledReport` canary). `pnpm --filter @madar/web i18n:check` **1923 keys EN/AR lockstep** (was 1722 → +201 for `reports.*` + center + nav).

#### Phase 3 deferred (still open)

- [ ] **Sales Summary page** (PAGES §37) — subsumed by owner dashboard for v1; revisit if §37 needs features beyond what the dashboard ships.
- [ ] **Custom Dashboard Builder** (PAGES §42) — drag-and-drop widget grid. Big multi-PR slice; defer.
- [ ] **Cash-flow snapshot** — needs supplier-payment tracking that isn't built yet.
- [ ] **XLSX exports** — CSV + PDF cover most needs; XLSX adds a binary dep for marginal value. Defer.
- [ ] **Multi-currency rollup** — needs an exchange-rate snapshot column on `Sale`. Same-currency-only with a warning is the v1 floor.
- [ ] **YoY overlay across DST shifts** — branch-timezone bucketing handles intra-tenant TZ correctly, but YoY overlays don't compensate for DST shifts. Documented edge case.
- [ ] **Scheduled report preview** — render the report in the UI before scheduling. Defer.
- [ ] **Per-tenant timezone for cron** — cron pattern is interpreted in the tenant's first-branch timezone; multi-TZ tenants get the same fire time across branches. Defer.
- [ ] **Pretty PDF renderers for P&L / Trends** — Slice 5 ships a basic Helvetica/Courier renderer; Slice 4 has the nice tax PDF. P&L + Trends PDF fidelity is a polish follow-up.
- [ ] **`scheduled_report` email template integration** — Slice 5 calls `EmailService.sendRaw()` directly today; swapping to a templated `EmailService.send()` is a small follow-up if/when the template land in `templates.ts`.

## Phase 3.5 — Critical POS-MVP gaps (store-readiness)

Cross-phase slice driven by the user's request to trial Madar in a real shop. Plan file: `~/.claude/plans/reactive-wiggling-stream.md`. Decisions: refund flow = cash + store credit (cashier picks); shifts = per-cashier open/close with Z-report; scope = features only, no prod hosting.

### A1 — Customers CRUD (DONE)
- [x] **Backend `apps/api/src/tenant/customers/`** — 5 endpoints under `TenantAuthGuard`, mirroring the branches-module shape. `GET /v1/customers?[search,page,limit]` (ILIKE across name/phone/email/code), `GET /v1/customers/:id` (detail + last 5 sales + sales_count + last_sale_at via parallel `tenantScoped` queries), `POST /v1/customers` (`@Idempotent`, any role — cashier needs to add walk-ins), `PATCH /v1/customers/:id` (owner/manager), `DELETE /v1/customers/:id` (owner only, soft-delete, blocks at 409 `has_store_credit` when balance≠0, `assertNotImpersonating()`). Conflict detection done as a pre-check `findFirst` rather than parsing Prisma's P2002 `meta.target` — that meta is unreliable for compound `(tenant_id, X)` uniques. Audit actions: `customer_created`, `customer_updated`, `customer_deleted`.
- [x] **Web API library** — `apps/web/src/lib/api/customers.ts` extended with `customersListRequest`, `customerGetRequest`, `customerCreateRequest`, `customerUpdateRequest`, `customerDeleteRequest`. Money on the wire stays a decimal string (BigInt cents), Number() at the formatting boundary only.
- [x] **Customers UI** at `/[locale]/customers/*` — list page with debounced search + table (name/phone/email/store-credit/sales-count/last-visit + Add-customer button), detail page with 3 tabs (Overview / Store credit / Sales history) + Edit + Delete (owner-only confirm), new + edit pages sharing one `CustomerForm`. `customers.css` (`.cu-*` prefix) uses design tokens + logical CSS only. Existing `/customers/[id]/store-credit` ledger route is left untouched and is reachable from the "Open ledger" CTA on the Store credit tab.
- [x] **Sidebar** — `customers` entry added between Branches and the money section, gated to all roles (cashier needs the picker, owner/manager edit + delete).
- [x] **POS customer picker** — `apps/web/src/app/[locale]/pos/_components/CustomerPickerModal.tsx` replaces the previously-hardcoded "Adam S." mock. Cart customer chip detaches on second click; otherwise opens the modal with debounced search (250ms) against `/v1/customers`. `CartCustomer` widened with `id` + `currency`; `customer_id` now flows through `holdMut`, `createSale`, and the offline-queued synthetic response (3 sites previously hardcoded to `null`). Modal has an inline "Add a new customer" deep link when the search returns no results.
- [x] **i18n** — `customers.*` namespace extended (~85 new keys: list/kpis/tabs/form/errors/confirm), `pos.customerPicker.*` namespace added (~7 keys). EN+AR lockstep at **1999 keys**.
- [x] **Tests** — `apps/api/test/customers/customers.spec.ts` — 8 integration tests all green: happy create + audit, duplicate phone 409, duplicate email 409, manager PATCH with before/after audit, RLS canary (tenant B sees 404 on tenant A's customer + missing in list), DELETE blocked when balance≠0 + unblocks when zeroed, cashier 403 on PATCH + DELETE but ok on list + create, search hits name/phone/email/code.
- [x] **Verification** — `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/api test -- customers` 8/8, `pnpm --filter @madar/web typecheck` clean, `pnpm --filter @madar/web build` clean (new routes: `/[locale]/customers` 8.39 kB / 190 kB, `/customers/[id]` 7.72 kB / 193 kB, `/customers/[id]/edit` 3.04 kB / 190 kB, `/customers/new` 3.92 kB / 181 kB), `pnpm i18n:check` 1999 keys lockstep.

### A2 — Cart tax preview in POS (DONE)
- [x] **Backend** — `apps/api/src/tenant/catalog/catalog.service.ts` extended with a `resolveTaxRates(tenantId, products)` helper that one-shot reads `tenants.default_tax_class_id` via `adminPrisma` plus a batched `tenantScoped.taxClass.findMany` for the union of (product-level class ids ∪ tenant default), then resolves per-product `tax_rate_pct = tax_class.rate_bps / 100`. Effective rate = product's own class first, then tenant default, else `null`. Wired into both `listProducts` and `getProduct`. `TenantDto` in `apps/api/src/tenant/auth/auth.service.ts` widened with `default_tax_class_id` + `tax_inclusive_default` (the underlying columns ship in `Tenant` already; just needed to project). `toTenantDto` was the only mapper to touch.
- [x] **Frontend** — `ApiProduct` (`apps/web/src/lib/api/catalog.ts`), `AuthTenant` (`apps/web/src/lib/auth/store.ts`), and the parallel `AuthTenantShape` (in `apps/web/src/lib/api/client.ts`) + `MeResponse.tenant` (in `apps/web/src/app/[locale]/impersonation-handoff/client.tsx`) all gained the new fields. `pos-client.tsx` now reads `taxInclusive` from `useAuthStore` and computes a per-line preview: inclusive mode extracts `price * rate / (100 + rate)`; exclusive mode adds `price * rate / 100`. Cart displays a Tax row (skipped when 0) labeled "Tax" or "Tax (included)" depending on mode. `PaymentSheet` now receives the real `taxInclusive` from the tenant DTO rather than a hardcoded `false`. The previous mock-data `tax = 0` is gone — server still re-computes authoritatively on `completeSale`, so the preview is fail-soft.
- [x] **i18n** — replaced the old `pos.cart.tax = "VAT 14%"` literal with neutral `pos.cart.tax = "Tax"` + new `pos.cart.taxIncluded = "Tax (included)"`. EN+AR lockstep at **2000 keys**.
- [x] **Tests** — `apps/api/test/catalog/product-tax-rate.spec.ts` (2 cases): list returns `tax_rate_pct = 14` for products inheriting the tenant default + `tax_rate_pct = 0` for a product overridden onto a zero-rate class; `/v1/auth/me` returns the new `tenant.default_tax_class_id` + `tenant.tax_inclusive_default` fields.
- [x] **Verification** — `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/web typecheck` clean (caught + fixed `AuthTenantShape` in `client.ts` + the placeholder tenant literal in `impersonation-handoff/client.tsx` + the `catalog-cache.test.ts` product fixture missing `tax_rate_pct`), `pnpm --filter @madar/api test -- catalog customers` **50/50** (8 customers + 42 catalog) green, `pnpm i18n:check` 2000 keys lockstep.

### A3 — Tenant suspension banner + route gate (DONE)
- [x] **Backend status gate** — extended `TenantAuthGuard` in `apps/api/src/tenant/auth/tenant-auth.guard.ts` to reject POST/PATCH/PUT/DELETE with HTTP **423 `tenant_suspended`** when `tenant.status` is `suspended` or `cancelled`. Reads + the small allowlist (`/v1/auth/logout`, `/v1/auth/refresh`, `/v1/payment-proofs`, `/v1/impersonation/exit`) stay open. The lookup is Redis-cached for 30s via the new `apps/api/src/tenant/auth/tenant-status.cache.ts` helper (key `tenant-status:<tenantId>`, falls back to `RedisService`'s in-memory store when `REDIS_URL` is unset). `BillingTrackerService.runDailyTick()` now calls `invalidateTenantStatus(tenantId, redis)` after every `active → grace_period → suspended → cancelled` transition so writes unblock (or re-block) immediately instead of waiting 30s for TTL.
- [x] **Frontend banner** — `apps/web/src/app/[locale]/(shell)/_components/SubscriptionBanner.tsx` mounted in `Shell.tsx` between `<ImpersonationBanner />` and `<Sidebar />`. Four modes: (a) trial ending in ≤3 days → coral nudge "Trial ends in N days"; (b) `grace_period` → amber "Subscription payment overdue"; (c) `suspended` → rose "Read-only until paid"; (d) `cancelled` → rose "Export your data". All carry a "Pay now" CTA linking to `/billing`. Plural-aware copy in EN+AR via `next-intl`.
- [x] **Client-side route gate** — same banner component runs a `useEffect` that calls `router.replace('/billing')` when `status ∈ {suspended, cancelled}` AND the current path is not on the allowlist (`/billing`, `/settings/security`, auth routes). Mirrors the backend allowlist so the UI never lands a user on a screen the server will 423 on next mutation.
- [x] **i18n** — new `subscription.banner.*` namespace (10 keys EN+AR, ICU plurals). Lockstep at **2009 keys**.
- [x] **Tests** — `apps/api/test/tenant-status/tenant-status.spec.ts` 4/4 green: active → POST 201; suspended → POST 423 + correct `code` + `status` payload; suspended → GET 200 + `/v1/auth/me` 200 + `/v1/auth/logout` not-423 (allowlist verified); cache-invalidate + flip-back-to-active unblocks the next POST in the same suite (the billing-tracker → cache integration path).
- [x] **Verification** — `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/api test -- tenant-status` 4/4, `pnpm --filter @madar/web typecheck` clean, `pnpm i18n:check` 2009 keys lockstep.

### B1 — Cashier shifts + cash drawer + Z-report (DONE)
- [x] **Schema migration `20260525000000_cashier_shifts`** — new `cashier_shifts` table (tenant-scoped, RLS via canonical `NULLIF` `tenant_isolation` policy) + `CashierShiftStatus` enum (`open | closed`) + partial unique index `(tenant_id, cashier_id) WHERE status='open' AND deleted_at IS NULL` so each cashier has at most one open shift. Also `ALTER TABLE sales ADD COLUMN shift_id UUID REFERENCES cashier_shifts(id) ON DELETE SET NULL` with a partial index on the non-null shape. `updated_at` trigger reuses `fn_set_updated_at()`.
- [x] **Backend** — new `apps/api/src/tenant/shifts/` module mirroring the stock-transfers/branches templates. Endpoints: `GET /v1/shifts/current` (caller's open shift or `null`), `GET /v1/shifts?[branch_id,status,from,to,page,limit]` (paginated; cashiers see own only, managers see all), `GET /v1/shifts/:id` (detail including Z-report breakdown — by-payment, top-5 products, transactions/items totals), `POST /v1/shifts/open` (cashier+, `@Idempotent`, 10/min; cashier branch-locked via `forbidden_branch`; 409 `shift_already_open`), `POST /v1/shifts/:id/close` (cashier of the shift OR owner/manager; computes `expected = opening_float + Σ cash sale_payments for shift` and `variance = declared - expected` in one query; 409 `shift_already_closed`). Audit actions: `shift_opened`, `shift_closed`.
- [x] **completeSale hook** — `apps/api/src/tenant/sales/sales.service.ts:completeSale` now looks up the cashier's open shift on the sale's branch inside its own `basePrisma.$transaction` and stamps `sale.shift_id` when one exists. Sales without an open shift still complete (back-compat with existing flows + tests). All 4 shift tests + the existing sales suite pass.
- [x] **Web API library** — `apps/web/src/lib/api/shifts.ts` typed wrappers: `shiftCurrentRequest`, `shiftsListRequest`, `shiftGetRequest`, `shiftOpenRequest`, `shiftCloseRequest`. Money on the wire as decimal strings (`Number(...)` at the formatting edge only).
- [x] **POS integration** — new `ShiftGate` wrapper in `pos-client.tsx`: queries `/v1/shifts/current`; when null and the cashier has a `branch_id`, renders `<OpenShiftModal>` and hides the POS sell screen until a shift opens; when open, exposes shift summary through a render-prop the header consumes. `PosHeader` got a new `shiftSlot` prop that renders the existing chip slot — old hardcoded "Mariam S. · shift #247" copy replaced with the real `<ShiftChip>` (elapsed time, formatted opening float, "End shift" button). `EndShiftModal` collects declared cash + notes, posts, then routes to `/shifts/:id` for the Z-report.
- [x] **Shifts list + Z-report pages** — `/[locale]/shifts` with tabs (Open / Closed / All) + dense table; `/[locale]/shifts/[id]` Z-report with cash reconciliation card (opening float / cash sales / refunds / expected / declared / variance — colored sage/rose by sign), by-payment-method breakdown, top-products list, and summary aside (gross revenue / transactions / items sold). `@media print` strips the shell chrome so `window.print()` produces a clean Z-report.
- [x] **Sidebar** — new "Shifts" entry under the money section with the ClipboardList icon, gated to `owner | manager | cashier | accountant`.
- [x] **i18n** — `shifts.*` + `pos.shift.*` namespaces in EN+AR (~80 new keys), lockstep at **2083 keys**.
- [x] **Tests** — `apps/api/test/shifts/shifts.spec.ts` (4 cases): cashier opens a shift + `/current` returns it + second open returns `shift_already_open` + audit row written; sale during shift stamps `shift_id` + close computes expected/variance correctly + Z-report rolls up gross revenue + re-close returns `shift_already_closed`; close with under-count surfaces a negative variance; RLS canary — tenant B receives 404 on tenant A's shift and tenant A's row never leaks into tenant B's list.
- [x] **Verification** — `pnpm db:migrate` applied cleanly, Prisma client regenerated, `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/api test -- shifts` 4/4 green, `pnpm --filter @madar/web typecheck` clean, `pnpm i18n:check` 2083 keys lockstep.

### B2 — Customer refunds / sale returns (DONE — backend + UI)
- [x] **Schema migration `20260526000000_sale_refunds`** — 3 new tenant-scoped tables (RLS via canonical `NULLIF` policy): `sale_refunds` (header with `code` RFN-XXXXXX unique per tenant, currency, subtotal/tax/total, status enum `completed | voided`, FKs to sales + cashier_shifts), `sale_refund_lines` (FK to sale_lines + qty + snapshotted unit_price/tax + restock flag, `CHECK qty > 0`), `sale_refund_payments` (mirrors sale_payments shape — method/amount/approval_code/store_credit_ledger_id). Plus `ALTER TABLE sales ADD COLUMN refunded_amount_cents BIGINT NOT NULL DEFAULT 0` as a denormalized "is fully refunded" counter.
- [x] **Backend** — `apps/api/src/tenant/sale-refunds/` module: `GET /v1/sale-refunds?[branch_id,sale_id,from,to,page,limit]` (paginated, cashiers see own), `GET /v1/sale-refunds/:id` (detail with lines + payments), `POST /v1/sale-refunds` (`@Idempotent`, 20/min, `assertNotImpersonating` per CLAUDE.md). Service runs one `basePrisma.$transaction` with `SET LOCAL app.current_tenant_id` and `SELECT ... FOR UPDATE` on the parent sale row (mirrors store-credit lock pattern). Per-line validation against `sale_line.qty - already_refunded_qty(line)` via a `groupBy` over prior refunds. Price + tax are **snapshotted from the original sale_line** so refunds happen at the price the customer paid. Restock=true (default) writes one `stock_movement{kind:'return_in'}` + upserts `branch_stock`. Store-credit payment slice locks the customer row via raw SQL `FOR UPDATE`, inserts a positive `store_credit_ledger` entry, updates the denormalized balance, and backfills the ledger's `reference_id` after the refund row exists. Updates `sales.refunded_amount_cents` and flips `payment_status='refunded'` when fully refunded. Audit `sale_refunded` with `{refund_id, code, total_cents, line_count, method}`.
- [x] **Manager-approval gate** — cashier-initiated refunds above `REFUND_MANAGER_THRESHOLD_CENTS` (env, default 5000 = $50) require `approved_by_user_id` in the body referencing an active owner or manager; backend re-verifies the approver's role. Without the approver: 403 `manager_approval_required` with the threshold in the response details.
- [x] **Shift attach** — refunds stamp `shift_id` from the cashier's current open shift on the sale's branch (same lookup pattern as `completeSale`). The B1 Z-report can then deduct cash refunds from expected cash in a future refinement (the `cash_refunds_cents` field is stubbed to 0 today — a join against `sale_refund_payments.method='cash'` will fill it in).
- [x] **Web API library** — `apps/web/src/lib/api/sale-refunds.ts` with `saleRefundsListRequest`, `saleRefundGetRequest`, `saleRefundCreateRequest`. Money on the wire stays a string per the bigint convention.
- [x] **Tests** — `apps/api/test/sale-refunds/sale-refunds.spec.ts` (7 cases) all green: full cash refund flips sale.payment_status to `refunded` + restocks via `return_in` movement + audits; partial refund leaves status `paid` + advances `refunded_amount_cents` + a follow-up refund over the remaining qty returns 422 `qty_exceeds_remaining`; payment-sum mismatch returns 422 `refund_total_mismatch`; RLS canary (tenant B refunding tenant A's sale → 404); manager-approval gate — cashier refund above threshold rejected without approver (403 `manager_approval_required`) + accepted with the owner as approver (`requires_manager: true`, `approved_by_user_id` populated); store-credit refund creates a positive ledger entry, sets customer's `store_credit_balance_minor` to the refund total, and `store_credit_currency_code` to the sale's currency; store-credit without `customer_id` returns 400 `store_credit_requires_customer`.
- [x] **Verification** — `pnpm db:migrate` applied cleanly + Prisma client regenerated, `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/api test -- sale-refunds` 7/7 green, full new-module suite (`customers tenant-status cron shifts sale-refunds`) **27/27** green together, `pnpm --filter @madar/web typecheck` clean.

#### B2 UI follow-up (DONE)
- [x] **Sales history list** at `/[locale]/sales` — new `GET /v1/sales?[branch_id,customer_id,payment_method,payment_status,from,to,page,limit]` endpoint (`apps/api/src/tenant/sales/dto/list-sales.dto.ts` + `sales.service.list()` + controller `@Get()`). Reader = any authed user; **cashier sees own only**. Hydrates `branch_code` + `cashier_name` + `line_count` via batched secondary queries. New `SalesListClient` at `apps/web/src/app/[locale]/(shell)/sales/{page,sales-client}.tsx` + `sales.css`: date range (default last 7d) + branch + status chip filters + method dropdown, paginated 50/page, status-toned pills, row click → `/sales/[id]/receipt`. Sidebar entry flipped `enabled: true` (`Sidebar.tsx:48`).
- [x] **Refund wizard** at `/[locale]/(shell)/sales/[id]/refund/{page,refund-client}.tsx` + `refund.css`. 3-step flow (Pick lines → Method → Review). Step 1: per-line qty stepper capped at `sale_line.qty - already_refunded_qty(line)` (refunds-list query feeds the cap); restock checkbox per line; live total uses snapshot prices with proportional tax (`tax_snapshot * qty_picked / sale_line.qty`). Step 2: single-method radio (Cash / Card / Bank transfer / Store credit); card requires approval code; store-credit shows a `CustomerPicker` modal when no customer is attached. Step 3: review summary + submit. Errors mapped under `refunds.errors.*` (`refund_total_mismatch`, `qty_exceeds_remaining`, `currency_mismatch`, `store_credit_requires_customer`, `network`). On success: `window.location.assign('/{locale}/sales/{id}/receipt?refunded={RFN-CODE}')`.
- [x] **Manager-approval modal** — backend returns 403 `manager_approval_required` with `details.threshold_cents` (extended `ApiError` + `ApiErrorPayload` in `apps/web/src/lib/api/client.ts` to carry the new `details?: Record<string, unknown>`). When the error fires, the wizard opens an in-modal approver picker. New tenant endpoint `GET /v1/users/approvers` (open to any authed user, returns active owners + managers `[{ id, name, role }]`) feeds the picker. New `approversListRequest` in `apps/web/src/lib/api/users.ts`. Cashier picks an approver; backend re-verifies the role inside the refund transaction.
- [x] **Refund button on receipt** (`apps/web/src/app/[locale]/sales/[id]/receipt/receipt-doc.tsx`) — role-gated (`cashier|manager|owner|accountant`) + status-gated (`payment_status === 'paid'` only). Links to `/{locale}/sales/{id}/refund`. New `.no-print a.receipt-refund-link` style in `receipt.css`. New i18n key `receipt.buttons.refund`.
- [x] **B1 Z-report `cash_refunds_cents`** — `shifts.service.ts:buildZReport` now runs a `saleRefundPayment.aggregate` joined to `sale_refunds.shift_id` where `method='cash' AND deleted_at IS NULL` and surfaces the sum. `close()` updated: `expected_closing_cash = opening_float + Σ(cash sale_payments) − Σ(cash sale_refund_payments)` so variance reflects post-refund expected. New test case in `apps/api/test/shifts/shifts.spec.ts` (sale 7000 → partial cash refund 3500 → close declared 23500 → expected 23500, variance 0, cash_refunds_cents 3500).
- [x] **i18n** — new top-level `refunds.*` namespace (~70 keys) and `salesList.*` namespace (~30 keys) added EN + AR. `receipt.buttons.refund` + `shell.nav.sales` filled in. `pnpm i18n:check` **2194 keys lockstep** (was 2083 → +111).
- [x] **Verification** — `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/api test -- shifts sale-refunds sales users` **67/67** green (5 shifts + 7 sale-refunds + the existing sales suite + users), `pnpm --filter @madar/web typecheck` + `pnpm --filter @madar/web build` clean (new routes: `/[locale]/sales` 5.49 kB / 187 kB First Load, `/[locale]/sales/[id]/refund` 6.2 kB / 192 kB).

### C1–C3 — BullMQ cron + trial reminders + low-stock alerts (DONE)
- [x] **Schema migration `20260524000000_cron_dedup_columns`** — adds two nullable timestamp columns: `tenants.trial_reminder_sent_at` (flipped once per tenant when the trial-ending reminder fires) and `branch_stock.last_low_stock_alert_at` (bumped on each row included in a low-stock digest, for 24h dedup).
- [x] **New email template `low_stock_alert`** in `apps/api/src/common/email/templates.ts` + new `LowStockAlertVars` interface and `SendInput` union entry in `email.types.ts`. Bilingual EN+AR with RTL shell, table-of-items rendered server-side (per-row HTML + text), overflow note when item count exceeds the digest cap (50). Item rows are escaped against HTML injection.
- [x] **C1 — `apps/api/src/admin/cron/` module** mirrors the PO email queue pattern. Six files: `cron.types.ts` (queue/job constants + payload + report types + tuned constants — `TRIAL_REMINDER_WINDOW_MIN_DAYS=2`, `TRIAL_REMINDER_WINDOW_MAX_DAYS=3`, `LOW_STOCK_DEDUP_HOURS=24`, `LOW_STOCK_DIGEST_CAP=50`), `cron.service.ts` (`AdminCronService` with `runTrialReminderTick` and `runLowStockTick`), `cron.processor.ts` (BullMQ `WorkerHost` dispatching by job name), `bootstrap-cron.service.ts` (`OnApplicationBootstrap` registers two `repeat` jobs at `0 8 * * *` UTC — overridable via `ADMIN_CRON_PATTERN` env — with stable `jobId`s so restarts don't accumulate duplicates), `cron.controller.ts` (manual `POST /v1/admin/cron/{trial-reminders,low-stock}/run-now` — Platform Owner / Finance only, `@Idempotent`, 4/min rate limit), `cron.module.ts` (Redis-aware `forRoot()` — wires BullMQ when REDIS_URL is set, registers only the service + controller when unset so manual triggers still work but the daily schedule is skipped with a logged warning). Wired into `apps/api/src/admin/admin.module.ts`.
- [x] **C2 — `runTrialReminderTick`** queries `adminPrisma.tenant.findMany({where: {status:'trialing', trial_ends_at ∈ [now+2d, now+3d), trial_reminder_sent_at: null}})`, sends the existing `trial_ending` template via `getTenantPrimaryRecipient`, stamps `trial_reminder_sent_at = now()`. Writes `platform_audit_log` `action='trial_reminder_sent'` with `{days_left, recipient_email}` metadata. Skips tenants with no active owner (returns `skipped_no_recipient` in the report).
- [x] **C3 — `runLowStockTick`** scans active tenants (`trialing|active|grace_period`), and for each runs one raw SQL JOIN against `branch_stock ↔ products ↔ branches` filtering on `qty_on_hand <= reorder_point` + `(last_low_stock_alert_at IS NULL OR last_low_stock_alert_at < now()-24h)`. One digest email per tenant to the owner with the (capped) item list and per-row `qty_on_hand / reorder_point`. Bumps `last_low_stock_alert_at` on every included row in one `updateMany`. Writes `platform_audit_log` `action='low_stock_alert_sent'` with `{item_count, capped_to}`.
- [x] **Env** — new optional `ADMIN_CRON_PATTERN` env var. Defaults to `0 8 * * *` UTC.
- [x] **Tests** — `apps/api/test/cron/cron.spec.ts` (4 cases): readonly admin 403 on both endpoints; in-window trial tenant emailed exactly once with `.eml` on disk + `trial_reminder_sent_at` set + second run produces zero new emails; out-of-window trial tenant (30d) is NOT emailed (column stays null); low-stock product at threshold triggers a digest .eml + `last_low_stock_alert_at` is bumped + audit row written + a retry within the dedup window produces no second email for the same row.
- [x] **Verification** — `pnpm --filter @madar/api typecheck` clean, `pnpm --filter @madar/api test -- cron` **4/4** green, `pnpm db:migrate` applied the new migration cleanly (Prisma client regenerated after killing stale node processes that were holding the engine .dll — a known Windows + OneDrive flake noted in tasks.md).

## Phase 4 — Ecosystem

- [ ] Public REST API + webhooks
- [ ] QuickBooks / Xero connectors
- [ ] Loyalty & customer segmentation
- [ ] Native mobile apps (iOS, Android)
- [ ] Super-admin Phase 4: webhook logs, compliance reports, API key management, ~~plan editor~~ (pulled forward — see below), discount codes
- [x] **A31 + A32 (plan editor)** pulled forward from Phase 4 — needed at install time so VPS deploys can create plans through the admin UI instead of running the demo seed. `apps/admin/src/app/(shell)/plans/` (list + `[id]` editor, owner-gated), `apps/api/src/admin/plans/` (controller + service + zod DTOs + audit writes), sidebar nav in "Pricing" section. Also adds `pnpm db:bootstrap-admin` for first-run super-admin (no demo data).

---

## Design bundle map (`docs/design/`)

| File | Maps to phase task |
|---|---|
| `project/design-tokens.css` | 1.3 design system |
| `project/i18n-ar.js` | 1.2 i18n |
| `project/icons.jsx` | 1.3 (use Lucide instead, this is reference) |
| `project/data.js` | 1.4 seed data |
| `project/Madar.html` | 1.3 app shell (sidebar + topbar) |
| `project/Madar Auth.html` | 1.5 auth pages |
| `project/Madar Admin.html` | 1.14 admin app |
| `project/Madar Print.html` | 1.15 print receipts |
| `project/screen-checkout.jsx` | **1.10 POS sell (priority)** |
| `project/screen-dashboard.jsx` | 1.12 reports + 2 owner dashboard |
| `project/screen-inventory.jsx` | 1.8 + 1.9 catalog + inventory |
| `project/screen-suppliers-branches.jsx` | Phase 2 suppliers + branches |
| `project/screen-analysis.jsx` | 1.12 + Phase 3 reporting |
| `project/screen-billing.jsx` | 1.13 subscription billing |
| `project/screen-extras.jsx` | Phase 2 sales records, POs, reconciliation, transfers, settings, onboarding |
| `project/admin-app.jsx`, `admin-screens.jsx`, `admin-verification.jsx`, `admin-data.js` | **1.14 super-admin MVP** |
| `project/components.jsx` | 1.3 base components (reference for styling) |
| `project/tweaks-panel.jsx` | Internal dev tool — skip for production |

---

## Recommended first vertical slice

To prove the design language and stack end-to-end before going wide, build a **single working slice**:

1. Scaffolding (1.1)
2. i18n infrastructure (1.2)
3. Design system tokens (1.3)
4. POS Sell Screen (1.10) — using mock data, no backend yet

That gives a runnable, bilingual, design-system-correct screen in the agreed stack. Backend, DB, auth, and the rest of Phase 1 then layer on without revisiting the visual foundation.

---

## Phase 3.6 — Self-host hardening (DONE — 8 slices)

Eight self-host readiness items shipped in one push. Each was scoped tight and reviewed independently; all gated on typecheck + targeted tests.

- [x] **#7 Owner-resets-staff-password** — `POST /v1/users/:id/reset-password` (owner-only, 5/min, `assertNotImpersonating`) mints a token via the existing `password_reset` template machinery and sends it to the recipient. New `UsersClient` row action between "Resend invite" and "Deactivate", visible only when the staff member has already verified their email. **15/15 tests** in `apps/api/test/users/users.spec.ts` (3 new). 6 new i18n keys.
- [x] **#1 Daily Postgres backups** — `infra/backup/{Dockerfile,backup.sh,restore.sh,entrypoint.sh}` runs `supercronic` + `pg_dump --format=custom` + `aws s3 cp` with 30-day rotation. `docker-compose.yml` gains a `backup` service under the `backup` profile (opt-in: `--profile backup`). Manual one-off via `docker compose run --rm backup backup`. Restore via `restore latest` or a specific key. New `docs/operations/backups.md` covers env, restore steps, monthly disaster-recovery test. Bash scripts pass `bash -n`; compose merges cleanly.
- [x] **#4 Cash-drawer kick from POS** — new `apps/web/src/lib/hardware/cash-drawer.ts` WebUSB helper + `webusb.d.ts` ambient types (30-line shim; avoids adding `@types/w3c-web-usb`). Receipt page auto-pops the drawer once per mount on cash receipts when a paired Star/Epson is detected; otherwise the **Open drawer** button triggers the one-time pairing dialog. New `docs/operations/hardware-setup.md` covers the 95%-case printer DIP-switch route. 5 new i18n keys.
- [x] **#5 Receipt logo + branding** — migration `20260528000000_tenant_logo` adds `tenants.logo_url`. Business module gains `POST /v1/tenant/logo` (owner-only multipart) + `DELETE /v1/tenant/logo` + `GET /v1/public/tenants/:id/logo` (`@Public` for receipt `<img src>`). Mirrors the product-image pipeline (5MB cap / magic-byte MIME / sharp resize+EXIF strip / storage abstraction). New `LogoSection` card on `/settings/business`; receipt page renders the logo above the shop name. **7/7 business tests** still green. 11 new i18n keys.
- [x] **#3 Bulk product CSV import** — new `apps/api/src/tenant/catalog/csv-import.service.ts` (~50-line standalone RFC 4180 parser — no new deps). `POST /v1/products/import` (owner|manager, ≤2MB/≤5000 rows, `?dry_run=1`, upsert by SKU). CSV columns: `sku, name_en, name_ar, category_code, price_cents, cost_cents, barcode, tax_class_code, branch_code, initial_qty, is_active`. New `ImportCsvModal` triggered from `InventoryHeader` — download-template link + dry-run preview + import with per-row error report (capped 100 displayed). 19 new i18n keys.
- [x] **#9 Notifications matrix** — migration `20260529000000_notification_preferences` adds the table + 2 new enums (`NotificationEventType`: 9 events; `NotificationChannel`: email + in_app). Unique `(tenant_id, event_type, channel)` + canonical NULLIF RLS policy. New `apps/api/src/tenant/notifications/` module with `GET/PATCH /v1/notifications/preferences` (owner|manager). Auto-seeds defaults on first read. 60s in-memory cache + invalidate on PATCH. Cron handlers (`runTrialReminderTick` + `runLowStockTick`) now gate on `notificationPreference.findUnique({ event_type×email })` before sending — defaults to enabled when no row exists. New `/settings/notifications` page with event × channel matrix. New SettingsShell entry `notifications` (visible to owner|manager). 30 new i18n keys.
- [x] **#8 End-of-day reconciliation page** — new `apps/api/src/tenant/reconcile/` module: `GET /v1/reconcile/day?date&branch_id?` returns per-branch + chain totals (gross revenue, transactions, items, cash sales, cash refunds, opening float, expected/declared/variance, by-payment breakdown). Reuses the `buildZReport` SQL pattern (sale_payments + sale_refund_payments JOINs). New `/[locale]/reconcile` page with date picker, chain card + per-branch panels with shift detail rows + by-payment breakdown, print-friendly (`@media print` hides controls). Sidebar `reconcile` entry flipped `enabled: true` (owner|manager|accountant). 24 new i18n keys.
- [x] **#2 Production deployment runbook** — new `infra/docker-compose.prod.yml` overlay (caddy reverse proxy + always-on restart + named volumes), `infra/caddy/Caddyfile` (auto-LE HTTPS for 3 subdomains, HSTS + zstd/gzip + `Permissions-Policy: usb=(self)` for the WebUSB drawer), `.env.production.example` (every secret documented), `infra/Dockerfile.{api,web,admin}` (multi-stage Node 20 builds with pnpm + Prisma generate). New `docs/deployment.md` — step-by-step VPS guide (Hetzner CX22 €4/mo): provision → install Docker → DNS → clone + env → migrations → first deploy → bootstrap tenant → smoke checklist → update/restore procedures. New `docs/operations/incident-runbook.md` covers the most common failure modes (site down, disk full, API 500, login emails missing, backup didn't run, restore steps).

**Deferred (#6 e-invoicing)** — Egypt ETA Phase 2 / Saudi ZATCA Phase 2 / UAE FTA. Each ~3-5 days; not legally required under threshold (~EGP 5M/yr in Egypt). Documented as Phase 4 — revisit when revenue or a B2B customer pulls it in.

### Verification

- API typecheck clean. New test totals: users 15/15, business 7/7. Migrations applied: `tenant_logo`, `notification_preferences`.
- Web typecheck + build clean. New routes: `/[locale]/settings/notifications`, `/[locale]/reconcile`.
- i18n lockstep at **2421 keys** (was 2324 → +97 across slices 1, 3, 4, 5, 6, 7).
- New infrastructure: `infra/backup/` (backup container), `infra/caddy/` (reverse proxy config), `infra/docker-compose.prod.yml`, `infra/Dockerfile.{api,web,admin}`.
- New docs: `docs/operations/backups.md`, `docs/operations/hardware-setup.md`, `docs/operations/incident-runbook.md`, `docs/deployment.md`.

---

## Working agreements

- **Read first** before starting any task: `CLAUDE.md`, the design reference for that task, and any prior ADR in `docs/decisions/`.
- **Never skip** the security checklist at the bottom of `CLAUDE.md` for any feature touching data.
- **One PR per concern**, screenshots in LTR + RTL + light + dark (tenant) or light + dark (admin).
- **No hardcoded** strings, colors, sizes, shadows. Tokens only.
- **No payment gateway**, no Stripe. Bank transfer + receipt + manual verification only.
- **No mocked database** in integration tests — RLS is the load-bearing safety net.
